import { notFound } from 'next/navigation'

/**
 * Locale-prefixed Catch-all Route für Puck-CMS-Pages (temporär deaktiviert).
 * Wird reaktiviert sobald Puck wieder live geschaltet wird.
 */
export default function LocaleCmsPage() {
  return notFound()
}
