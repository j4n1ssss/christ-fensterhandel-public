---
phase: 04-dashboards-und-rollen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/access/is-admin.ts
  - src/access/is-admin-or-mitarbeiter.ts
  - src/access/is-own-anfrage.ts
  - src/access/role-checks.ts
  - src/lib/status-transitions.ts
  - src/collections/system/users.ts
  - src/collections/business/anfragen.ts
  - src/collections/business/status-historie.ts
  - src/collections/business/preisregeln.ts
  - src/collections/business/rabattcodes.ts
  - src/collections/produkte/produkttypen.ts
  - src/collections/produkte/materialien.ts
  - src/collections/produkte/profile.ts
  - src/collections/produkte/fluegelanzahl.ts
  - src/collections/produkte/zusatzlichter.ts
  - src/collections/produkte/oeffnungsarten.ts
  - src/collections/produkte/fensterformen.ts
  - src/collections/ausstattung/farben.ts
  - src/collections/ausstattung/dichtungsfarben.ts
  - src/collections/ausstattung/verglasungen.ts
  - src/collections/ausstattung/schallschutz.ts
  - src/collections/ausstattung/sicherheitsglas.ts
  - src/collections/ausstattung/glasdekore.ts
  - src/collections/ausstattung/sprossen.ts
  - src/collections/ausstattung/extras.ts
  - src/collections/system/media.ts
  - tests/unit/test-access-control.test.ts
  - tests/unit/test-status-transitions.test.ts
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - SEC-03
  - ADMIN-04
  - ADMIN-05
  - ADMIN-06

must_haves:
  truths:
    - "Unauthenticated API calls to /api/anfragen return 401 (not open data)"
    - "Mitarbeiter can update Anfrage status but cannot delete Anfragen or edit Preise/Kontaktdaten"
    - "Kunde can only read Anfragen matching their email"
    - "Status transitions follow defined workflow (NEU->IN_BEARBEITUNG->BESTAETIGT->BEZAHLT->ABGESCHLOSSEN + RUECKFRAGE/ABGELEHNT)"
    - "Kommentar is required for RUECKFRAGE and ABGELEHNT transitions"
    - "Interne Notizen field is only readable/writable by Admin/Mitarbeiter/Viewer"
  artifacts:
    - path: "src/access/is-admin.ts"
      provides: "Admin role check access function"
      exports: ["isAdmin"]
    - path: "src/access/is-admin-or-mitarbeiter.ts"
      provides: "Admin or Mitarbeiter role check"
      exports: ["isAdminOrMitarbeiter"]
    - path: "src/access/is-own-anfrage.ts"
      provides: "Kunde query constraint for own Anfragen"
      exports: ["isOwnAnfrage"]
    - path: "src/lib/status-transitions.ts"
      provides: "Valid status transition map and validation"
      exports: ["VALID_TRANSITIONS", "COMMENT_REQUIRED", "isValidTransition"]
    - path: "tests/unit/test-access-control.test.ts"
      provides: "Unit tests for access control functions"
    - path: "tests/unit/test-status-transitions.test.ts"
      provides: "Unit tests for status transition logic"
  key_links:
    - from: "src/collections/business/anfragen.ts"
      to: "src/access/*.ts"
      via: "access config block importing access functions"
      pattern: "import.*from.*@/access"
    - from: "src/collections/business/anfragen.ts"
      to: "src/lib/status-transitions.ts"
      via: "beforeChange hook validating transitions"
      pattern: "isValidTransition|COMMENT_REQUIRED"
    - from: "src/collections/system/users.ts"
      to: "rolle field"
      via: "saveToJWT: true on rolle field"
      pattern: "saveToJWT.*true"
---

<objective>
Security foundation: Access control policies on ALL collections, status transition validation, saveToJWT on rolle field, and unit tests.

Purpose: Without access control, all API endpoints are open. Without status transition validation, anyone can set any status. This plan locks down the entire system before building dashboards.
Output: Access control utility functions, status transition logic, updated collection configs, unit tests.
</objective>

<execution_context>
@/Users/janisjankewitz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/janisjankewitz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dashboards-und-rollen/04-CONTEXT.md
@.planning/phases/04-dashboards-und-rollen/04-RESEARCH.md

<interfaces>
<!-- Existing code the executor needs -->

From src/collections/system/users.ts:
```typescript
// rolle field exists but MISSING saveToJWT: true -- MUST be added
{
  name: 'rolle',
  type: 'select',
  defaultValue: 'viewer',
  options: [
    { label: 'Admin', value: 'admin' },
    { label: 'Mitarbeiter', value: 'mitarbeiter' },
    { label: 'Viewer', value: 'viewer' },
    { label: 'Kunde', value: 'kunde' },
  ],
}
```

From src/collections/business/anfragen.ts:
```typescript
// Status field values:
'neu' | 'in_bearbeitung' | 'bestaetigt' | 'bezahlt' | 'abgeschlossen' | 'rueckfrage' | 'abgelehnt'

// Existing beforeChange hook creates StatusHistorie entries on status change
// Must be EXTENDED with transition validation, NOT replaced

// interne_notizen field exists but has NO access control yet
{
  name: 'interne_notizen',
  type: 'textarea',
  label: 'Interne Notizen',
}

// kontaktdaten is a group field with nested email:
// kontaktdaten.email -- used for Kunde query constraint
```

From src/collections/business/status-historie.ts:
```typescript
// Already immutable: update: () => false, delete: () => false
// Has: anfrage, von_status, zu_status, geaendert_von, zeitpunkt, kommentar
```

From src/payload.config.ts:
```typescript
// admin.user: Users.slug -- already configured
// No custom views yet -- will be added in Plan 02
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Access control utilities + Status transition logic + Tests</name>
  <files>
    src/access/is-admin.ts,
    src/access/is-admin-or-mitarbeiter.ts,
    src/access/is-own-anfrage.ts,
    src/access/role-checks.ts,
    src/lib/status-transitions.ts,
    tests/unit/test-access-control.test.ts,
    tests/unit/test-status-transitions.test.ts
  </files>
  <behavior>
    - isAdmin returns true only for rolle='admin', false for all others and unauthenticated
    - isAdminOrMitarbeiter returns true for admin and mitarbeiter, false for viewer/kunde/unauthenticated
    - isOwnAnfrage returns true for admin/mitarbeiter/viewer, returns query constraint { 'kontaktdaten.email': { equals: user.email } } for kunde, returns false for unauthenticated
    - isValidTransition('neu', 'in_bearbeitung') returns true
    - isValidTransition('neu', 'abgeschlossen') returns false (skip states)
    - isValidTransition('abgelehnt', 'neu') returns true (admin reopen)
    - isValidTransition('abgeschlossen', 'in_bearbeitung') returns true (admin reopen)
    - COMMENT_REQUIRED includes 'rueckfrage' and 'abgelehnt'
    - All transitions from CONTEXT.md are covered: NEU->IN_BEARBEITUNG, IN_BEARBEITUNG->BESTAETIGT/RUECKFRAGE/ABGELEHNT, BESTAETIGT->BEZAHLT, BEZAHLT->ABGESCHLOSSEN, RUECKFRAGE->IN_BEARBEITUNG, ABGELEHNT->NEU, ABGESCHLOSSEN->IN_BEARBEITUNG
  </behavior>
  <action>
    1. Create `src/access/is-admin.ts`: Export `isAdmin` Access function that checks `req.user?.rolle === 'admin'`.
    2. Create `src/access/is-admin-or-mitarbeiter.ts`: Export `isAdminOrMitarbeiter` that checks admin or mitarbeiter.
    3. Create `src/access/is-own-anfrage.ts`: Export `isOwnAnfrage` that returns true for admin/mitarbeiter/viewer, query constraint for kunde, false for unauthenticated.
    4. Create `src/access/role-checks.ts`: Export helper `hasRole(user, roles: string[])` used by the above. Also export `isStaff` (admin/mitarbeiter/viewer) and field-level access functions: `staffCanRead` (admin/mitarbeiter/viewer can read), `staffCanWrite` (admin/mitarbeiter can write).
    5. Create `src/lib/status-transitions.ts`: Export `VALID_TRANSITIONS` Record, `COMMENT_REQUIRED` array, `isValidTransition(from, to)` function, `getNextStatuses(current)` function.
    6. Write tests FIRST (RED), then implement (GREEN):
       - `tests/unit/test-access-control.test.ts`: Test all access functions with mock user objects for each role + unauthenticated
       - `tests/unit/test-status-transitions.test.ts`: Test all valid/invalid transitions, comment requirements, getNextStatuses
  </action>
  <verify>
    <automated>npx jest tests/unit/test-access-control.test.ts tests/unit/test-status-transitions.test.ts --no-coverage -x</automated>
  </verify>
  <done>All access control functions and status transition logic exported and tested. Every role permutation verified.</done>
</task>

<task type="auto">
  <name>Task 2: Apply access control to ALL collections + saveToJWT + Status transition validation</name>
  <files>
    src/collections/system/users.ts,
    src/collections/system/media.ts,
    src/collections/business/anfragen.ts,
    src/collections/business/status-historie.ts,
    src/collections/business/preisregeln.ts,
    src/collections/business/rabattcodes.ts,
    src/collections/produkte/produkttypen.ts,
    src/collections/produkte/materialien.ts,
    src/collections/produkte/profile.ts,
    src/collections/produkte/fluegelanzahl.ts,
    src/collections/produkte/zusatzlichter.ts,
    src/collections/produkte/oeffnungsarten.ts,
    src/collections/produkte/fensterformen.ts,
    src/collections/ausstattung/farben.ts,
    src/collections/ausstattung/dichtungsfarben.ts,
    src/collections/ausstattung/verglasungen.ts,
    src/collections/ausstattung/schallschutz.ts,
    src/collections/ausstattung/sicherheitsglas.ts,
    src/collections/ausstattung/glasdekore.ts,
    src/collections/ausstattung/sprossen.ts,
    src/collections/ausstattung/extras.ts
  </files>
  <action>
    CRITICAL FIRST: Add `saveToJWT: true` to the `rolle` field in `src/collections/system/users.ts`. Without this, `req.user.rolle` is undefined in access functions and ALL access control fails.

    Also add `admin.hidden` for Kunde-role users in Users collection admin config (Kunden should not see admin panel collections).

    **Users Collection** access:
    - read: isAdminOrMitarbeiter (staff can see all users)
    - create: isAdmin (only admin creates users; register endpoint handled separately by Payload auth)
    - update: isAdmin (only admin edits users)
    - delete: isAdmin

    **Anfragen Collection** access (most complex):
    - create: `() => true` (public can submit Anfragen)
    - read: `isOwnAnfrage` (admin/mitarbeiter/viewer see all, kunde sees own, unauthenticated blocked)
    - update: `isAdminOrMitarbeiter` (only staff can update)
    - delete: `isAdmin` (only admin can delete)
    - Field-level on `interne_notizen`: read = staffCanRead, update = staffCanWrite
    - Field-level on `kontaktdaten`: update = isAdmin (per user decision: Mitarbeiter cannot edit Kontaktdaten)
    - Field-level on `gesamtpreis`: update = isAdmin (per user decision: Mitarbeiter cannot edit Preise)
    - Add `admin.listSearchableFields: ['anfrage_nummer', 'kontaktdaten.nachname', 'kontaktdaten.email']` for ADMIN-02 search

    **Anfragen beforeChange hook** -- EXTEND (not replace) existing hook:
    - Import `isValidTransition`, `COMMENT_REQUIRED` from `@/lib/status-transitions`
    - Before the existing StatusHistorie creation block, add transition validation:
      - If status changed and `!isValidTransition(originalDoc.status, data.status)`, throw APIError 400
      - If status changed and `COMMENT_REQUIRED.includes(data.status)` and no `data._status_kommentar`, throw APIError 400
      - Pass `data._status_kommentar` as `kommentar` to StatusHistorie creation
    - Add afterChange hook placeholder for future N8N webhook integration: comment block with `// TODO Phase 5: N8N webhook on status change`

    **StatusHistorie Collection** access (keep immutable, add role check):
    - create: `isAdminOrMitarbeiter` (only staff creates via hook, not public)
    - read: `() => true` (needed by Kunden-Dashboard to show timeline -- filtered by anfrage relationship)
    - update: `() => false` (immutable)
    - delete: `() => false` (immutable)

    **Produkt/Ausstattung Collections** (all 13 collections) access:
    - read: `() => true` (public reads for konfigurator -- these are product catalog data)
    - create: `isAdmin` (only admin manages product data)
    - update: `isAdmin`
    - delete: `isAdmin`

    **Preisregeln** access:
    - read: `isAdminOrMitarbeiter` (prices are internal, not public -- server-side price calc uses Local API which bypasses access)
    - create/update/delete: `isAdmin`

    **Rabattcodes** access:
    - read: `isAdminOrMitarbeiter` (codes are internal; validation endpoint uses Local API)
    - create/update/delete: `isAdmin`

    **Media** access:
    - read: `() => true` (public reads images)
    - create/update/delete: `isAdmin`
  </action>
  <verify>
    <automated>npx jest --no-coverage -x && cd /Users/janisjankewitz/Desktop/Arbeitsbereich/02_Programmierung/Christ\ Fensterhandel/playground/02-christ-fensterhandel-gsd && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - saveToJWT: true on rolle field
    - ALL 19 collections have explicit access control blocks (no open defaults)
    - Anfragen beforeChange hook validates status transitions and requires comment for RUECKFRAGE/ABGELEHNT
    - Admin search on Anfragen configured via listSearchableFields
    - interne_notizen has field-level access (only staff)
    - kontaktdaten and gesamtpreis field updates restricted to admin
    - Existing tests still pass, build succeeds
  </done>
</task>

<task type="auto">
  <name>Task 3: Add _status_kommentar virtual field + afterChange hook placeholder</name>
  <files>
    src/collections/business/anfragen.ts
  </files>
  <action>
    Add a `_status_kommentar` field to Anfragen collection for the status change comment workflow:
    - type: 'textarea', label: 'Status-Kommentar', admin: { condition: (data) => false } (hidden in default edit view, used by custom admin component in Plan 02)
    - Alternatively, use a virtual/transient field pattern: add the field but mark it as `admin: { readOnly: false, description: 'Wird bei Statusaenderung als Kommentar gespeichert' }`
    - The beforeChange hook (from Task 2) reads `data._status_kommentar`, passes it to StatusHistorie creation, then strips it from `data` before save (so it is not persisted on the Anfrage itself)
    - In the hook: after creating StatusHistorie with kommentar, do `delete data._status_kommentar` before returning data

    Also add the afterChange hook array to Anfragen config with a placeholder:
    ```typescript
    afterChange: [
      async ({ doc, previousDoc, req }) => {
        // Phase 5: N8N webhook integration
        // Will send webhook to N8N on status change for email notifications
        if (previousDoc && previousDoc.status !== doc.status) {
          // TODO: fetch(process.env.N8N_WEBHOOK_URL, { ... })
        }
      },
    ],
    ```
  </action>
  <verify>
    <automated>npx jest --no-coverage -x</automated>
  </verify>
  <done>
    - _status_kommentar field exists on Anfragen, used transiently during status updates
    - afterChange hook placeholder ready for Phase 5 N8N integration
    - All tests pass
  </done>
</task>

</tasks>

<verification>
1. `npx jest --no-coverage -x` -- all unit tests pass (access control + status transitions)
2. `npx next build` succeeds without errors
3. Access control applied to all 19 collections (grep for `access:` in all collection files)
4. saveToJWT: true exists on users rolle field
</verification>

<success_criteria>
- Every collection has explicit access control (no open defaults)
- Status transitions validated in beforeChange hook
- Kommentar required for RUECKFRAGE and ABGELEHNT
- Unit tests cover all role permutations and transition rules
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/04-dashboards-und-rollen/04-01-SUMMARY.md`
</output>
