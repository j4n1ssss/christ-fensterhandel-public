---
phase: 02-konfigurator-pipeline
plan: 05
type: execute
wave: 5
depends_on: ["02-04"]
files_modified:
  - src/components/konfigurator/steps/step-zusammenfassung.tsx
  - src/components/konfigurator/step-content.tsx
autonomous: false
requirements:
  - KONF-12

must_haves:
  truths:
    - "Step 10 zeigt alle gewaehlten Optionen als vollstaendige Zusammenfassung"
    - "Preisvorschau berechnet korrekten Preis aus Flaeche * Grundpreis + Aufpreise"
    - "Jede Konfigurationszeile zeigt Option-Name aufgeloest aus CMS-Daten"
    - "Gesamter 10-Step Flow ist von Anfang bis Ende durchlaufbar"
  artifacts:
    - path: "src/components/konfigurator/steps/step-zusammenfassung.tsx"
      provides: "Step 10: Full configuration summary with price preview"
  key_links:
    - from: "step-zusammenfassung.tsx"
      to: "src/lib/konfigurator/price-calculator.ts"
      via: "calculatePreviewPrice for price display"
      pattern: "calculatePreviewPrice"
    - from: "step-zusammenfassung.tsx"
      to: "src/lib/konfigurator/store.ts"
      via: "reads all selections for summary display"
      pattern: "useKonfiguratorStore"
---

<objective>
Step 10 (Zusammenfassung) mit vollstaendiger Konfigurations-Uebersicht und Preisvorschau. Anschliessend visueller Check des gesamten 10-Step Flows.

Purpose: Der letzte Step zeigt dem Kunden alles was er konfiguriert hat — mit Preis. Dies ist der Abschluss der Konfigurator-Pipeline. Die "In den Warenkorb"-Aktion kommt erst in Phase 3.
Output: Funktionaler Step 10 + visuell verifizierter Komplett-Flow.
</objective>

<execution_context>
@/Users/janisjankewitz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/janisjankewitz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-konfigurator-pipeline/02-CONTEXT.md
@.planning/phases/02-konfigurator-pipeline/02-RESEARCH.md
@.planning/phases/02-konfigurator-pipeline/02-01-SUMMARY.md
@.planning/phases/02-konfigurator-pipeline/02-04-SUMMARY.md

<interfaces>
From src/lib/konfigurator/store.ts:
- useKonfiguratorStore: all selections + cmsData

From src/lib/konfigurator/price-calculator.ts:
- calculatePreviewPrice(selections, cmsData): number

From src/payload-types.ts:
- All collection types for resolving UUIDs to display names
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Step 10 — Zusammenfassung mit Preisvorschau</name>
  <files>
    src/components/konfigurator/steps/step-zusammenfassung.tsx,
    src/components/konfigurator/step-content.tsx
  </files>
  <action>
    **Step 10 — Zusammenfassung (step-zusammenfassung.tsx):**
    'use client'. Reads ALL selections from store + cmsData. Resolves every UUID to the corresponding CMS object's display name.

    Layout: Clean summary card with sections:

    Section 1: "Ihre Konfiguration" — Table/list with:
    - Produkttyp: {resolved name}
    - Material: {resolved name}
    - Profil: {resolved name_einfach} ({name_technisch})
    - Fluegelanzahl: {resolved name}
    - Zusatzlichter: {resolved names, comma-separated} or "Keine"
    - Oeffnungsarten: For each wing: "Fluegel {n}: {oeffnungsart name}, Griff {griffSeite}" or "Fluegel {n}: {oeffnungsart name}" if no handle
    - Fensterform: {resolved name}
    - Masse: {breite} x {hoehe} mm ({flaeche} m2)
    - Aussenfarbe: {resolved name} (+ color swatch from farb_code)
    - Innenfarbe: {resolved name or "Gleich wie Aussen"} (+ color swatch)
    - Dichtungsfarbe: {resolved name} (+ color swatch)
    - Verglasung: {resolved name}
    - Schallschutz: {resolved name or "Keiner"}
    - Sicherheitsglas: {resolved name or "Keines"}
    - Glasdekor: {resolved name or "Keines"}
    - Sprossen: {resolved name or "Keine"}
    - Extras: {resolved names, comma-separated} or "Keine"

    Section 2: "Preisvorschau" — Call calculatePreviewPrice(selections, cmsData).
    Show: Grundpreis ({flaeche} m2 x {grundpreis} EUR/m2) = {basePrice} EUR
    Aufpreise itemized if > 0 (Verglasung +X, Farbe +X, etc.)
    Gesamt: {total} EUR (grosse, hervorgehobene Zahl)
    Hinweis: "Dies ist eine unverbindliche Preisvorschau. Der endgueltige Preis wird nach Pruefung Ihrer Anfrage bestaetigt."

    Section 3: Actions — "In den Warenkorb" Button (disabled, greyed out with Tooltip "Kommt in Phase 3") + "Konfiguration aendern" Button (goes back to Step 1).

    **Update step-content.tsx:**
    Add mapping: 10 -> StepZusammenfassung. Now all 10 steps are mapped.

    **Resolve Helper:** Create a local helper function `resolveName(id: string | null, collection: any[]): string` that finds the item by ID and returns its name (or name_einfach for profiles). Returns "—" if not found.
  </action>
  <verify>
    <automated>cd /Users/janisjankewitz/Desktop/Arbeitsbereich/02_Programmierung/Christ\ Fensterhandel/playground/02-christ-fensterhandel-gsd && npx jest tests/unit/test-price-calculator.test.ts --passWithNoTests 2>&1 | tail -10</automated>
  </verify>
  <done>Step 10 shows all 18+ configuration lines with resolved names from CMS data. Preisvorschau shows base price + aufpreise + total. "In den Warenkorb" button is visible but disabled (Phase 3). All 10 steps mapped in step-content.tsx.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visueller Check — Kompletter 10-Step Konfigurator Flow</name>
  <action>
    Kompletter 10-Step Fenster-Konfigurator wurde gebaut: Landing Page mit 3 Karten, 10 Steps durchlaufbar mit konditionaler Filterung, SVG-Vorschau mit Farbe/Masse/Sprossen, Zusammenfassung mit Preisvorschau. User muss den gesamten Flow visuell pruefen.
  </action>
  <verify>
    1. Starte den Dev-Server: `npm run dev`
    2. Oeffne http://localhost:3000 — 3 Konfigurator-Karten sichtbar?
    3. Klicke "Fenster" -> /konfigurator/fenster oeffnet sich
    4. Sidebar zeigt Step 1 aktiv, Rest ausgegraut
    5. Step 1: Produkttyp waehlen (z.B. Fenster) -> Step 2 oeffnet sich
    6. Step 2: Material mit Badges (Lieferzeit/Garantie) -> waehlen
    7. Step 3: Profile mit technischen Daten (Uw-Wert etc.) -> waehlen
    8. Step 4: Fluegelanzahl -> waehlen, optional Oberlicht/Unterlicht
    9. Step 5: Oeffnungsart pro Fluegel, Griff-Seite bei Dreh/Dreh-Kipp
    10. Step 6: Fensterform
    11. Step 7: Masse eingeben (z.B. 1200 x 1400 mm) — Min/Max Validierung pruefen
    12. Step 8: Farben waehlen (Aussen, Innen mit "Gleich wie Aussen", Dichtung)
    13. Step 9: Verglasung (Pflicht) + optionale Extras
    14. Step 10: Zusammenfassung — alle Optionen korrekt? Preis plausibel?
    15. SVG-Vorschau: Zeigt Fluegel, Oeffnungsart-Symbole, Farbe, Masse, Sprossen?
    16. Zurueck-Navigation: Gehe zurueck zu Step 2, aendere Material -> Profil reset?
    17. Mobile-Ansicht (Browser DevTools, 375px): Sidebar als Dropdown? Sticky Footer?
  </verify>
  <done>User approves the complete 10-step configurator flow including visual quality, conditional filtering, SVG preview, and mobile responsiveness.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — TypeScript compiles
2. `npx jest` — all tests pass
3. Complete 10-step flow navigable from start to finish
4. Summary shows all selections with resolved CMS names
5. Price preview calculates correctly from preisregeln
6. Human verification of visual quality and flow
</verification>

<success_criteria>
- Step 10 displays complete configuration summary with all option names resolved
- Price preview shows base price (area * rate) + itemized aufpreise + total
- "In den Warenkorb" button visible but disabled (Phase 3 scope)
- Complete flow (Steps 1-10) works without errors
- Human approves visual quality and functional correctness
</success_criteria>

<output>
After completion, create `.planning/phases/02-konfigurator-pipeline/02-05-SUMMARY.md`
</output>
