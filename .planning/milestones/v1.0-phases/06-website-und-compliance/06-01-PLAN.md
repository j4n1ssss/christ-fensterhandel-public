---
phase: 06-website-und-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/payload.config.ts
  - src/collections/website/pages.ts
  - src/payload-globals/navigation.ts
  - src/payload-globals/footer.ts
  - src/components/puck-blocks/hero-block.tsx
  - src/components/puck-blocks/text-block.tsx
  - src/components/puck-blocks/feature-grid-block.tsx
  - src/components/puck-blocks/cta-banner-block.tsx
  - src/components/puck-blocks/image-text-block.tsx
  - src/lib/puck-config.ts
  - src/app/(frontend)/[...slug]/page.tsx
  - src/app/(frontend)/page.tsx
  - src/app/(frontend)/layout.tsx
  - package.json
autonomous: true
requirements:
  - WEB-01
  - WEB-02
  - WEB-03
  - WEB-04

must_haves:
  truths:
    - "Puck Editor ist im Admin erreichbar und zeigt Component-Sidebar mit 5 Bloecken"
    - "Eine neue Seite kann per Drag-and-Drop mit Hero, TextBlock, FeatureGrid, CTA-Banner, Bild/Text Kombi gebaut werden"
    - "Gespeicherte Puck-Seite ist unter ihrem Slug im Frontend sichtbar (z.B. /ueber-uns)"
    - "Draft-Seiten sind im Frontend NICHT sichtbar, erst nach Publish"
    - "Navigation-Global und Footer-Global sind im Admin editierbar und werden im Frontend angezeigt"
    - "Homepage (/) zeigt die Puck-Seite mit slug 'home'"
    - "Live Preview im Puck Editor zeigt Breakpoint-Umschalter fuer Mobile (375px), Tablet (768px), Desktop (1280px)"
  artifacts:
    - path: "src/payload.config.ts"
      provides: "Puck plugin registered, Navigation + Footer globals, localization config stub"
      contains: "createPuckPlugin"
    - path: "src/components/puck-blocks/hero-block.tsx"
      provides: "Hero block component for Puck"
      exports: ["HeroBlock"]
    - path: "src/components/puck-blocks/text-block.tsx"
      provides: "TextBlock with Lexical rich text"
      exports: ["TextBlock"]
    - path: "src/components/puck-blocks/feature-grid-block.tsx"
      provides: "FeatureGrid with configurable columns"
      exports: ["FeatureGridBlock"]
    - path: "src/components/puck-blocks/cta-banner-block.tsx"
      provides: "CTA banner with button"
      exports: ["CTABannerBlock"]
    - path: "src/components/puck-blocks/image-text-block.tsx"
      provides: "Image + Text side by side"
      exports: ["ImageTextBlock"]
    - path: "src/lib/puck-config.ts"
      provides: "Extended Puck config with all 5 custom components and breakpoint viewport definitions"
      exports: ["puckConfig"]
    - path: "src/app/(frontend)/[...slug]/page.tsx"
      provides: "Catch-all route rendering published Puck pages by slug"
    - path: "src/payload-globals/navigation.ts"
      provides: "Editable header navigation links"
      exports: ["Navigation"]
    - path: "src/payload-globals/footer.ts"
      provides: "Editable footer with company data and legal links"
      exports: ["Footer"]
  key_links:
    - from: "src/payload.config.ts"
      to: "Puck Editor admin view"
      via: "createPuckPlugin in plugins array"
      pattern: "createPuckPlugin"
    - from: "src/app/(frontend)/[...slug]/page.tsx"
      to: "payload.find collection pages"
      via: "Payload Local API query with slug filter and _status published"
      pattern: "payload\\.find.*pages.*published"
    - from: "src/app/(frontend)/layout.tsx"
      to: "payload.findGlobal navigation + footer"
      via: "Payload Local API global queries"
      pattern: "findGlobal.*navigation|footer"
    - from: "src/lib/puck-config.ts"
      to: "Puck Editor preview"
      via: "Viewport definitions for breakpoint toggle in editor"
      pattern: "viewports|375|768|1280"
---

<objective>
Puck Page Builder mit 5 Block-Components, Catch-All Routing, Navigation/Footer Globals, Live Preview mit Breakpoints und Draft/Published Workflow.

Purpose: Geschaeftsinhaber kann Website-Seiten (Homepage, Ueber-uns, Impressum, Datenschutz) per Drag-and-Drop bauen und veroeffentlichen, ohne Developer-Hilfe. Live Preview mit Breakpoint-Toggle erlaubt Vorschau auf Mobile/Tablet/Desktop.
Output: Funktionierender Page Builder im Admin, 5 Block-Components, Frontend-Rendering via Catch-All Route, editierbare Navigation und Footer, Breakpoint-Toggle im Preview.
</objective>

<execution_context>
@/Users/janisjankewitz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/janisjankewitz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-website-und-compliance/06-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/payload.config.ts:
```typescript
// plugins: [] — empty array, ready for Puck plugin
// globals: [WebhookErrors] — add Navigation + Footer here
// collections: [...all 18 collections] — Pages will be auto-generated by plugin
```

From src/payload-globals/webhook-errors.ts (pattern to follow):
```typescript
import type { GlobalConfig } from 'payload'
export const WebhookErrors: GlobalConfig = {
  slug: 'webhook_errors',
  label: 'Webhook Fehler',
  admin: { group: 'System' },
  // ...
}
```

From src/app/(frontend)/layout.tsx:
```typescript
// Header with nav links (Logo, Dashboard/Login, CartBadge)
// <html lang="de"> — will need to stay "de" for now (i18n in Plan 02)
// Imports: NuqsAdapter, CartBadge, getCurrentUser, LogoutButton, User icon
```

From src/app/(frontend)/page.tsx:
```typescript
// Current homepage — Landing page with 3 Konfigurator cards
// Will be replaced: redirect to catch-all slug "home" Puck page OR keep as fallback
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Puck Plugin Setup + Pages Collection + Navigation/Footer Globals + 5 Block Components + Breakpoint Viewports</name>
  <files>
    package.json,
    src/payload.config.ts,
    src/payload-globals/navigation.ts,
    src/payload-globals/footer.ts,
    src/components/puck-blocks/hero-block.tsx,
    src/components/puck-blocks/text-block.tsx,
    src/components/puck-blocks/feature-grid-block.tsx,
    src/components/puck-blocks/cta-banner-block.tsx,
    src/components/puck-blocks/image-text-block.tsx,
    src/lib/puck-config.ts
  </files>
  <action>
    NOTE on file count: This task modifies 10 files. 5 of those are templated Puck block components following the same ComponentConfig pattern — each is ~30-50 lines of similar structure. This is intentional grouping, not scope creep.

    1. Install dependencies:
       ```bash
       npm install @delmaredigital/payload-puck @tailwindcss/typography js-cookie
       npm install -D @types/js-cookie
       ```
       NOTE: Check if peer dep is `@puckeditor/core` or `@measured/puck` — install whichever the plugin requires. The plugin's package.json lists the correct peer dep name.

    2. Create `src/payload-globals/navigation.ts` — Payload GlobalConfig:
       - slug: 'navigation', label: 'Navigation', admin.group: 'Website'
       - access: read: () => true, update: admin/mitarbeiter only (check req.user?.rolle)
       - fields: `links` array with sub-fields: label (text, required, localized: true), url (text, required), newTab (checkbox, default false)
       - Follow WebhookErrors pattern for structure

    3. Create `src/payload-globals/footer.ts` — Payload GlobalConfig:
       - slug: 'footer', label: 'Footer', admin.group: 'Website'
       - access: same as Navigation
       - fields: firmenname (text, default 'Christ Fensterhandel'), adresse (textarea, localized: true), telefon (text), email (email), legal_links array (label localized + url)

    4. Create 5 Puck block components in `src/components/puck-blocks/`:

       **hero-block.tsx**: ComponentConfig with fields — title (text), subtitle (textarea), ctaText (text), ctaLink (text), backgroundImage (text, URL from Media). Render: full-width section with centered text, CTA button linking to ctaLink. Use Tailwind classes consistent with project (bg-background, text-foreground, etc.). Default props with Christ Fensterhandel content.

       **text-block.tsx**: ComponentConfig with fields — heading (text, optional), content (textarea for rich text content), alignment (radio: left/center). Render: max-w-3xl mx-auto prose section. Use @tailwindcss/typography prose class for rich text styling.

       **feature-grid-block.tsx**: ComponentConfig with fields — heading (text), features array (title, description, icon text). Render: heading + responsive grid (1 col mobile, 2 col md, 3 col lg) of feature cards with icon placeholder, title, description. Cards use Card/shadow styling from project design.

       **cta-banner-block.tsx**: ComponentConfig with fields — heading (text), description (text), buttonText (text), buttonLink (text), variant (radio: primary/secondary for bg color). Render: full-width banner with bg-primary or bg-secondary, centered text, button.

       **image-text-block.tsx**: ComponentConfig with fields — heading (text), text (textarea), imageUrl (text, from Media), imagePosition (radio: left/right). Render: 2-col layout (stacked on mobile), image on chosen side, text on other side.

    5. Create `src/lib/puck-config.ts`:
       - Import all 5 block components
       - Use `extendConfig` from `@delmaredigital/payload-puck/config/editor` (or equivalent — check plugin exports)
       - Register components: HeroBlock, TextBlock, FeatureGridBlock, CTABannerBlock, ImageTextBlock
       - Define categories: layout (Hero, CTA-Banner), content (TextBlock, FeatureGrid, ImageText)
       - **WEB-03 — Live Preview Breakpoints:** Configure viewport definitions for the Puck editor preview. The Puck editor supports a `viewports` array in its config. Define 3 breakpoints:
         ```typescript
         viewports: [
           { width: 375, height: 'auto', label: 'Mobile', icon: 'Smartphone' },
           { width: 768, height: 'auto', label: 'Tablet', icon: 'Tablet' },
           { width: 1280, height: 'auto', label: 'Desktop', icon: 'Monitor' },
         ]
         ```
         NOTE: Check the actual Puck/plugin API for viewport configuration. The Puck editor (`@measured/puck` or `@puckeditor/core`) natively supports a `viewports` prop on the `<Puck>` component that renders a breakpoint toggle bar in the editor toolbar. If the `@delmaredigital/payload-puck` plugin exposes this via `createPuckPlugin({ viewports: [...] })` or via the extended config, use that. If the plugin does NOT expose viewports natively, configure it via the Puck config's `root.render` or by passing viewports through the plugin's editor component customization. Inspect the plugin source to determine the correct approach. The 3 breakpoints (375px, 768px, 1280px) per CONTEXT.md are non-negotiable.

    6. Update `src/payload.config.ts`:
       - Import createPuckPlugin from '@delmaredigital/payload-puck/plugin'
       - Import Navigation and Footer globals
       - Add localization config (needed for localized global fields): locales [{label:'Deutsch',code:'de'},{label:'English',code:'en'}], defaultLocale:'de', fallback:true
       - Add createPuckPlugin() to plugins array FIRST (before any other plugins). Config: pagesCollection: 'pages', autoGenerateCollection: true, editorStylesheet pointing to src/app/(frontend)/styles.css or globals.css
       - Add Navigation and Footer to globals array: [WebhookErrors, Navigation, Footer]
       - The plugin auto-generates a Pages collection with puckData field and versions: { drafts: true }

    IMPORTANT: Do NOT set localized: true on the puckData field itself (per research anti-pattern). Only simple fields like title get localized.

    NOTE on plugin API: If `createPuckPlugin` or `extendConfig` imports do not match the research exactly, inspect the installed package's exports (check node_modules/@delmaredigital/payload-puck/dist/) and adjust imports accordingly. The plugin may export differently than documented.
  </action>
  <verify>
    <automated>cd /Users/janisjankewitz/Desktop/Arbeitsbereich/02_Programmierung/Christ\ Fensterhandel/playground/02-christ-fensterhandel-gsd && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - @delmaredigital/payload-puck installed with correct peer deps
    - payload.config.ts has Puck plugin in plugins[], localization config, Navigation + Footer in globals[]
    - 5 block components exist with correct ComponentConfig structure
    - puck-config.ts extends base config with all 5 components and 3 viewport breakpoints (375/768/1280)
    - Navigation and Footer globals exist with correct fields and access control
    - Puck editor preview has breakpoint toggle for Mobile/Tablet/Desktop (WEB-03)
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Catch-All Frontend Route + Homepage Redirect + Navigation/Footer in Layout</name>
  <files>
    src/app/(frontend)/[...slug]/page.tsx,
    src/app/(frontend)/page.tsx,
    src/app/(frontend)/layout.tsx
  </files>
  <action>
    1. Create `src/app/(frontend)/[...slug]/page.tsx` — Server Component:
       - Accept params: { slug: string[] }
       - Join slug array to single string (e.g., ['ueber-uns'] -> 'ueber-uns')
       - Use Payload Local API: payload.find({ collection: 'pages', where: { slug: { equals: fullSlug }, _status: { equals: 'published' } }, limit: 1 })
       - If no published page found, call notFound()
       - Render page using PageRenderer from @delmaredigital/payload-puck/render with the page's puckData
       - Import the puck config (extended with custom blocks) and pass to PageRenderer
       - Generate metadata from page title field
       - IMPORTANT: Check the actual PageRenderer import path from the plugin. It may be '@delmaredigital/payload-puck/render' or a different export. Inspect node_modules if needed.

    2. Update `src/app/(frontend)/page.tsx`:
       - The current page is the Landing Page with 3 Konfigurator cards
       - Keep the existing landing page content BUT also try to load a Puck page with slug 'home'
       - If a published Puck page with slug 'home' exists, render it via PageRenderer
       - If no 'home' page exists, render the existing Landing Page content as fallback
       - This way the Geschaeftsinhaber can replace the homepage via Puck when ready

    3. Update `src/app/(frontend)/layout.tsx`:
       - Add Navigation rendering: fetch Navigation global via payload.findGlobal({ slug: 'navigation' }), render links in the header nav area (between logo and auth/cart section)
       - Add Footer component at bottom: fetch Footer global via payload.findGlobal({ slug: 'footer' }), render structured footer with firmenname, adresse, telefon, email, and legal_links
       - Keep existing header elements (Logo, Dashboard/Login links, CartBadge) intact
       - Navigation links render as Next.js Link components with optional target="_blank" for newTab
       - Footer: simple centered layout with company info and legal links row

    IMPORTANT: The catch-all [...slug] route must NOT conflict with existing routes (/konfigurator/*, /warenkorb, /kunden/*, /anfrage/*, /status-pruefen). These are in separate directories within (frontend)/ so Next.js route priority should handle this automatically — named routes take precedence over catch-all. Verify this works.
  </action>
  <verify>
    <automated>cd /Users/janisjankewitz/Desktop/Arbeitsbereich/02_Programmierung/Christ\ Fensterhandel/playground/02-christ-fensterhandel-gsd && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - Catch-all /[...slug] route renders published Puck pages by slug
    - Existing routes (/konfigurator, /warenkorb, /kunden, /anfrage, /status-pruefen) still work (named route priority)
    - Homepage (/) tries to load Puck 'home' page, falls back to existing Landing Page
    - Navigation links from Global appear in header
    - Footer with company data and legal links appears at bottom
    - Draft pages return 404 on frontend (only _status: 'published' shown)
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes — no TypeScript errors
2. Dev server starts without crashing: `npm run dev` (manual check)
3. Admin panel shows Puck editor with 5 blocks available (manual check)
4. Puck editor preview shows breakpoint toggle with Mobile (375px), Tablet (768px), Desktop (1280px) options (manual check — WEB-03)
5. Creating a page with slug "test-seite" and publishing it makes it visible at /test-seite (manual check)
6. Draft pages are NOT visible on frontend (manual check)
7. Navigation and Footer globals are editable in admin and rendered on frontend (manual check)
8. Existing routes (/konfigurator/fenster, /warenkorb, /kunden/login) still work (manual check)
</verification>

<success_criteria>
- Puck Page Builder is functional in Payload Admin with 5 custom block components
- Live Preview shows breakpoint toggle for Mobile (375px), Tablet (768px), Desktop (1280px) — WEB-03
- Published pages render on frontend via catch-all route
- Draft pages do not appear on frontend
- Navigation and Footer are editable via Payload Globals and displayed on all frontend pages
- Homepage can be replaced by a Puck page with slug 'home'
- All existing routes continue to function
</success_criteria>

<output>
After completion, create `.planning/phases/06-website-und-compliance/06-01-SUMMARY.md`
</output>
