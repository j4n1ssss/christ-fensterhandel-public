---
phase: 06-website-und-compliance
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/payload.config.ts
  - src/app/(frontend)/[locale]/[...slug]/page.tsx
  - src/app/(frontend)/[...slug]/page.tsx
  - src/app/(frontend)/page.tsx
  - src/app/(frontend)/layout.tsx
  - src/middleware.ts
  - src/components/i18n/language-toggle.tsx
  - src/components/cookie-banner/cookie-banner.tsx
  - src/app/(payload)/api/admin/anonymize-customer/route.ts
  - src/lib/i18n.ts
autonomous: true
requirements:
  - I18N-01
  - I18N-02
  - I18N-03
  - DSGVO-01
  - DSGVO-02
  - DSGVO-03

must_haves:
  truths:
    - "CMS-Felder (title in Pages, labels in Navigation/Footer) haben DE/EN Tabs im Admin"
    - "URL /de/ueber-uns zeigt die deutsche Version der Puck-Seite"
    - "URL /en/ueber-uns zeigt die englische Version (Fallback auf Deutsch wenn keine EN-Uebersetzung)"
    - "Sprachumschalter im Header wechselt zwischen /de/... und /en/..."
    - "Besuch ohne Locale-Prefix (z.B. /ueber-uns) wird zu /de/ueber-uns redirected"
    - "Cookie-Banner erscheint beim ersten Besuch mit 3 Kategorien"
    - "Nach Akzeptieren verschwindet der Banner und erscheint nicht mehr"
    - "Admin kann Kundendaten anonymisieren (Kontaktdaten werden zu GELOESCHT)"
    - "Konfigurator/Warenkorb/Kunden-Routes bleiben ohne Locale-Prefix funktional"
  artifacts:
    - path: "src/middleware.ts"
      provides: "i18n routing: redirects bare paths to /de/..., skips non-CMS routes"
      contains: "middleware"
    - path: "src/components/i18n/language-toggle.tsx"
      provides: "DE/EN toggle in header navigation"
      exports: ["LanguageToggle"]
    - path: "src/components/cookie-banner/cookie-banner.tsx"
      provides: "DSGVO cookie consent banner with 3 categories"
      exports: ["CookieBanner"]
    - path: "src/app/(frontend)/[locale]/[...slug]/page.tsx"
      provides: "Locale-prefixed catch-all route for Puck pages"
    - path: "src/app/(payload)/api/admin/anonymize-customer/route.ts"
      provides: "Admin-only endpoint to anonymize customer data"
      exports: ["POST"]
    - path: "src/lib/i18n.ts"
      provides: "Locale constants, helpers, type definitions"
      exports: ["LOCALES", "DEFAULT_LOCALE", "isValidLocale"]
  key_links:
    - from: "src/middleware.ts"
      to: "src/app/(frontend)/[locale]/[...slug]/page.tsx"
      via: "Redirect /ueber-uns to /de/ueber-uns, then locale param flows to page"
      pattern: "NextResponse\\.redirect"
    - from: "src/components/i18n/language-toggle.tsx"
      to: "URL pathname"
      via: "Replaces /de/ with /en/ or vice versa in current URL"
      pattern: "pathname\\.replace"
    - from: "src/app/(payload)/api/admin/anonymize-customer/route.ts"
      to: "payload.update users + anfragen"
      via: "Overwrites kontaktdaten with GELOESCHT"
      pattern: "payload\\.update.*GELOESCHT"
---

<objective>
Mehrsprachigkeit DE/EN fuer CMS-Seiten mit URL-Prefix-Routing und Sprachumschalter, Cookie-Banner mit 3 granularen Kategorien, und Admin-Datenloeschung (Anonymisierung).

Purpose: Deutsche und englische Inhalte fuer CMS-verwaltete Seiten, DSGVO-konforme Cookie-Einwilligung beim ersten Besuch, und Recht auf Vergessenwerden durch Daten-Anonymisierung.
Output: i18n Middleware + Locale-Routes, Sprachumschalter, Cookie-Banner, Anonymisierungs-Endpoint.
</objective>

<execution_context>
@/Users/janisjankewitz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/janisjankewitz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-website-und-compliance/06-RESEARCH.md
@.planning/phases/06-website-und-compliance/06-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 that this plan depends on -->

From src/payload.config.ts (after Plan 01):
```typescript
// localization already configured: { locales: [{code:'de'},{code:'en'}], defaultLocale:'de', fallback:true }
// Pages collection auto-generated by Puck plugin with _status field
// Navigation global with localized 'label' field in links array
// Footer global with localized 'adresse' and legal_links.label fields
```

From src/app/(frontend)/[...slug]/page.tsx (Plan 01 created):
```typescript
// Catch-all route fetching published Puck pages by slug
// Must be refactored to [locale]/[...slug] pattern
```

From src/app/(frontend)/layout.tsx (Plan 01 updated):
```typescript
// Header with Navigation global links + auth + CartBadge
// Footer with Footer global data
// <html lang="de"> — must become dynamic based on locale
```

From src/collections/business/anfragen.ts (existing):
```typescript
// kontaktdaten group: vorname, nachname, email, telefon, adresse, nachricht
// These fields get anonymized by the deletion endpoint
```

From src/collections/system/users.ts (existing):
```typescript
// Users collection with auth:true, rolle field
// Fields: email (auth), vorname, nachname, telefon, rolle
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: i18n Middleware + Locale Route + Language Toggle + Layout Updates</name>
  <files>
    src/lib/i18n.ts,
    src/middleware.ts,
    src/app/(frontend)/[locale]/[...slug]/page.tsx,
    src/app/(frontend)/[...slug]/page.tsx,
    src/app/(frontend)/page.tsx,
    src/app/(frontend)/layout.tsx,
    src/components/i18n/language-toggle.tsx
  </files>
  <action>
    1. Create `src/lib/i18n.ts`:
       - Export LOCALES = ['de', 'en'] as const
       - Export DEFAULT_LOCALE = 'de'
       - Export type Locale = 'de' | 'en'
       - Export isValidLocale(locale: string): locale is Locale
       - Export getAlternateLocale(current: Locale): Locale — returns the other locale

    2. Create `src/middleware.ts` — Next.js middleware:
       - Import LOCALES, DEFAULT_LOCALE from '@/lib/i18n'
       - Skip paths: /admin, /api, /konfigurator, /warenkorb, /kunden, /anfrage, /status-pruefen, /_next, paths containing '.' (static files)
       - If pathname already has valid locale prefix (/de/... or /en/...), pass through
       - Otherwise redirect to /${DEFAULT_LOCALE}${pathname} (e.g., /ueber-uns -> /de/ueber-uns)
       - Root / redirects to /de/
       - Export config.matcher excluding non-CMS routes

    3. Move the catch-all route:
       - Move/refactor `src/app/(frontend)/[...slug]/page.tsx` to `src/app/(frontend)/[locale]/[...slug]/page.tsx`
       - Add locale param extraction from params
       - Pass locale to payload.find() as `locale` parameter and set `fallbackLocale: 'de'`
       - Pass locale to Navigation/Footer global queries too
       - Delete or keep the old [...slug] as a redirect to /de/[slug]

    4. Update `src/app/(frontend)/page.tsx`:
       - Since middleware redirects / to /de/, this page may still be needed as a fallback
       - Option A: redirect to /de/ using next/navigation redirect()
       - Option B: Keep as-is and let middleware handle it
       - Choose whichever is simplest — if middleware handles / -> /de/ redirect already, the root page.tsx just needs to handle the /de/ case
       - Create `src/app/(frontend)/[locale]/page.tsx` that loads the Puck 'home' page with locale, falling back to existing landing page

    5. Create `src/components/i18n/language-toggle.tsx` — Client Component:
       - 'use client'
       - Read current pathname from usePathname() (next/navigation)
       - Detect current locale from pathname (first segment after /)
       - On click, replace locale segment in pathname (e.g., /de/ueber-uns -> /en/ueber-uns)
       - Navigate using router.push()
       - Render as compact toggle: "DE | EN" or "DE / EN" with active locale highlighted
       - Style with Tailwind, match project header aesthetic (text-sm, font-medium)

    6. Update `src/app/(frontend)/layout.tsx`:
       - Accept locale param (either from params or from pathname detection)
       - Set <html lang={locale}> dynamically
       - Add LanguageToggle component in header nav (between auth links and CartBadge)
       - Pass locale to Navigation/Footer global queries for localized labels
       - Note: Since this layout wraps both locale-prefixed routes AND non-locale routes (konfigurator, etc.), detect locale from the path or default to 'de'

    ROUTING STRUCTURE after this task:
    - / -> middleware redirects to /de/
    - /de/ -> [locale]/page.tsx -> Puck 'home' page
    - /de/ueber-uns -> [locale]/[...slug]/page.tsx -> Puck page by slug
    - /en/ueber-uns -> [locale]/[...slug]/page.tsx -> Puck page by slug (EN locale)
    - /konfigurator/fenster -> unchanged (middleware skips)
    - /warenkorb -> unchanged (middleware skips)
    - /kunden/* -> unchanged (middleware skips)

    CRITICAL: Verify that middleware does NOT interfere with existing routes. Test all skip patterns.
  </action>
  <verify>
    <automated>cd /Users/janisjankewitz/Desktop/Arbeitsbereich/02_Programmierung/Christ\ Fensterhandel/playground/02-christ-fensterhandel-gsd && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - Middleware redirects bare paths to /de/ prefix, skips non-CMS routes
    - Locale-prefixed catch-all route renders Puck pages with correct locale
    - Language toggle in header switches between /de/ and /en/ URLs
    - HTML lang attribute is dynamic based on locale
    - Navigation and Footer global queries use locale for localized fields
    - Existing routes (konfigurator, warenkorb, kunden, anfrage) unaffected
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Cookie-Banner + Datenloeschung (Anonymisierung) + DSGVO-01 Verification</name>
  <files>
    src/components/cookie-banner/cookie-banner.tsx,
    src/app/(frontend)/layout.tsx,
    src/app/(payload)/api/admin/anonymize-customer/route.ts
  </files>
  <action>
    1. Create `src/components/cookie-banner/cookie-banner.tsx` — Client Component:
       - 'use client'
       - Import js-cookie (Cookies from 'js-cookie')
       - Import Button, Card from Shadcn UI components
       - Cookie name: 'christ-cookie-consent'
       - State: visible (boolean), expanded (boolean for showing category details), consent object { necessary: true (always), statistics: false, marketing: false }
       - On mount (useEffect): check if consent cookie exists. If not, show banner.
       - UI structure:
         - Fixed bottom banner (z-50, p-4, bg-background border-t)
         - Title: "Cookie-Einstellungen" (or appropriate DSGVO text)
         - Brief text explaining cookie usage
         - 3 buttons: "Alle akzeptieren" (primary), "Nur notwendige" (outline), "Einstellungen" (ghost, toggles expanded)
         - When expanded: show 3 category toggles:
           - Notwendig (always on, disabled toggle) — Session, Security
           - Statistik (toggle) — vorbereitet fuer Google Analytics
           - Marketing (toggle) — vorbereitet fuer Tracking Pixels
         - "Auswahl speichern" button when expanded
       - saveConsent function: JSON.stringify categories, set cookie with path:'/', expires:365, sameSite:'lax'
       - After saving, setVisible(false)
       - Style with Tailwind matching project design (bg-background, text-foreground, border-border, etc.)

    2. Add CookieBanner to `src/app/(frontend)/layout.tsx`:
       - Import CookieBanner component
       - Add <CookieBanner /> before closing </body> tag (after </NuqsAdapter> wrapper, or inside it)
       - It renders only on first visit (component handles visibility internally)

    3. Create `src/app/(payload)/api/admin/anonymize-customer/route.ts` — POST endpoint:
       - Auth check: use payload.auth({ headers }) to verify admin role
       - If not admin (rolle !== 'admin'), return 403
       - Accept JSON body: { customerId: string }
       - Validate customerId is provided, return 400 if missing
       - Anonymize Users record:
         - email: `geloescht-${customerId}@anonymisiert.local`
         - vorname: 'GELOESCHT'
         - nachname: 'GELOESCHT'
         - telefon: '' (empty string)
         - Keep rolle, createdAt, updatedAt for audit
       - Find all Anfragen where kontaktdaten.email matches original email OR where a kunde relationship matches:
         - Anonymize kontaktdaten group: vorname/nachname/email/telefon/adresse/nachricht all replaced with 'GELOESCHT' or empty
         - Keep produkte array, preis_gesamt, status, snapshot — these are needed for business statistics
       - Return { success: true, anonymized_anfragen: count }
       - Wrap in try/catch, return 500 on error

    4. DSGVO-01 verification: The Datenschutz-Checkbox is already implemented in Phase 3 (ContactForm). No code changes needed — just confirm it still exists by checking src/app/(frontend)/anfrage/page.tsx or the ContactForm component for the datenschutz checkbox field.
  </action>
  <verify>
    <automated>cd /Users/janisjankewitz/Desktop/Arbeitsbereich/02_Programmierung/Christ\ Fensterhandel/playground/02-christ-fensterhandel-gsd && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - Cookie banner appears on first visit with 3 categories (Notwendig, Statistik, Marketing)
    - After accepting, banner does not reappear (cookie persisted for 1 year)
    - "Nur notwendige" button saves only necessary category
    - "Alle akzeptieren" saves all categories as true
    - Category toggles work correctly when expanded
    - Anonymization endpoint requires admin auth (403 for non-admin)
    - Anonymization replaces kontaktdaten with GELOESCHT, keeps Konfigurationsdaten
    - Datenschutz checkbox still present in Anfrage contact form (DSGVO-01)
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. Middleware correctly redirects / to /de/ (manual check)
3. /konfigurator/fenster still works without locale prefix (manual check)
4. Language toggle switches between /de/ and /en/ URLs (manual check)
5. Admin CMS fields show DE/EN tabs for localized fields (manual check)
6. Cookie banner appears on first visit, disappears after consent (manual check)
7. Admin anonymization endpoint works: POST /api/admin/anonymize-customer with customerId (manual check)
8. Datenschutz checkbox still present in contact form (manual check)
</verification>

<success_criteria>
- CMS pages accessible via /de/slug and /en/slug with locale-appropriate content
- Language toggle in header enables switching between DE and EN
- Fallback to German content when no English translation exists
- Cookie banner with 3 granular categories, persistent consent
- Admin can anonymize customer data (DSGVO right to deletion)
- Datenschutz checkbox verified as still functional
- All existing non-CMS routes unaffected by i18n middleware
</success_criteria>

<output>
After completion, create `.planning/phases/06-website-und-compliance/06-02-SUMMARY.md`
</output>
