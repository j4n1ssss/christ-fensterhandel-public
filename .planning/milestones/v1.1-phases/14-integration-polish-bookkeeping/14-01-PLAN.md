---
phase: 14-integration-polish-bookkeeping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/admin/profile-hub-status-cell.tsx
  - src/components/admin/profile-last-editor.tsx
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "Hub-Status Badge zeigt 'Vollständig' / 'Unvollständig' mit echten UTF-8 Umlauten"
    - "Hub-Status Tooltip zeigt 'Alle Pflicht-Hub-Felder befüllt' mit echtem ü"
    - "ProfileLastEditor fetch sendet Authentifizierungs-Cookie via credentials: include"
    - "ROADMAP.md Phase 14 Checkbox und Progress-Tabelle sind korrekt aktualisiert"
  artifacts:
    - path: "src/components/admin/profile-hub-status-cell.tsx"
      provides: "UTF-8 corrected badge text"
      contains: "Vollständig"
    - path: "src/components/admin/profile-last-editor.tsx"
      provides: "Authenticated fetch call"
      contains: 'credentials: "include"'
    - path: ".planning/ROADMAP.md"
      provides: "Updated phase 14 completion status"
      contains: "[x] **Phase 14:"
  key_links:
    - from: "src/components/admin/profile-hub-status-cell.tsx"
      to: "Payload Admin list view"
      via: "Cell component rendering"
      pattern: "Vollständig.*Unvollständig"
    - from: "src/components/admin/profile-last-editor.tsx"
      to: "/api/profile/{id}"
      via: "fetch with credentials"
      pattern: 'credentials.*include'
---

<objective>
Fix the two remaining integration gaps from the v1.1 milestone audit: UTF-8 umlauts in the Hub-Status badge text, and missing `credentials: "include"` in the ProfileLastEditor fetch call. Update ROADMAP.md and STATE.md to reflect Phase 14 completion.

Purpose: Close INT-COSMETIC-01 and INT-CREDENTIALS-01 from the milestone audit so v1.1 ships with zero known integration issues.
Output: Two fixed source files, updated ROADMAP and STATE bookkeeping.
</objective>

<execution_context>
@/Users/janisjankewitz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/janisjankewitz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/components/admin/profile-hub-status-cell.tsx (current state):
```typescript
// Line 31: ASCII transliteration — needs UTF-8 fix
{isComplete ? "Vollstaendig" : "Unvollstaendig"}

// Line 36: ASCII transliteration — needs UTF-8 fix
? "Alle Pflicht-Hub-Felder befuellt"
```

From src/components/admin/profile-last-editor.tsx (current state):
```typescript
// Line 61: Missing credentials option — needs fix
fetch(`/api/profile/${id}?depth=1`)
```

Established pattern (from profile-history-panel.tsx):
```typescript
// All other admin fetch calls use credentials: "include"
credentials: "include",
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix UTF-8 umlauts in Hub-Status badge + add credentials to LastEditor fetch</name>
  <files>src/components/admin/profile-hub-status-cell.tsx, src/components/admin/profile-last-editor.tsx</files>
  <read_first>
    - src/components/admin/profile-hub-status-cell.tsx (see current ASCII strings on lines 31, 36)
    - src/components/admin/profile-last-editor.tsx (see fetch call on line 61)
  </read_first>
  <action>
In `src/components/admin/profile-hub-status-cell.tsx`, make three string replacements:

1. Line 31: Change `"Vollstaendig"` to `"Vollständig"` (ae -> ä, replace the entire word)
2. Line 31: Change `"Unvollstaendig"` to `"Unvollständig"` (ae -> ä, replace the entire word)
3. Line 36: Change `"Alle Pflicht-Hub-Felder befuellt"` to `"Alle Pflicht-Hub-Felder befüllt"` (ue -> ü, replace the entire word)

No other changes to the file. The component logic, imports, styles, and structure remain identical.

In `src/components/admin/profile-last-editor.tsx`, change line 61 from:
```typescript
fetch(`/api/profile/${id}?depth=1`)
```
to:
```typescript
fetch(`/api/profile/${id}?depth=1`, { credentials: "include" })
```

This adds the second argument object with `credentials: "include"` to match the pattern used by all other admin panel fetch calls (anfrage-detail-view.tsx, status-timeline.tsx, profile-history-panel.tsx, status-workflow.tsx). No other changes to the file.
  </action>
  <verify>
    <automated>cd "/Users/janisjankewitz/Desktop/Arbeitsbereich/02_Programmierung/Christ Fensterhandel/playground/02-christ-fensterhandel-gsd" && grep -c "Vollständig" src/components/admin/profile-hub-status-cell.tsx && grep -c "Unvollständig" src/components/admin/profile-hub-status-cell.tsx && grep -c "befüllt" src/components/admin/profile-hub-status-cell.tsx && grep -c 'credentials.*include' src/components/admin/profile-last-editor.tsx && grep -c "Vollstaendig\|Unvollstaendig\|befuellt" src/components/admin/profile-hub-status-cell.tsx | grep "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - profile-hub-status-cell.tsx contains the string `Vollständig` (with ä) exactly once
    - profile-hub-status-cell.tsx contains the string `Unvollständig` (with ä) exactly once
    - profile-hub-status-cell.tsx contains the string `befüllt` (with ü) exactly once
    - profile-hub-status-cell.tsx does NOT contain `Vollstaendig`, `Unvollstaendig`, or `befuellt`
    - profile-last-editor.tsx contains `credentials: "include"` on the fetch line
    - profile-last-editor.tsx fetch call reads: `fetch(\`/api/profile/${id}?depth=1\`, { credentials: "include" })`
    - `npx jest --passWithNoTests` exits 0 (no regressions)
  </acceptance_criteria>
  <done>Badge shows real UTF-8 umlauts, fetch sends credentials cookie, no test regressions</done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md and STATE.md bookkeeping for Phase 14 completion</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/ROADMAP.md (current phase 14 status, progress table)
    - .planning/STATE.md (current position, progress counters)
  </read_first>
  <action>
In `.planning/ROADMAP.md`:

1. Line 34: Change `- [ ] **Phase 14: Integration Polish + Bookkeeping**` to `- [x] **Phase 14: Integration Polish + Bookkeeping** ... (completed 2026-03-23)`

2. Line 200 (plan checklist): Change `- [ ] 14-01-PLAN.md` to `- [x] 14-01-PLAN.md`

3. Line 197: Change `**Plans**: 1 plan` to `**Plans**: 1 plan` (already correct, verify only)

4. Progress table (line 224): Change Phase 14 row from:
```
| 14. Integration Polish + Bookkeeping | v1.1 | 0/1 | Not started | - |
```
to:
```
| 14. Integration Polish + Bookkeeping | v1.1 | 1/1 | Complete | 2026-03-23 |
```

In `.planning/STATE.md`:

1. Update `status:` from `completed` to `completed` (stays same)
2. Update `stopped_at:` to `Completed 14-01-PLAN.md (final gap closure phase complete)`
3. Update `last_updated:` to current date
4. Update `last_activity:` to `2026-03-23 -- Completed 14-01 Gap Closure (UTF-8 badge fix, credentials include, ROADMAP bookkeeping)`
5. Update `total_phases:` from `7` to `8`
6. Update `completed_phases:` from `7` to `8`
7. Update `total_plans:` from `13` to `14`
8. Update `completed_plans:` from `13` to `14`
9. Update `Current Position` section:
   - Phase line: `Phase: 14 of 14 (Gap Closure) -- COMPLETE`
   - Plan line: `Plan: 1 of 1 in current phase (complete)`
   - Status line: `All phases complete. v1.1 milestone + all gap closures finished.`
   - Last activity: `2026-03-23 -- Completed 14-01 Gap Closure (UTF-8 badge fix, credentials include, ROADMAP bookkeeping)`
10. Update `Current focus:` to `v1.1 COMPLETE + All Gap Closures -- All 8 phases (7-14), 14 plans done`
11. Update `Next step:` to `All gap closure items resolved. v1.1 milestone fully complete. No remaining plans.`
12. Update `Stopped at:` to `Completed 14-01-PLAN.md (final gap closure phase complete)`
  </action>
  <verify>
    <automated>cd "/Users/janisjankewitz/Desktop/Arbeitsbereich/02_Programmierung/Christ Fensterhandel/playground/02-christ-fensterhandel-gsd" && grep -c "\[x\] \*\*Phase 14" .planning/ROADMAP.md && grep -c "1/1.*Complete.*2026-03-23" .planning/ROADMAP.md && grep "total_phases" .planning/STATE.md && grep "completed_plans: 14" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md line 34 contains `[x] **Phase 14:`
    - ROADMAP.md plan list contains `[x] 14-01-PLAN.md`
    - ROADMAP.md progress table Phase 14 row shows `1/1 | Complete | 2026-03-23`
    - STATE.md `total_phases: 8`
    - STATE.md `completed_phases: 8`
    - STATE.md `total_plans: 14`
    - STATE.md `completed_plans: 14`
    - STATE.md contains `14-01-PLAN.md` in stopped_at
  </acceptance_criteria>
  <done>ROADMAP and STATE correctly reflect Phase 14 as complete with all counters updated</done>
</task>

</tasks>

<verification>
1. `grep "Vollständig" src/components/admin/profile-hub-status-cell.tsx` returns match with real ä
2. `grep "befüllt" src/components/admin/profile-hub-status-cell.tsx` returns match with real ü
3. `grep 'credentials.*include' src/components/admin/profile-last-editor.tsx` returns match
4. `grep -c "Vollstaendig\|Unvollstaendig\|befuellt" src/components/admin/profile-hub-status-cell.tsx` returns 0
5. `npx jest --passWithNoTests` exits 0
6. ROADMAP.md Phase 14 checkbox is `[x]` and progress table shows Complete
7. STATE.md progress counters show 8 phases, 14 plans all complete
</verification>

<success_criteria>
- INT-COSMETIC-01 closed: Badge displays "Vollständig"/"Unvollständig" with real umlauts
- INT-CREDENTIALS-01 closed: ProfileLastEditor fetch includes credentials
- ROADMAP and STATE bookkeeping reflect Phase 14 completion
- No test regressions (jest passes)
</success_criteria>

<output>
After completion, create `.planning/phases/14-integration-polish-bookkeeping/14-01-SUMMARY.md`
</output>
