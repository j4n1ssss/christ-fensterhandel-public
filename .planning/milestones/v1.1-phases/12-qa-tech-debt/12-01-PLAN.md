---
phase: 12-qa-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/collections/produkte/fensterformen.ts
  - src/collections/ausstattung/sicherheitsglas.ts
  - src/payload-types.ts
  - src/lib/konfigurator/types.ts
  - src/lib/konfigurator/filters.ts
  - src/components/konfigurator/steps/step-form.tsx
  - src/components/konfigurator/steps/step-verglasung-extras.tsx
  - tests/unit/test-filters-hub.test.ts
  - tests/unit/test-filters.test.ts
autonomous: true
requirements:
  - DEBT-03

must_haves:
  truths:
    - "npm run generate:types produces Fensterform and Sicherheitsglas interface names (no truncated Fensterforman/Sicherheitsgla)"
    - "npx tsc --noEmit passes with zero errors after type rename"
    - "npx jest passes all existing tests after import updates"
    - "Full payload-types.ts interface list reviewed -- no unexpected truncated names found across ALL collections"
  artifacts:
    - path: "src/collections/produkte/fensterformen.ts"
      provides: "typescript.interface override for Fensterform"
      contains: "interface: \"Fensterform\""
    - path: "src/collections/ausstattung/sicherheitsglas.ts"
      provides: "typescript.interface override for Sicherheitsglas"
      contains: "interface: \"Sicherheitsglas\""
    - path: "src/payload-types.ts"
      provides: "Clean generated type names"
      contains: "export interface Fensterform"
  key_links:
    - from: "src/lib/konfigurator/types.ts"
      to: "src/payload-types.ts"
      via: "type import"
      pattern: "import.*Fensterform[^a].*from.*payload-types"
    - from: "src/lib/konfigurator/filters.ts"
      to: "src/payload-types.ts"
      via: "type import"
      pattern: "import.*Fensterform[^a].*from.*payload-types"
---

<objective>
Fix TypeScript type generation typos by adding `typescript.interface` overrides to fensterformen and sicherheitsglas collections, then cascade the rename through all importing files. Additionally, audit ALL collections systematically to ensure no other truncated type names exist.

Purpose: Payload CMS derives TypeScript interface names from `labels.singular`, producing truncated German words ("Fensterforman" from "Fensterform", "Sicherheitsgla" from "Sicherheitsglas"). The `typescript.interface` property overrides this without changing admin UI labels. Per user decision, ALL collections must be systematically checked, not just the 2 known typos.

Output: Clean type names across the entire codebase, all tests passing, no compile errors, full audit of all collection interface names.
</objective>

<execution_context>
@/Users/janisjankewitz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/janisjankewitz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-qa-tech-debt/12-CONTEXT.md
@.planning/phases/12-qa-tech-debt/12-RESEARCH.md

<interfaces>
<!-- Current typo type names that will be renamed -->

From src/payload-types.ts (auto-generated, will be regenerated):
```typescript
// Line 79 - collection map entry
fensterformen: Fensterforman;  // WILL BECOME: Fensterform
// Line 84 - collection map entry
sicherheitsglas: Sicherheitsgla;  // WILL BECOME: Sicherheitsglas
// Line 363 - interface declaration
export interface Fensterforman { ... }  // WILL BECOME: Fensterform
// Line 494 - interface declaration
export interface Sicherheitsgla { ... }  // WILL BECOME: Sicherheitsglas
```

Files importing these types (exhaustive list):
- src/lib/konfigurator/types.ts: `Fensterforman` (line 8), `Sicherheitsgla` (line 13)
- src/lib/konfigurator/filters.ts: `Fensterforman` (line 6), `Sicherheitsgla` (line 11)
- src/components/konfigurator/steps/step-form.tsx: `Fensterforman` (line 7)
- src/components/konfigurator/steps/step-verglasung-extras.tsx: `Sicherheitsgla` (line 11)
- tests/unit/test-filters-hub.test.ts: `Fensterforman` (line 14), `Sicherheitsgla` (line 19)
- tests/unit/test-filters.test.ts: `Fensterforman` (line 10)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typescript.interface overrides, regenerate types, and audit ALL collections</name>
  <files>src/collections/produkte/fensterformen.ts, src/collections/ausstattung/sicherheitsglas.ts, src/payload-types.ts</files>
  <read_first>
    - src/collections/produkte/fensterformen.ts
    - src/collections/ausstattung/sicherheitsglas.ts
  </read_first>
  <action>
**Step 1: Add typescript.interface overrides**

Add `typescript` property with `interface` override to both collection configs. This controls the generated TypeScript interface name without changing admin UI labels.

**fensterformen.ts** -- Add after the `labels` property (around line 9):
```typescript
typescript: {
  interface: "Fensterform",
},
```

**sicherheitsglas.ts** -- Add after the `labels` property (around line 9):
```typescript
typescript: {
  interface: "Sicherheitsglas",
},
```

**Step 2: Regenerate types**

```bash
npm run generate:types
npm run generate:importmap
```

**Step 3: Audit ALL collections systematically (per user decision: ALLE Collections systematisch pruefen)**

After regeneration, inspect the full list of generated interface names in payload-types.ts:

```bash
grep "^export interface " src/payload-types.ts
```

Review EVERY interface name in the output. For each, verify:
- The name is a proper German/English word (not truncated)
- The name matches what would be expected from the collection slug/label

Known patterns to watch for: Payload derives interface names from `labels.singular` by appending "an" or similar suffixes that can cause truncation with German words ending in consonant clusters.

If ANY additional truncated/incorrect names are found beyond Fensterforman/Sicherheitsgla:
1. Add `typescript.interface` override to that collection config
2. Re-run `npm run generate:types`
3. Note the additional fix in the summary

**Step 4: Verify clean output**

Confirm payload-types.ts contains `export interface Fensterform` (not `Fensterforman`) and `export interface Sicherheitsglas` (not `Sicherheitsgla`). Also confirm no other unexpected interface names exist.
  </action>
  <verify>
    <automated>cd /Users/janisjankewitz/Desktop/Arbeitsbereich/02_Programmierung/Christ\ Fensterhandel/playground/02-christ-fensterhandel-gsd && grep -c 'interface: "Fensterform"' src/collections/produkte/fensterformen.ts && grep -c 'interface: "Sicherheitsglas"' src/collections/ausstattung/sicherheitsglas.ts && grep -c 'export interface Fensterform ' src/payload-types.ts && grep -c 'export interface Sicherheitsglas ' src/payload-types.ts && echo "--- Full interface audit ---" && grep "^export interface " src/payload-types.ts</automated>
  </verify>
  <acceptance_criteria>
    - fensterformen.ts contains `typescript: { interface: "Fensterform" }`
    - sicherheitsglas.ts contains `typescript: { interface: "Sicherheitsglas" }`
    - src/payload-types.ts contains `export interface Fensterform {` (not `Fensterforman`)
    - src/payload-types.ts contains `export interface Sicherheitsglas {` (not `Sicherheitsgla`)
    - src/payload-types.ts does NOT contain the string `Fensterforman` anywhere
    - src/payload-types.ts does NOT contain the string `Sicherheitsgla` as a standalone type name
    - Full payload-types.ts interface list reviewed, no unexpected truncated names found
  </acceptance_criteria>
  <done>Both collections have typescript.interface overrides, generate:types produces clean interface names, and ALL collection interface names have been audited for truncation issues</done>
</task>

<task type="auto">
  <name>Task 2: Update all type imports across codebase after rename</name>
  <files>src/lib/konfigurator/types.ts, src/lib/konfigurator/filters.ts, src/components/konfigurator/steps/step-form.tsx, src/components/konfigurator/steps/step-verglasung-extras.tsx, tests/unit/test-filters-hub.test.ts, tests/unit/test-filters.test.ts</files>
  <read_first>
    - src/lib/konfigurator/types.ts
    - src/lib/konfigurator/filters.ts
    - src/components/konfigurator/steps/step-form.tsx
    - src/components/konfigurator/steps/step-verglasung-extras.tsx
    - tests/unit/test-filters-hub.test.ts
    - tests/unit/test-filters.test.ts
    - src/payload-types.ts
  </read_first>
  <action>
Rename all occurrences of the old type names to new ones in every file that imports them. This is a straightforward find-and-replace:

**Rename `Fensterforman` to `Fensterform`** in these files:
1. **src/lib/konfigurator/types.ts**:
   - Line 8: `Fensterforman,` -> `Fensterform,`
   - Line 30: `fensterformen: Fensterforman[]` -> `fensterformen: Fensterform[]`
2. **src/lib/konfigurator/filters.ts**:
   - Line 6: `Fensterforman,` -> `Fensterform,`
   - Line 156: `getHubField<Fensterforman>(` -> `getHubField<Fensterform>(`
3. **src/components/konfigurator/steps/step-form.tsx**:
   - Line 7: `import type { Fensterforman, Media }` -> `import type { Fensterform, Media }`
   - Line 65: `) as Fensterforman[]` -> `) as Fensterform[]`
4. **tests/unit/test-filters-hub.test.ts**:
   - Line 14: `Fensterforman,` -> `Fensterform,`
   - Lines 109, 114: `Partial<Fensterforman>` -> `Partial<Fensterform>`, `): Fensterforman` -> `): Fensterform`
   - Lines 725, 872: `as Fensterforman[]` -> `as Fensterform[]`, `getHubField<Fensterforman>` -> `getHubField<Fensterform>`
5. **tests/unit/test-filters.test.ts**:
   - Line 10: `Fensterforman,` -> `Fensterform,`
   - Line 35: `Partial<Fensterforman>` -> `Partial<Fensterform>`, `): Fensterforman` -> `): Fensterform`

**Rename `Sicherheitsgla` to `Sicherheitsglas`** in these files:
1. **src/lib/konfigurator/types.ts**:
   - Line 13: `Sicherheitsgla,` -> `Sicherheitsglas,`
   - Line 35: `sicherheitsglas: Sicherheitsgla[]` -> `sicherheitsglas: Sicherheitsglas[]`
2. **src/lib/konfigurator/filters.ts**:
   - Line 11: `Sicherheitsgla,` -> `Sicherheitsglas,`
   - Line 252: `getHubField<Sicherheitsgla>(` -> `getHubField<Sicherheitsglas>(`
3. **src/components/konfigurator/steps/step-verglasung-extras.tsx**:
   - Line 11: `Sicherheitsgla,` -> `Sicherheitsglas,`
   - Line 115: `sicherheitsglas: Sicherheitsgla[]` -> `sicherheitsglas: Sicherheitsglas[]`
4. **tests/unit/test-filters-hub.test.ts**:
   - Line 19: `Sicherheitsgla,` -> `Sicherheitsglas,`
   - Lines 187, 192: `Partial<Sicherheitsgla>` -> `Partial<Sicherheitsglas>`, `): Sicherheitsgla` -> `): Sicherheitsglas`
   - Lines 569, 935: `getHubField<Sicherheitsgla>` -> `getHubField<Sicherheitsglas>`

After all renames, run:
```bash
npx tsc --noEmit
npx jest
```

Both must pass with zero errors.

**If Task 1 found additional truncated names:** Also update any imports for those types using the same find-and-replace approach.
  </action>
  <verify>
    <automated>cd /Users/janisjankewitz/Desktop/Arbeitsbereich/02_Programmierung/Christ\ Fensterhandel/playground/02-christ-fensterhandel-gsd && npx tsc --noEmit && npx jest --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "Fensterforman" src/ tests/` returns zero matches (excluding payload-types.ts which was already regenerated)
    - `grep -r "Sicherheitsgla[^s]" src/ tests/` returns zero matches
    - `npx tsc --noEmit` exits with code 0
    - `npx jest` exits with code 0 (all tests pass)
    - src/lib/konfigurator/types.ts imports `Fensterform` and `Sicherheitsglas` (not the old names)
    - src/lib/konfigurator/filters.ts imports `Fensterform` and `Sicherheitsglas` (not the old names)
  </acceptance_criteria>
  <done>All type imports updated, TypeScript compiles clean, all tests pass</done>
</task>

</tasks>

<verification>
1. `npm run generate:types` -- produces clean type names (no Fensterforman, no Sicherheitsgla)
2. `grep "^export interface " src/payload-types.ts` -- full interface list reviewed, no truncated names
3. `npx tsc --noEmit` -- zero compile errors
4. `npx jest` -- all existing tests pass
5. `grep -r "Fensterforman" src/ tests/` -- zero matches
6. `grep -r "Sicherheitsgla[^s]" src/ tests/` -- zero matches
</verification>

<success_criteria>
- DEBT-03 resolved: generate:types produces clean Fensterform and Sicherheitsglas interface names
- ALL collection interface names audited -- no unexpected truncated names in payload-types.ts
- Zero TypeScript compile errors across entire codebase
- All existing unit tests pass without modification to test logic (only type name imports changed)
- No runtime impact: only TypeScript types changed, no JavaScript behavior affected
</success_criteria>

<output>
After completion, create `.planning/phases/12-qa-tech-debt/12-01-SUMMARY.md`
</output>
