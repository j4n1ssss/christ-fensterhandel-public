---
phase: 17-status-config-centralization
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - src/components/admin/status-workflow.tsx
  - src/components/admin/status-timeline.tsx
  - src/components/admin/anfrage-detail-view.tsx
  - src/components/admin/dashboard-overview.tsx
  - src/components/kunden/status-timeline.tsx
  - src/components/kunden/gast-tracking-form.tsx
autonomous: true
requirements:
  - STAT-02

must_haves:
  truths:
    - "No file outside status-config.ts defines local STATUS_COLORS or STATUS_LABELS constants"
    - "Admin components render status badges with the same hex colors as before migration"
    - "Kunden components render status badges with the same Tailwind classes as before migration"
    - "All fallback patterns still work for unknown status keys"
  artifacts:
    - path: "src/components/admin/status-workflow.tsx"
      provides: "Status workflow using centralized config"
      contains: "import.*status-config"
    - path: "src/components/admin/status-timeline.tsx"
      provides: "Admin timeline using centralized config"
      contains: "import.*status-config"
    - path: "src/components/admin/anfrage-detail-view.tsx"
      provides: "Detail view using centralized config"
      contains: "import.*status-config"
    - path: "src/components/admin/dashboard-overview.tsx"
      provides: "Dashboard overview using centralized config"
      contains: "import.*status-config"
    - path: "src/components/kunden/status-timeline.tsx"
      provides: "Kunden timeline using STATUS_TAILWIND"
      contains: "import.*status-config"
    - path: "src/components/kunden/gast-tracking-form.tsx"
      provides: "Guest tracking form using STATUS_TAILWIND"
      contains: "import.*status-config"
  key_links:
    - from: "src/components/admin/status-workflow.tsx"
      to: "src/lib/status-config.ts"
      via: "import { STATUS_COLORS, STATUS_LABELS }"
      pattern: "import.*STATUS_COLORS.*STATUS_LABELS.*from.*status-config"
    - from: "src/components/admin/dashboard-overview.tsx"
      to: "src/lib/status-config.ts"
      via: "import (server component, no 'use client' conflict)"
      pattern: "import.*STATUS_COLORS.*STATUS_LABELS.*from.*status-config"
    - from: "src/components/kunden/status-timeline.tsx"
      to: "src/lib/status-config.ts"
      via: "import { STATUS_TAILWIND, STATUS_LABELS }"
      pattern: "import.*STATUS_TAILWIND.*from.*status-config"
    - from: "src/components/kunden/gast-tracking-form.tsx"
      to: "src/lib/status-config.ts"
      via: "import { STATUS_TAILWIND, STATUS_LABELS }"
      pattern: "import.*STATUS_TAILWIND.*from.*status-config"
---

<objective>
Migrate all 6 consumer files from local STATUS_COLORS/STATUS_LABELS definitions to imports from src/lib/status-config.ts. After this plan, no file outside status-config.ts contains local status color or label definitions.

Purpose: Complete the centralization by eliminating the 6-way duplication. This ensures Phase 18's new statuses only need to be added in one place.

Output: 6 modified component files, all importing from @/lib/status-config.
</objective>

<execution_context>
@/Users/janisjankewitz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/janisjankewitz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-status-config-centralization/17-CONTEXT.md
@.planning/phases/17-status-config-centralization/17-RESEARCH.md
@.planning/phases/17-status-config-centralization/17-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output: src/lib/status-config.ts exports -->
```typescript
// Types
export type StatusKey = 'neu' | 'in_bearbeitung' | 'bestaetigt' | 'bezahlt' | 'abgeschlossen' | 'rueckfrage' | 'abgelehnt'
export type StatusGroup = 'offen' | 'zahlung' | 'produktion' | 'lieferung' | 'abgeschlossen'
export type CustomerPhase = 'Anfrage' | 'Angebot' | 'Zahlung' | 'Produktion' | 'Lieferung'

// Hex colors (admin inline styles)
export const STATUS_COLORS: Record<StatusKey, string>

// German display labels
export const STATUS_LABELS: Record<StatusKey, string>

// Tailwind classes (kunden components)
export const STATUS_TAILWIND: Record<StatusKey, { bg: string; text: string; dot: string }>

// Customer-facing text, phase mapping, groups, email triggers
export const STATUS_CUSTOMER_TEXT: Record<StatusKey, string>
export const STATUS_CUSTOMER_PHASE: Record<StatusKey, CustomerPhase | null>
export const STATUS_GROUP: Record<StatusKey, StatusGroup>
export const EMAIL_TRIGGER_STATUSES: StatusKey[]

// Helpers with fallback for unknown keys
export function getStatusColor(status: string): string   // fallback: '#6b7280'
export function getStatusLabel(status: string): string   // fallback: raw status key
export function isCustomerFacing(status: string): boolean // fallback: false
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate 4 admin components to import from status-config.ts</name>
  <files>src/components/admin/status-workflow.tsx, src/components/admin/status-timeline.tsx, src/components/admin/anfrage-detail-view.tsx, src/components/admin/dashboard-overview.tsx</files>
  <read_first>
    - src/lib/status-config.ts (the new source of truth -- verify exports are available)
    - src/components/admin/status-workflow.tsx (full file -- find exact lines 6-24 to delete, find all STATUS_COLORS[x] and STATUS_LABELS[x] usage sites)
    - src/components/admin/status-timeline.tsx (full file -- find exact lines 5-23 to delete, find all usage sites)
    - src/components/admin/anfrage-detail-view.tsx (full file -- find exact lines 8-26 to delete, find all usage sites)
    - src/components/admin/dashboard-overview.tsx (full file -- find exact lines 6-24 to delete, find all usage sites including double-quote variants)
  </read_first>
  <action>
    For each of the 4 admin files, perform the same mechanical transformation:

    **status-workflow.tsx:**
    1. Add import: `import { STATUS_COLORS, STATUS_LABELS } from '@/lib/status-config'`
    2. Delete lines 6-14 (local `const STATUS_COLORS: Record<string, string> = { ... }`)
    3. Delete lines 16-24 (local `const STATUS_LABELS: Record<string, string> = { ... }`)
    4. All existing usage like `STATUS_COLORS[status] || '#6b7280'` and `STATUS_LABELS[status] || status` remain UNCHANGED -- the imported maps have the same shape and keys

    **status-timeline.tsx:**
    1. Add import: `import { STATUS_COLORS, STATUS_LABELS } from '@/lib/status-config'`
    2. Delete lines 5-13 (local STATUS_COLORS)
    3. Delete lines 15-23 (local STATUS_LABELS)
    4. All existing usage remains unchanged

    **anfrage-detail-view.tsx:**
    1. Add import: `import { STATUS_COLORS, STATUS_LABELS } from '@/lib/status-config'`
    2. Delete lines 8-16 (local STATUS_COLORS)
    3. Delete lines 18-26 (local STATUS_LABELS)
    4. All existing usage remains unchanged

    **dashboard-overview.tsx:**
    1. Add import: `import { STATUS_COLORS, STATUS_LABELS } from '@/lib/status-config'`
    2. Delete lines 6-14 (local STATUS_COLORS, note: uses double quotes unlike other files)
    3. Delete lines 16-24 (local STATUS_LABELS, note: uses double quotes)
    4. All existing usage remains unchanged
    5. IMPORTANT: This file is a React Server Component (no 'use client', uses `await getPayload()`). The import works because status-config.ts has NO 'use client' directive.

    Key rules:
    - Do NOT change any STATUS_COLORS[x] or STATUS_LABELS[x] access patterns in the component bodies
    - Do NOT change any fallback patterns (|| '#6b7280' etc.)
    - Do NOT add 'use client' to status-config.ts
    - Do NOT modify status-transitions.ts
  </action>
  <verify>
    <automated>cd /Users/janisjankewitz/Desktop/Arbeitsbereich/02_Programmierung/Christ\ Fensterhandel/playground/02-christ-fensterhandel-gsd && grep -r "const STATUS_COLORS\|const STATUS_LABELS" src/components/admin/ | wc -l | xargs test 0 -eq && echo "PASS: No local definitions in admin/" || echo "FAIL: Local definitions still exist"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "const STATUS_COLORS" src/components/admin/status-workflow.tsx` returns nothing
    - `grep "const STATUS_LABELS" src/components/admin/status-workflow.tsx` returns nothing
    - `grep "import.*STATUS_COLORS.*from.*status-config" src/components/admin/status-workflow.tsx` returns a match
    - `grep "const STATUS_COLORS" src/components/admin/status-timeline.tsx` returns nothing
    - `grep "const STATUS_LABELS" src/components/admin/status-timeline.tsx` returns nothing
    - `grep "import.*from.*status-config" src/components/admin/status-timeline.tsx` returns a match
    - `grep "const STATUS_COLORS" src/components/admin/anfrage-detail-view.tsx` returns nothing
    - `grep "const STATUS_LABELS" src/components/admin/anfrage-detail-view.tsx` returns nothing
    - `grep "import.*from.*status-config" src/components/admin/anfrage-detail-view.tsx` returns a match
    - `grep "const STATUS_COLORS" src/components/admin/dashboard-overview.tsx` returns nothing
    - `grep "const STATUS_LABELS" src/components/admin/dashboard-overview.tsx` returns nothing
    - `grep "import.*from.*status-config" src/components/admin/dashboard-overview.tsx` returns a match
    - dashboard-overview.tsx still does NOT contain 'use client' (remains server component)
    - `npx jest -x` exits with code 0 (no regressions in existing tests)
  </acceptance_criteria>
  <done>All 4 admin component files import STATUS_COLORS and STATUS_LABELS from @/lib/status-config. Zero local definitions remain in src/components/admin/.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate 2 kunden components to import STATUS_TAILWIND from status-config.ts</name>
  <files>src/components/kunden/status-timeline.tsx, src/components/kunden/gast-tracking-form.tsx</files>
  <read_first>
    - src/lib/status-config.ts (verify STATUS_TAILWIND export shape: { bg, text, dot })
    - src/components/kunden/status-timeline.tsx (full file -- find lines 4-23 to delete, find all STATUS_COLORS usage to rename to STATUS_TAILWIND)
    - src/components/kunden/gast-tracking-form.tsx (full file -- find lines 34-52 to delete, find all STATUS_COLORS usage -- this file uses FLAT strings like 'bg-blue-50 text-blue-700' which must be adapted to STATUS_TAILWIND structured objects)
  </read_first>
  <action>
    **kunden/status-timeline.tsx:**
    1. Replace `import type { StatusHistorie } from '@/payload-types'` line area -- add: `import { STATUS_TAILWIND, STATUS_LABELS } from '@/lib/status-config'`
    2. Delete lines 4-12 (local `const STATUS_COLORS: Record<string, { bg: string; text: string; dot: string }> = { ... }`)
    3. Delete lines 15-23 (local `const STATUS_LABELS: Record<string, string> = { ... }`)
    4. Rename ALL `STATUS_COLORS` references in the component body to `STATUS_TAILWIND`:
       - Line ~65: `const colors = STATUS_COLORS[entry.zu_status] || STATUS_COLORS.neu` becomes `const colors = STATUS_TAILWIND[entry.zu_status as keyof typeof STATUS_TAILWIND] || STATUS_TAILWIND.neu`
       - Line ~112: `const colors = STATUS_COLORS[status] || STATUS_COLORS.neu` becomes `const colors = STATUS_TAILWIND[status as keyof typeof STATUS_TAILWIND] || STATUS_TAILWIND.neu`
    5. The `.bg`, `.text`, `.dot` property access on the colors object remains unchanged because STATUS_TAILWIND has the same { bg, text, dot } shape

    **kunden/gast-tracking-form.tsx:**
    This file requires more careful adaptation because it uses FLAT Tailwind strings, not { bg, text, dot } objects.

    1. Add import: `import { STATUS_TAILWIND, STATUS_LABELS } from '@/lib/status-config'`
    2. Delete lines 34-42 (local `const STATUS_LABELS: Record<string, string> = { ... }`)
    3. Delete lines 44-52 (local `const STATUS_COLORS: Record<string, string> = { ... }` with flat strings like `'bg-blue-50 text-blue-700'`)
    4. Adapt the 2 usage sites in the component body:

       Usage site 1 (line ~195, status badge):
       BEFORE: `className={\`inline-flex self-start rounded-full px-3 py-1 text-sm font-medium ${STATUS_COLORS[result.status || ''] || 'bg-gray-50 text-gray-700'}\`}`
       AFTER: `className={\`inline-flex self-start rounded-full px-3 py-1 text-sm font-medium ${STATUS_TAILWIND[(result.status || '') as keyof typeof STATUS_TAILWIND]?.bg ?? 'bg-gray-50'} ${STATUS_TAILWIND[(result.status || '') as keyof typeof STATUS_TAILWIND]?.text ?? 'text-gray-700'}\`}`

       OR create a local helper at the top of the component for readability:
       ```typescript
       function getTailwindClasses(status: string): string {
         const tw = STATUS_TAILWIND[status as keyof typeof STATUS_TAILWIND]
         return tw ? `${tw.bg} ${tw.text}` : 'bg-gray-50 text-gray-700'
       }
       ```
       Then use: `className={\`inline-flex self-start rounded-full px-3 py-1 text-sm font-medium ${getTailwindClasses(result.status || '')}\`}`

       Usage site 2 (line ~221, timeline dot):
       BEFORE: `STATUS_COLORS[entry.zu_status]?.split(' ')[0]?.replace('bg-', 'bg-') || 'bg-gray-300'`
       AFTER: `STATUS_TAILWIND[entry.zu_status as keyof typeof STATUS_TAILWIND]?.dot ?? 'bg-gray-300'`
       (This is actually CLEANER -- the old code hacked the dot color by splitting the flat string. STATUS_TAILWIND.dot provides it directly.)

    Final verification step: Run the grep command to confirm zero local definitions remain across ALL of src/:
    `grep -r "const STATUS_COLORS\|const STATUS_LABELS" src/ | grep -v status-config.ts`
    This must return empty (zero matches).
  </action>
  <verify>
    <automated>cd /Users/janisjankewitz/Desktop/Arbeitsbereich/02_Programmierung/Christ\ Fensterhandel/playground/02-christ-fensterhandel-gsd && DUPES=$(grep -r "const STATUS_COLORS\|const STATUS_LABELS" src/ | grep -v status-config.ts | wc -l | tr -d ' ') && test "$DUPES" -eq 0 && echo "PASS: Zero local definitions outside status-config.ts" || echo "FAIL: $DUPES local definitions still exist"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "const STATUS_COLORS" src/components/kunden/status-timeline.tsx` returns nothing
    - `grep "const STATUS_LABELS" src/components/kunden/status-timeline.tsx` returns nothing
    - `grep "import.*STATUS_TAILWIND.*from.*status-config" src/components/kunden/status-timeline.tsx` returns a match
    - `grep "const STATUS_COLORS" src/components/kunden/gast-tracking-form.tsx` returns nothing
    - `grep "const STATUS_LABELS" src/components/kunden/gast-tracking-form.tsx` returns nothing
    - `grep "import.*STATUS_TAILWIND.*from.*status-config" src/components/kunden/gast-tracking-form.tsx` returns a match
    - `grep -r "const STATUS_COLORS\|const STATUS_LABELS" src/ | grep -v status-config.ts` returns empty (ZERO matches)
    - `npx jest -x` exits with code 0 (no regressions)
    - gast-tracking-form.tsx timeline dot uses STATUS_TAILWIND[...].dot (not string splitting hack)
  </acceptance_criteria>
  <done>All 6 consumer files import from @/lib/status-config. grep -r "const STATUS_COLORS|const STATUS_LABELS" src/ returns only status-config.ts. The kunden/gast-tracking-form.tsx is adapted from flat strings to structured STATUS_TAILWIND objects. All existing tests pass.</done>
</task>

</tasks>

<verification>
Phase-level verification after both tasks complete:

1. **Centralization complete:** `grep -r "const STATUS_COLORS\|const STATUS_LABELS" src/ | grep -v status-config.ts` returns 0 lines
2. **All imports wired:** `grep -rl "import.*from.*status-config" src/` returns exactly 6 files (the 4 admin + 2 kunden components)
3. **Pure data module:** `grep 'use client' src/lib/status-config.ts` returns nothing
4. **Type safety:** `npx tsc --noEmit` exits 0 (TypeScript compilation passes)
5. **Test suite green:** `npx jest -x` exits 0
6. **All exports present:** `grep -c 'export' src/lib/status-config.ts` returns at least 13
</verification>

<success_criteria>
All 6 component files import status colors and labels exclusively from src/lib/status-config.ts. Zero local STATUS_COLORS or STATUS_LABELS definitions exist outside that file. The existing visual behavior is identical (same hex values for admin, same Tailwind classes for kunden, same fallback patterns). TypeScript compiles without errors. All existing tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/17-status-config-centralization/17-02-SUMMARY.md`
</output>
