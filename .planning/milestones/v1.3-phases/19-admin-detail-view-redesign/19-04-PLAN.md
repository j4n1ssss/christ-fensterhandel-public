---
phase: 19-admin-detail-view-redesign
plan: 04
type: execute
wave: 4
depends_on: ["19-01", "19-02", "19-03"]
files_modified:
  - src/app/(payload)/custom.scss
  - src/components/admin/attention-bar.tsx
  - src/components/admin/splitbutton.tsx
  - src/components/admin/product-card.tsx
  - src/components/admin/tab-panel.tsx
  - src/components/admin/anfrage-detail-view.tsx
autonomous: true
requirements: [ADMN-01]
gap_closure: true

must_haves:
  truths:
    - "admin-custom.css (custom.scss) contains CSS classes for ALL structural styles -- typography, spacing, layout, form elements, links, error messages"
    - "The 5 React component files use className references instead of inline style objects for all non-dynamic properties"
    - "Only truly dynamic values (status colors computed from JS variables like statusColor, primaryColor) remain as inline styles"
    - "The wrapper element in anfrage-detail-view.tsx uses className='admin-content' matching the dashboard page pattern"
    - "product-card.tsx has 0 inline style= attributes"
  artifacts:
    - path: "src/app/(payload)/custom.scss"
      provides: "Complete CSS class system covering typography, form elements, buttons, links, layout utilities, error messages"
      contains: ".admin-content"
    - path: "src/components/admin/attention-bar.tsx"
      provides: "AttentionBar with CSS classes instead of inline styles"
    - path: "src/components/admin/splitbutton.tsx"
      provides: "Splitbutton with CSS classes for form elements, buttons, error messages"
    - path: "src/components/admin/product-card.tsx"
      provides: "ProductCard with CSS classes for typography and price display"
    - path: "src/components/admin/tab-panel.tsx"
      provides: "TabPanel with CSS classes for contact, notes, details tabs"
    - path: "src/components/admin/anfrage-detail-view.tsx"
      provides: "Main view using admin-content wrapper class and CSS classes for child layout"
  key_links:
    - from: "src/components/admin/attention-bar.tsx"
      to: "src/app/(payload)/custom.scss"
      via: "className references to .attention-bar__* classes"
      pattern: "className.*attention-bar"
    - from: "src/components/admin/splitbutton.tsx"
      to: "src/app/(payload)/custom.scss"
      via: "className references to .comment-panel__*, .form-*, .btn-* classes"
      pattern: "className.*comment-panel|className.*form-|className.*btn-"
    - from: "src/components/admin/anfrage-detail-view.tsx"
      to: "src/app/(payload)/custom.scss"
      via: "className='admin-content' wrapper"
      pattern: "className.*admin-content"
---

<objective>
Extract all static inline styles from the 5 Phase 19 React components into CSS classes in custom.scss. The wrapper in anfrage-detail-view.tsx must use the `.admin-content` class (matching the dashboard page pattern) instead of inline padding/maxWidth. Only truly dynamic values (status colors computed from JS variables) remain as inline styles.

Purpose: Close the Phase 19 Success Criterion 1 gap -- "admin-custom.css ist via Payload Config eingebunden und strukturelle Admin-Styles (Layout, Grid, Spacing) sind als CSS-Klassen definiert statt als Inline-Styles". User feedback: "Es sind viele Inline Stylings" and "Styling ist katastrophal."
Output: Updated custom.scss with ~60 new CSS classes, 5 refactored React components with minimal inline styles.
</objective>

<execution_context>
@/Users/janisjankewitz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/janisjankewitz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-admin-detail-view-redesign/19-CONTEXT.md
@.planning/phases/19-admin-detail-view-redesign/19-UI-SPEC.md
@.planning/phases/19-admin-detail-view-redesign/19-01-SUMMARY.md
@.planning/phases/19-admin-detail-view-redesign/19-02-SUMMARY.md
@.planning/phases/19-admin-detail-view-redesign/19-03-SUMMARY.md

<interfaces>
<!-- Existing CSS classes from Plan 01 (custom.scss) that are ALREADY used correctly -->
<!-- These must be PRESERVED -- this plan ADDS new classes, does NOT replace existing ones -->

Already in custom.scss and used via className:
- .admin-content (wrapper from dashboard page -- REUSE for detail view)
- .attention-bar, .attention-bar--warn, .attention-bar--urgent, .attention-bar--critical
- .status-badge
- .urgency-badge
- .splitbutton, .splitbutton__primary, .splitbutton__chevron
- .splitbutton__dropdown, .splitbutton__dropdown-item, .splitbutton__dropdown-dot
- .comment-panel
- .detail-layout
- .product-card, .product-card__header, .product-card__measures, .product-card__spec-grid
- .product-card__extras, .product-card__price
- .quantity-badge
- .tab-panel__bar, .tab-panel__trigger, .tab-panel__trigger--active, .tab-panel__content
- .terminal-info, .terminal-info__reopen
- .spec-row, .spec-row__label, .spec-row__value

<!-- anfrage-detail-view.tsx structure after Plan 03 rewrite (202 lines, composed version) -->
<!-- Key inline styles to replace in this file: -->
<!-- Line 99: loading div style={{ padding, color }} -->
<!-- Line 107: not-found div style={{ padding, color }} -->
<!-- Line 117: wrapper div style={{ padding: "24px", maxWidth: "1400px" }} -->
<!-- Line 119: standard-link div style={{ textAlign, marginBottom }} -->
<!-- Lines 122-126: standard-link anchor style={{ fontSize, color, textDecoration }} -->
<!-- Line 146: action-zone div style={{ margin: "16px 0" }} -->
<!-- Lines 160-168: empty-state div style={{ padding, color, fontSize, textAlign, background, border, borderRadius }} -->
<!-- Line 174: products-column div style={{ display, flexDirection, gap }} -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend custom.scss with missing CSS classes for all static inline styles</name>
  <files>src/app/(payload)/custom.scss</files>
  <read_first>
    - src/app/(payload)/custom.scss (current state -- 252 lines, ALL existing classes must be preserved)
    - src/components/admin/attention-bar.tsx (identify all inline style= objects)
    - src/components/admin/splitbutton.tsx (identify all inline style= objects)
    - src/components/admin/product-card.tsx (identify all inline style= objects)
    - src/components/admin/tab-panel.tsx (identify all inline style= objects)
    - src/components/admin/anfrage-detail-view.tsx (identify all inline style= objects -- NOTE: this is the 202-line composed version from Plan 03, NOT the old 540-line file)
    - .planning/phases/19-admin-detail-view-redesign/19-UI-SPEC.md (typography scale, spacing scale, color values)
  </read_first>
  <action>
    Append the following new CSS classes to custom.scss AFTER the existing `.spec-row__value` block (line 251). Do NOT modify or delete ANY existing CSS above line 251.

    Add these class families in the exact order shown:

    ```scss
    /* === Gap Closure: Typography + Layout Utility Classes === */

    /* Display text (20px, weight 600) -- Anfrage-Nummer, Gesamtpreis */
    .text-display {
      font-size: 20px;
      font-weight: 600;
      color: var(--theme-elevation-1000);
    }

    /* Body text (13px, weight 400) -- default content text */
    .text-body {
      font-size: 13px;
      color: var(--theme-elevation-800);
    }

    /* Body muted (13px, weight 400, lighter color) -- zusammenfassung, secondary */
    .text-body-muted {
      font-size: 13px;
      color: var(--theme-elevation-600);
    }

    /* Caption text (12px) -- labels, dates, small text */
    .text-caption {
      font-size: 12px;
      color: var(--theme-elevation-500);
    }

    /* Heading (14px, weight 600) -- card titles, product identity */
    .text-heading {
      font-size: 14px;
      font-weight: 600;
      color: var(--theme-elevation-900);
    }

    /* Label (12px, weight 600) -- form labels, section headers */
    .text-label {
      font-size: 12px;
      font-weight: 600;
      color: var(--theme-elevation-600);
      display: block;
      margin-bottom: 4px;
    }

    /* Section heading (13px, weight 600) -- group titles */
    .text-section-heading {
      font-size: 13px;
      font-weight: 600;
      margin-bottom: 8px;
      color: var(--theme-elevation-800);
    }

    /* Fluegel append text (13px, weight 400) */
    .text-fluegel-append {
      font-size: 13px;
      font-weight: 400;
    }

    /* === Attention Bar Sub-Elements === */

    .attention-bar__left {
      display: inline-flex;
      align-items: center;
      gap: 12px;
    }

    .attention-bar__right {
      display: inline-flex;
      align-items: center;
      gap: 16px;
    }

    /* === Link Styles === */

    /* Standard admin link -- blue, no underline, hover underline */
    .link-email {
      color: #3b82f6;
      text-decoration: none;
    }
    .link-email:hover {
      text-decoration: underline;
    }

    /* Edit link -- blue, underline, inline-block */
    .link-edit {
      display: inline-block;
      margin-top: 8px;
      font-size: 12px;
      color: #3b82f6;
      text-decoration: underline;
      cursor: pointer;
      background: none;
      border: none;
      padding: 0;
    }

    /* Standard view link (top right corner) */
    .link-standard-view {
      font-size: 12px;
      color: var(--theme-elevation-500);
      text-decoration: underline;
    }

    /* === Form Elements === */

    .form-textarea {
      width: 100%;
      padding: 8px 10px;
      border: 1px solid var(--theme-elevation-300);
      border-radius: 4px;
      font-size: 13px;
      background: var(--theme-input-bg, var(--theme-bg));
      color: var(--theme-elevation-800);
      resize: vertical;
      line-height: 1.5;
      box-sizing: border-box;
    }

    .form-input {
      width: 100%;
      padding: 8px 10px;
      border: 1px solid var(--theme-elevation-300);
      border-radius: 4px;
      font-size: 13px;
      background: var(--theme-input-bg, var(--theme-bg));
      color: var(--theme-elevation-800);
      box-sizing: border-box;
    }

    .form-select {
      width: 100%;
      padding: 8px 10px;
      border: 1px solid var(--theme-elevation-300);
      border-radius: 4px;
      font-size: 13px;
      background: var(--theme-input-bg, var(--theme-bg));
      color: var(--theme-elevation-800);
      box-sizing: border-box;
    }

    .form-row-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 8px;
      margin-bottom: 8px;
    }

    .form-group {
      margin-bottom: 8px;
    }

    /* === Button Styles === */

    .btn-ghost {
      padding: 6px 12px;
      border: 1px solid var(--theme-elevation-300);
      border-radius: 4px;
      background: transparent;
      cursor: pointer;
      font-size: 12px;
      color: var(--theme-elevation-800);
    }
    .btn-ghost:hover {
      background: var(--theme-elevation-50);
    }

    /* Primary button -- background color set via inline style (dynamic) */
    .btn-primary {
      padding: 6px 12px;
      border: none;
      border-radius: 4px;
      color: #fff;
      cursor: pointer;
      font-size: 12px;
      font-weight: 600;
    }
    .btn-primary:disabled {
      opacity: 0.5;
      cursor: not-allowed;
    }

    /* Destructive button */
    .btn-destructive {
      padding: 6px 12px;
      border: none;
      border-radius: 4px;
      background: #ef4444;
      color: #fff;
      cursor: pointer;
      font-size: 12px;
      font-weight: 600;
    }
    .btn-destructive:disabled {
      opacity: 0.5;
      cursor: not-allowed;
    }

    /* Save button (neutral/secondary) */
    .btn-save {
      margin-top: 8px;
      padding: 6px 12px;
      border: 1px solid var(--theme-elevation-300);
      border-radius: 4px;
      background: var(--theme-elevation-100);
      cursor: pointer;
      font-size: 12px;
      font-weight: 600;
    }
    .btn-save:disabled {
      opacity: 0.6;
      cursor: not-allowed;
    }

    /* Anonymize button (destructive outline) */
    .btn-anonymize {
      margin-top: 16px;
      padding: 6px 12px;
      border: 1px solid #ef4444;
      border-radius: 4px;
      background: transparent;
      color: #ef4444;
      cursor: pointer;
      font-size: 12px;
      font-weight: 600;
    }
    .btn-anonymize:disabled {
      opacity: 0.6;
      cursor: not-allowed;
    }

    /* Button row (flex end) */
    .btn-row {
      display: flex;
      gap: 8px;
      justify-content: flex-end;
      margin-top: 10px;
    }

    /* === Comment Panel Sub-Elements === */

    .comment-panel__heading {
      font-size: 13px;
      font-weight: 600;
      margin-bottom: 8px;
    }

    .comment-panel__heading--destructive {
      font-size: 13px;
      font-weight: 600;
      margin-bottom: 8px;
      color: #ef4444;
    }

    /* === Error Message === */

    .error-message {
      margin-top: 8px;
      padding: 8px 12px;
      border-radius: 4px;
      background: #ef444418;
      color: #ef4444;
      font-size: 12px;
    }

    /* === Tab Panel: Kontakt Tab === */

    .kontakt-tab {
      font-size: 13px;
      line-height: 1.8;
    }

    .kontakt-tab__name {
      font-weight: 600;
      color: var(--theme-elevation-900);
    }

    .kontakt-tab__phone {
      color: var(--theme-elevation-700);
    }

    .kontakt-tab__address {
      color: var(--theme-elevation-600);
      margin-top: 4px;
    }

    .kontakt-tab__message {
      margin-top: 8px;
      padding-top: 8px;
      border-top: 1px solid var(--theme-elevation-200);
      font-style: italic;
      color: var(--theme-elevation-600);
    }

    /* === Tab Panel: Details Tab === */

    .details-tab {
      display: flex;
      flex-direction: column;
      gap: 16px;
    }

    .details-tab__section {
      display: flex;
      flex-direction: column;
      gap: 8px;
    }

    .details-tab__heading--destructive {
      font-size: 13px;
      font-weight: 600;
      margin-bottom: 8px;
      color: #ef4444;
    }

    /* Detail field (readonly key-value) */
    .detail-field__label {
      font-size: 12px;
      color: var(--theme-elevation-500);
    }

    .detail-field__value {
      font-size: 13px;
      color: var(--theme-elevation-800);
    }

    /* === Product Card: Price Sub-Elements === */

    .price-single {
      font-size: 13px;
      font-weight: 600;
      color: var(--theme-elevation-900);
    }

    .price-unit {
      font-size: 13px;
      color: var(--theme-elevation-600);
    }

    .price-multiplier {
      font-size: 13px;
      color: var(--theme-elevation-500);
    }

    .price-total {
      font-size: 13px;
      font-weight: 600;
      color: var(--theme-elevation-900);
    }

    /* === Layout Utilities (Detail View) === */

    .detail-view__standard-link {
      text-align: right;
      margin-bottom: 8px;
    }

    .detail-view__action-zone {
      margin: 16px 0;
    }

    .detail-view__products-column {
      display: flex;
      flex-direction: column;
      gap: 12px;
    }

    .detail-view__empty-state {
      padding: 24px;
      color: var(--theme-elevation-500);
      font-size: 13px;
      text-align: center;
      background: var(--theme-elevation-50);
      border: 1px solid var(--theme-elevation-200);
      border-radius: 6px;
    }

    .detail-view__loading {
      padding: 24px;
      color: var(--theme-elevation-500);
    }
    ```

    IMPORTANT: Preserve ALL existing CSS from line 1 to 251 exactly as-is. Only APPEND new classes after line 251.
  </action>
  <verify>
    <automated>grep -c "\.text-display\|\.text-body\|\.form-textarea\|\.btn-ghost\|\.btn-primary\|\.btn-destructive\|\.error-message\|\.kontakt-tab\|\.details-tab\|\.price-single\|\.detail-view__empty-state\|\.attention-bar__left\|\.link-email\|\.btn-anonymize\|\.detail-field__label" src/app/\(payload\)/custom.scss</automated>
  </verify>
  <acceptance_criteria>
    - custom.scss contains `.text-display` class with `font-size: 20px` and `font-weight: 600`
    - custom.scss contains `.text-body` class with `font-size: 13px`
    - custom.scss contains `.text-body-muted` class with `color: var(--theme-elevation-600)`
    - custom.scss contains `.text-caption` class with `font-size: 12px` and `color: var(--theme-elevation-500)`
    - custom.scss contains `.text-heading` class with `font-size: 14px` and `font-weight: 600`
    - custom.scss contains `.text-label` class with `display: block` and `margin-bottom: 4px`
    - custom.scss contains `.text-section-heading` class with `font-size: 13px`
    - custom.scss contains `.attention-bar__left` class with `display: inline-flex`
    - custom.scss contains `.attention-bar__right` class with `gap: 16px`
    - custom.scss contains `.link-email` class with `color: #3b82f6`
    - custom.scss contains `.link-edit` class with `text-decoration: underline`
    - custom.scss contains `.link-standard-view` class
    - custom.scss contains `.form-textarea` class with `resize: vertical`
    - custom.scss contains `.form-input` class
    - custom.scss contains `.form-select` class
    - custom.scss contains `.form-row-grid` class with `grid-template-columns: 1fr 1fr`
    - custom.scss contains `.btn-ghost` class with `background: transparent`
    - custom.scss contains `.btn-primary` class with `color: #fff`
    - custom.scss contains `.btn-destructive` class with `background: #ef4444`
    - custom.scss contains `.btn-save` class with `background: var(--theme-elevation-100)`
    - custom.scss contains `.btn-anonymize` class with `border: 1px solid #ef4444`
    - custom.scss contains `.btn-row` class with `justify-content: flex-end`
    - custom.scss contains `.comment-panel__heading` class
    - custom.scss contains `.comment-panel__heading--destructive` class with `color: #ef4444`
    - custom.scss contains `.error-message` class with `background: #ef444418`
    - custom.scss contains `.kontakt-tab` class with `line-height: 1.8`
    - custom.scss contains `.kontakt-tab__name` with `font-weight: 600`
    - custom.scss contains `.kontakt-tab__phone` with `color: var(--theme-elevation-700)`
    - custom.scss contains `.kontakt-tab__address` with `margin-top: 4px`
    - custom.scss contains `.kontakt-tab__message` with `font-style: italic`
    - custom.scss contains `.details-tab` class with `flex-direction: column`
    - custom.scss contains `.detail-field__label` with `font-size: 12px`
    - custom.scss contains `.detail-field__value` with `font-size: 13px`
    - custom.scss contains `.price-single` with `font-weight: 600`
    - custom.scss contains `.price-unit` with `color: var(--theme-elevation-600)`
    - custom.scss contains `.price-multiplier` with `color: var(--theme-elevation-500)`
    - custom.scss contains `.price-total` with `font-weight: 600`
    - custom.scss contains `.detail-view__standard-link` with `text-align: right`
    - custom.scss contains `.detail-view__action-zone` with `margin: 16px 0`
    - custom.scss contains `.detail-view__products-column` with `flex-direction: column`
    - custom.scss contains `.detail-view__empty-state` with `text-align: center`
    - custom.scss contains `.detail-view__loading` with `padding: 24px`
    - custom.scss still contains ALL original classes: `.admin-content`, `.attention-bar`, `.splitbutton`, `.product-card`, `.quantity-badge`, `.tab-panel__bar`, `.terminal-info`, `.spec-row`
  </acceptance_criteria>
  <done>custom.scss extended with ~60 new CSS classes covering typography (text-display, text-body, text-heading, text-label, text-caption), form elements (form-textarea, form-input, form-select), buttons (btn-ghost, btn-primary, btn-destructive, btn-save, btn-anonymize), links (link-email, link-edit), attention-bar sub-elements, comment-panel sub-elements, kontakt-tab, details-tab, price, detail-view layout utilities, and error-message. All original Plan 01 classes preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor all 5 React components to use CSS classes instead of inline styles</name>
  <files>
    src/components/admin/attention-bar.tsx
    src/components/admin/splitbutton.tsx
    src/components/admin/product-card.tsx
    src/components/admin/tab-panel.tsx
    src/components/admin/anfrage-detail-view.tsx
  </files>
  <read_first>
    - src/app/(payload)/custom.scss (the updated file from Task 1 -- verify new classes exist before using them)
    - src/components/admin/attention-bar.tsx (current file with inline styles)
    - src/components/admin/splitbutton.tsx (current file with inline styles)
    - src/components/admin/product-card.tsx (current file with inline styles)
    - src/components/admin/tab-panel.tsx (current file with inline styles)
    - src/components/admin/anfrage-detail-view.tsx (Plan 03 output -- 202-line composed version with AttentionBar + Splitbutton + ProductCard + TabPanel. Contains 8 inline style blocks to replace with CSS classes.)
  </read_first>
  <action>
    Refactor all 5 files to replace inline `style={{...}}` objects with `className="..."` references to the CSS classes from custom.scss. The rule is: **ONLY keep inline styles for values that are COMPUTED from JavaScript variables at runtime** (status colors, dynamic opacity for disabled states where the background color is dynamic).

    ### File 1: attention-bar.tsx

    Replace the entire render return. Key changes:
    - Line 44-48: `<div style={{display: "inline-flex", alignItems: "center", gap: "12px"}}>` becomes `<div className="attention-bar__left">`
    - Line 51-57: `<span style={{fontSize: "20px", fontWeight: 600, color: "var(--theme-elevation-1000)"}}>` becomes `<span className="text-display">`
    - Status badge at line 62-67: KEEP inline style for `background: ${statusColor}18`, `color: statusColor`, `border: 1px solid ${statusColor}40` -- these are DYNAMIC
    - Urgency badge at line 75-79: KEEP inline style for `color: urgencyColor`, `background: ${urgencyColor}1f` -- these are DYNAMIC
    - Line 90-93: `<span style={{fontSize: "12px", color: "var(--theme-elevation-500)"}}>` becomes `<span className="text-caption">`
    - Line 102-106: `<div style={{display: "inline-flex", alignItems: "center", gap: "16px"}}>` becomes `<div className="attention-bar__right">`
    - Line 109-112: `<span style={{fontSize: "13px", color: "var(--theme-elevation-600)"}}>` becomes `<span className="text-body-muted">`
    - Line 118-121: `<span style={{fontSize: "20px", fontWeight: 600, color: "var(--theme-elevation-1000)"}}>` becomes `<span className="text-display">`

    ### File 2: splitbutton.tsx

    Key changes:
    - Line 175 splitbutton__primary: KEEP `style={{ background: primaryColor }}` -- DYNAMIC
    - Line 185 splitbutton__chevron: KEEP `style={{ background: primaryColor, filter: "brightness(0.9)" }}` -- DYNAMIC
    - Line 203 splitbutton__dropdown-dot: KEEP `style={{ background: STATUS_COLORS[...] }}` -- DYNAMIC
    - Line 218-226: Error div becomes `<div className="error-message">`  -- remove ALL inline styles
    - Line 236 border on comment-panel: KEEP `style={{ border: 1px solid ${STATUS_COLORS[...]}40 }}` -- DYNAMIC
    - Line 239: `<div style={{fontSize: 13, fontWeight: 600, marginBottom: 8}}>` becomes `<div className="comment-panel__heading">`
    - Line 245-258: textarea becomes `<textarea className="form-textarea" ...>` -- remove ALL style props
    - Line 261-266: button row becomes `<div className="btn-row">` -- remove ALL style props
    - Line 268-279: Cancel button becomes `<button className="btn-ghost" ...>` -- remove ALL style props
    - Line 283-298: Submit button becomes `<button className="btn-primary" style={{ background: STATUS_COLORS[showCommentFor as StatusKey] || "#6b7280", opacity: saving || !comment.trim() ? 0.5 : 1 }} ...>` -- KEEP only dynamic background and opacity
    - Line 309 stornierung panel border: KEEP `style={{ border: "1px solid #ef444440" }}` -- this is a hardcoded destructive color but applied conditionally, OK to keep
    - Line 312-317: heading becomes `<div className="comment-panel__heading--destructive">`
    - Line 321: form group becomes `<div className="form-group">`
    - Line 323-329: label becomes `<label className="text-label">`
    - Line 333-346: textarea becomes `<textarea className="form-textarea" ...>`
    - Line 350-356: grid row becomes `<div className="form-row-grid">`
    - Line 360-366, 390-396: labels become `<label className="text-label">`
    - Line 370-380: input becomes `<input className="form-input" ...>`
    - Line 399-409: select becomes `<select className="form-select" ...>`
    - Line 419-424: button row becomes `<div className="btn-row">` (remove marginTop from style, it's not in btn-row? -- actually check: the stornierung panel has NO marginTop on btn-row. Add `margin-top: 0` explicitly if needed or just use btn-row without extra margin, since btn-row already has `margin-top: 10px`. For the stornierung panel, this is fine.)
    - Line 426-440: Cancel becomes `<button className="btn-ghost" ...>`
    - Line 445-462: Submit becomes `<button className="btn-destructive" ...>` with `style={{ opacity: saving || !stornierungGrund.trim() ? 0.5 : 1 }}` -- KEEP only dynamic opacity

    ### File 3: product-card.tsx

    Key changes:
    - Line 47-51: identity span becomes `<span className="text-heading">`
    - Line 62: fluegel append becomes `<span className="text-fluegel-append">`
    - Line 93-98: single price becomes `<span className="price-single">`
    - Line 105-108: unit price becomes `<span className="price-unit">`
    - Line 113-116: multiplier becomes `<span className="price-multiplier">`
    - Line 127-130: total becomes `<span className="price-total">`

    ### File 4: tab-panel.tsx

    Key changes in KontaktTab:
    - Line 144: container becomes `<div className="kontakt-tab">`
    - Line 145: name becomes `<div className="kontakt-tab__name">`
    - Line 153: email link becomes `<a className="link-email" href={...}>` -- REMOVE the onMouseEnter/onMouseLeave inline hover handlers (CSS :hover handles this now)
    - Line 168: telefon becomes `<div className="kontakt-tab__phone">`
    - Line 174-178: address becomes `<div className="kontakt-tab__address">`
    - Line 188-195: nachricht becomes `<div className="kontakt-tab__message">`
    - Line 201-216: anonymize button becomes `<button className="btn-anonymize" ...>` with `style={{ opacity: anonymizing ? 0.6 : 1 }}` -- KEEP only dynamic opacity

    Key changes in NotizenTab:
    - Line 247-256: textarea becomes `<textarea className="form-textarea" ...>`
    - Line 258-268: save button becomes `<button className="btn-save" ...>` with `style={{ opacity: savingNotizen ? 0.6 : 1 }}` -- KEEP only dynamic opacity

    Key changes in DetailsTab:
    - Line 300: container becomes `<div className="details-tab">`
    - Line 305-309: section heading becomes `<div className="text-section-heading">`
    - Line 315-319: section column becomes `<div className="details-tab__section">`
    - Line 344-351: edit link becomes `<a className="link-edit" ...>` -- KEEP onClick handler
    - Line 366-370: stornierung heading becomes `<div className="details-tab__heading--destructive">`
    - Line 376-380: section column becomes `<div className="details-tab__section">`
    - Line 397-404: edit link becomes `<a className="link-edit" ...>`

    Key changes in DetailField:
    - Line 429-432: label becomes `<div className="detail-field__label">`
    - Line 437-440: value becomes `<div className="detail-field__value">`

    ### File 5: anfrage-detail-view.tsx (Plan 03 output -- 202-line composed version)

    NOTE: This file was completely rewritten by Plan 03 from 540 lines to 202 lines. It now composes AttentionBar, Splitbutton, ProductCard, and TabPanel sub-components. The inline styles to replace are:

    - Line 99: `<div style={{ padding: "24px", color: "var(--theme-elevation-500)" }}>` (loading state) becomes `<div className="detail-view__loading">`
    - Line 107: `<div style={{ padding: "24px", color: "var(--theme-elevation-500)" }}>` (not-found state) becomes `<div className="detail-view__loading">`
    - Line 117: `<div style={{ padding: "24px", maxWidth: "1400px" }}>` (main wrapper) becomes `<div className="admin-content">` -- matching the dashboard-overview.tsx pattern
    - Line 119: `<div style={{ textAlign: "right", marginBottom: "8px" }}>` (standard link container) becomes `<div className="detail-view__standard-link">`
    - Lines 122-126: `<a ... style={{ fontSize: "12px", color: "var(--theme-elevation-500)", textDecoration: "underline" }}>` (standard view link) becomes `<a className="link-standard-view" ...>` -- KEEP href and onClick handler
    - Line 146: `<div style={{ margin: "16px 0" }}>` (action zone wrapping Splitbutton) becomes `<div className="detail-view__action-zone">`
    - Lines 159-168: `<div style={{ padding: "24px", color: "...", fontSize: "13px", textAlign: "center", background: "...", border: "...", borderRadius: "6px" }}>` (empty state) becomes `<div className="detail-view__empty-state">`
    - Line 173-174: `<div style={{ display: "flex", flexDirection: "column", gap: "12px" }}>` (products column) becomes `<div className="detail-view__products-column">`

    ### What STAYS as inline style (exhaustive list of allowed remaining inline styles):

    1. **attention-bar.tsx**: status-badge `background/color/border` (statusColor), urgency-badge `color/background` (urgencyColor)
    2. **splitbutton.tsx**: splitbutton__primary `background` (primaryColor), splitbutton__chevron `background/filter` (primaryColor), dropdown-dot `background` (STATUS_COLORS[target]), comment-panel `border` (statusColor), btn-primary `background/opacity` (statusColor + disabled state), stornierung panel `border` (#ef444440), btn-destructive `opacity` (disabled state)
    3. **tab-panel.tsx**: tab-panel__trigger--active `borderBottomColor` (statusColor), btn-anonymize `opacity` (anonymizing), btn-save `opacity` (savingNotizen)
    4. **product-card.tsx**: NONE -- all styles are static
    5. **anfrage-detail-view.tsx**: NONE -- all styles are static
  </action>
  <verify>
    <automated>grep -c 'style={{' src/components/admin/attention-bar.tsx src/components/admin/product-card.tsx src/components/admin/anfrage-detail-view.tsx && grep -c 'style={{' src/components/admin/splitbutton.tsx src/components/admin/tab-panel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - attention-bar.tsx: grep `style={{` count is exactly 2 (status-badge dynamic colors, urgency-badge dynamic colors)
    - attention-bar.tsx: contains `className="attention-bar__left"`
    - attention-bar.tsx: contains `className="attention-bar__right"`
    - attention-bar.tsx: contains `className="text-display"` (appears 2 times)
    - attention-bar.tsx: contains `className="text-body-muted"`
    - attention-bar.tsx: contains `className="text-caption"`
    - attention-bar.tsx: does NOT contain `fontSize: "20px"` as inline style
    - attention-bar.tsx: does NOT contain `display: "inline-flex"` as inline style

    - splitbutton.tsx: grep `style={{` count is at most 7 (primaryColor on primary btn, primaryColor+filter on chevron, dropdown-dot color, comment-panel border, btn-primary bg+opacity, stornierung-panel border, btn-destructive opacity)
    - splitbutton.tsx: contains `className="error-message"`
    - splitbutton.tsx: contains `className="comment-panel__heading"`
    - splitbutton.tsx: contains `className="comment-panel__heading--destructive"`
    - splitbutton.tsx: contains `className="form-textarea"`
    - splitbutton.tsx: contains `className="form-input"`
    - splitbutton.tsx: contains `className="form-select"`
    - splitbutton.tsx: contains `className="form-row-grid"`
    - splitbutton.tsx: contains `className="btn-row"` (appears 2 times)
    - splitbutton.tsx: contains `className="btn-ghost"` (appears 2 times)
    - splitbutton.tsx: contains `className="btn-primary"`
    - splitbutton.tsx: contains `className="btn-destructive"`
    - splitbutton.tsx: contains `className="text-label"` (appears 3 times)
    - splitbutton.tsx: contains `className="form-group"`
    - splitbutton.tsx: does NOT contain `padding: "8px 10px"` as inline style
    - splitbutton.tsx: does NOT contain `borderRadius: 4` as inline style (except where dynamic)

    - product-card.tsx: grep `style={{` count is exactly 0
    - product-card.tsx: contains `className="text-heading"`
    - product-card.tsx: contains `className="text-fluegel-append"`
    - product-card.tsx: contains `className="price-single"`
    - product-card.tsx: contains `className="price-unit"`
    - product-card.tsx: contains `className="price-multiplier"`
    - product-card.tsx: contains `className="price-total"`
    - product-card.tsx: does NOT contain `fontSize:` anywhere

    - tab-panel.tsx: grep `style={{` count is at most 3 (tab trigger borderBottomColor, btn-anonymize opacity, btn-save opacity)
    - tab-panel.tsx: contains `className="kontakt-tab"`
    - tab-panel.tsx: contains `className="kontakt-tab__name"`
    - tab-panel.tsx: contains `className="kontakt-tab__phone"`
    - tab-panel.tsx: contains `className="kontakt-tab__address"`
    - tab-panel.tsx: contains `className="kontakt-tab__message"`
    - tab-panel.tsx: contains `className="link-email"`
    - tab-panel.tsx: contains `className="btn-anonymize"`
    - tab-panel.tsx: contains `className="form-textarea"`
    - tab-panel.tsx: contains `className="btn-save"`
    - tab-panel.tsx: contains `className="details-tab"`
    - tab-panel.tsx: contains `className="details-tab__section"` (appears 2 times)
    - tab-panel.tsx: contains `className="text-section-heading"`
    - tab-panel.tsx: contains `className="link-edit"` (appears 2 times)
    - tab-panel.tsx: contains `className="detail-field__label"`
    - tab-panel.tsx: contains `className="detail-field__value"`
    - tab-panel.tsx: does NOT contain `onMouseEnter` (hover handled by CSS)
    - tab-panel.tsx: does NOT contain `onMouseLeave` (hover handled by CSS)
    - tab-panel.tsx: does NOT contain `fontSize: "13px"` as inline style
    - tab-panel.tsx: does NOT contain `fontWeight: 600` as inline style (except computed)

    - anfrage-detail-view.tsx: grep `style={{` count is exactly 0
    - anfrage-detail-view.tsx: contains `className="admin-content"` (wrapper class matching dashboard page)
    - anfrage-detail-view.tsx: contains `className="detail-view__standard-link"`
    - anfrage-detail-view.tsx: contains `className="link-standard-view"`
    - anfrage-detail-view.tsx: contains `className="detail-view__action-zone"`
    - anfrage-detail-view.tsx: contains `className="detail-view__empty-state"`
    - anfrage-detail-view.tsx: contains `className="detail-view__products-column"`
    - anfrage-detail-view.tsx: contains `className="detail-view__loading"` (appears 2 times)
    - anfrage-detail-view.tsx: does NOT contain `padding: "24px", maxWidth: "1400px"` (old wrapper inline style removed)
    - anfrage-detail-view.tsx: does NOT contain `style={{` anywhere
  </acceptance_criteria>
  <done>All 5 React component files refactored to use CSS classes from custom.scss. product-card.tsx and anfrage-detail-view.tsx have ZERO inline styles. attention-bar.tsx retains only 2 dynamic style blocks (status colors). splitbutton.tsx retains at most 7 dynamic style blocks (button colors, borders). tab-panel.tsx retains at most 3 dynamic style blocks (tab active color, disabled opacity). The wrapper in anfrage-detail-view.tsx uses className="admin-content" matching the dashboard page pattern.</done>
</task>

</tasks>

<verification>
1. `grep -c 'style={{' src/components/admin/anfrage-detail-view.tsx` -- should return 0
2. `grep -c 'style={{' src/components/admin/product-card.tsx` -- should return 0
3. `grep -c 'style={{' src/components/admin/attention-bar.tsx` -- should return 2
4. `grep -c 'style={{' src/components/admin/splitbutton.tsx` -- should return at most 7
5. `grep -c 'style={{' src/components/admin/tab-panel.tsx` -- should return at most 3
6. `grep 'className="admin-content"' src/components/admin/anfrage-detail-view.tsx` -- should match
7. `grep -c 'text-display\|text-body\|text-heading\|text-caption\|text-label\|form-textarea\|btn-ghost\|btn-primary\|btn-destructive\|error-message\|kontakt-tab\|details-tab\|price-single\|link-email\|detail-field' src/app/\(payload\)/custom.scss` -- should return 40+
8. `grep 'admin-content' src/app/\(payload\)/custom.scss` -- should match (class exists in CSS)
</verification>

<success_criteria>
- custom.scss extended with ~60 new CSS classes for typography, forms, buttons, links, layout
- anfrage-detail-view.tsx uses className="admin-content" wrapper (matching dashboard page)
- anfrage-detail-view.tsx has 0 inline style= attributes
- product-card.tsx has 0 inline style= attributes
- attention-bar.tsx has exactly 2 inline style= attributes (dynamic status/urgency colors)
- splitbutton.tsx has at most 7 inline style= attributes (dynamic button colors + borders)
- tab-panel.tsx has at most 3 inline style= attributes (dynamic tab color + disabled opacity)
- All existing className references from Plan 01/02 still work (no regression)
- Phase 19 Success Criterion 1 fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/19-admin-detail-view-redesign/19-04-SUMMARY.md`
</output>
