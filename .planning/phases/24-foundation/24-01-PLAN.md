---
phase: 24-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/payload-globals/settings.ts
  - src/lib/settings.ts
  - src/payload.config.ts
  - src/components/admin/custom-nav.tsx
  - src/components/admin/settings-page.tsx
  - src/app/(payload)/custom.scss
  - tests/unit/test-settings.test.ts
autonomous: true
requirements: [BASE-01]
must_haves:
  truths:
    - "Admin kann in der Einstellungen-Page Firmendaten, MwSt-Satz, Stripe-Config und Dokumenten-Einstellungen pflegen"
    - "Aenderungen wirken sofort (kein Cache, jeder API-Call liest aktuellen Wert aus DB)"
    - "Einstellungen-Link ist im System-Dropdown der Navigation sichtbar"
    - "Nicht-Admins sehen alle Felder als read-only, Save-Button ist versteckt"
  artifacts:
    - path: "src/payload-globals/settings.ts"
      provides: "Settings Global config with all field groups"
      exports: ["Settings"]
    - path: "src/lib/settings.ts"
      provides: "getSettings() helper"
      exports: ["getSettings"]
    - path: "src/components/admin/settings-page.tsx"
      provides: "Custom Admin Page with 4 tabs"
      exports: ["default"]
    - path: "tests/unit/test-settings.test.ts"
      provides: "Unit test for getSettings() runtime behavior"
  key_links:
    - from: "src/components/admin/settings-page.tsx"
      to: "settings Global API"
      via: "fetch to /api/globals/settings"
      pattern: "api/globals/settings"
    - from: "src/payload.config.ts"
      to: "src/payload-globals/settings.ts"
      via: "globals array import"
      pattern: "import.*Settings.*settings"
    - from: "src/components/admin/custom-nav.tsx"
      to: "/admin/einstellungen"
      via: "SYSTEM_SECTION items array"
      pattern: "einstellungen"
    - from: "src/lib/settings.ts"
      to: "downstream tax/PDF consumers"
      via: "direct getPayload call on every invocation (no cache)"
      pattern: "findGlobal.*settings"
---

<objective>
Create the Settings Global (Payload data layer), getSettings() helper with unit test, Custom Admin Page with 4 tabs (Firmendaten, Steuer, Stripe, Dokumente), and add the Einstellungen navigation link.

Purpose: Phase 24 foundation -- all downstream features (E-Mail, PDF, Stripe) read company data, tax rate, and document settings from this single source of truth.
Output: Settings Global registered in Payload, getSettings() helper in lib/ with unit test, Settings Admin Page at /admin/einstellungen, navigation link in System dropdown.
</objective>

<execution_context>
@/Users/janisjankewitz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/janisjankewitz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-foundation/24-CONTEXT.md
@.planning/phases/24-foundation/24-RESEARCH.md
@.planning/phases/24-foundation/24-UI-SPEC.md

<interfaces>
<!-- Existing Payload Global pattern from webhook-errors.ts -->
From src/payload-globals/webhook-errors.ts:
```typescript
import type { GlobalConfig } from 'payload'
export const WebhookErrors: GlobalConfig = {
  slug: 'webhook_errors',
  label: 'Webhook Fehler',
  admin: { group: 'System' },
  access: { read: ..., update: ... },
  fields: [ ... ],
}
```

From src/payload.config.ts (globals registration):
```typescript
globals: [WebhookErrors, Navigation, Footer],
```

From src/payload.config.ts (custom views registration):
```typescript
admin: {
  components: {
    views: {
      dashboard: { Component: "@/components/admin/dashboard-overview#default" },
    },
  },
},
```

From src/components/admin/custom-nav.tsx (System section, lines 124-133):
```typescript
const SYSTEM_SECTION: NavSection = {
  key: "system",
  label: "System",
  roles: ["admin"],
  items: [
    { label: "Medien", href: "/admin/collections/media" },
    { label: "Edit-History", href: "/admin/collections/edit_history" },
    { label: "Webhook Fehler", href: "/admin/globals/webhook_errors" },
  ],
};
```

From src/app/(payload)/custom.scss (established CSS classes):
- `.admin-content` - page container with padding
- `.tab-panel__bar`, `.tab-panel__trigger`, `.tab-panel__trigger--active`, `.tab-panel__content` - tab pattern
- `.form-input`, `.form-textarea`, `.form-select`, `.form-group`, `.form-row-grid` - form elements
- `.btn-save` - save button
- `.text-display`, `.text-caption`, `.text-label`, `.text-section-heading`, `.text-body-muted` - typography
- `.detail-view__loading` - loading state
- `.error-message` - error display
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Settings Global + getSettings helper + unit test + payload.config registration + nav link</name>
  <files>src/payload-globals/settings.ts, src/lib/settings.ts, src/payload.config.ts, src/components/admin/custom-nav.tsx, tests/unit/test-settings.test.ts</files>
  <read_first>
    - src/payload-globals/webhook-errors.ts (pattern for GlobalConfig with access control)
    - src/payload-globals/navigation.ts (pattern for Global with fields)
    - src/payload.config.ts (globals array, views registration -- full file)
    - src/components/admin/custom-nav.tsx (SYSTEM_SECTION items array, lines 120-135)
    - src/collections/system/media.ts (media collection slug for upload relationTo)
    - .planning/phases/24-foundation/24-CONTEXT.md (locked field decisions)
    - .planning/phases/24-foundation/24-RESEARCH.md (Settings Global code example)
    - tests/unit/test-server-price.test.ts (existing test pattern for Jest mocking in this project)
    - jest.config.ts (Jest configuration)
  </read_first>
  <behavior>
    tests/unit/test-settings.test.ts:
    - getSettings() calls payload.findGlobal with slug 'settings'
    - getSettings() returns the data from findGlobal (mock returns { mwst_satz: 19, firmenname: 'Test GmbH' })
    - getSettings() does NOT cache between calls (two calls = two findGlobal invocations, verified by mock call count)
  </behavior>
  <action>
1. Create `src/payload-globals/settings.ts` with a Payload GlobalConfig:
   - slug: `'settings'`
   - label: `'Einstellungen'`
   - admin: `{ group: 'System', hidden: true }` (hidden because we use Custom Admin Page)
   - access: read for roles `['admin', 'mitarbeiter', 'viewer']` via `req.user?.rolle`, update only for `req.user?.rolle === 'admin'`
   - Fields (flat, no nested groups -- Payload Global flat fields work best for the custom page):
     **Firmendaten:**
     - `firmenname` (text, label 'Firmenname')
     - `adresse_strasse` (text, label 'Strasse')
     - `adresse_hausnummer` (text, label 'Hausnummer')
     - `adresse_plz` (text, label 'PLZ')
     - `adresse_ort` (text, label 'Ort')
     - `telefon` (text, label 'Telefon')
     - `email` (email, label 'E-Mail')
     - `steuernummer` (text, label 'Steuernummer')
     - `ust_id` (text, label 'USt-IdNr.')
     - `bank_iban` (text, label 'IBAN')
     - `bank_bic` (text, label 'BIC')
     - `bank_name` (text, label 'Bank-Name')
     **Steuer:**
     - `mwst_satz` (number, label 'MwSt-Satz (%)', defaultValue 19, required true)
     - `preisanzeige` (select, label 'Preisanzeige', defaultValue 'brutto', options: [{label: 'Brutto (inkl. MwSt)', value: 'brutto'}, {label: 'Netto (zzgl. MwSt)', value: 'netto'}])
     **Stripe:**
     - `stripe_zahlungslink_ablauf_stunden` (number, label 'Zahlungslink-Ablaufzeit (Stunden)', defaultValue 24)
     - `stripe_waehrung` (select, label 'Waehrung', defaultValue 'eur', options: EUR/USD/PYG)
     **Dokumente:**
     - `angebots_gueltigkeit_tage` (number, label 'Angebots-Gueltigkeit (Tage)', defaultValue 30)
     - `widerrufsbelehrung` (textarea, label 'Widerrufsbelehrung')
     - `agb_link` (text, label 'AGB-Link')
     - `agb_pdf` (upload, relationTo 'media', label 'AGB als PDF')
     - `pdf_logo` (upload, relationTo 'media', label 'Logo fuer PDFs')
     **E-Mail (prepared for Phase 25):**
     - `email_absender_name` (text, label 'E-Mail Absender-Name')
     - `email_reply_to` (email, label 'Reply-To Adresse')
     - `email_signatur` (textarea, label 'E-Mail-Signatur')
     **Meta:**
     - `zuletzt_aktualisiert_am` (date, admin: { readOnly: true })
     - `zuletzt_aktualisiert_von` (relationship, relationTo 'users', admin: { readOnly: true })
   - hooks.beforeChange: Set `data.zuletzt_aktualisiert_am = new Date().toISOString()` and `data.zuletzt_aktualisiert_von = req.user?.id`

2. Create `src/lib/settings.ts`:
   ```typescript
   import { getPayload } from 'payload'
   import config from '@payload-config'

   export async function getSettings() {
     const payload = await getPayload({ config })
     return payload.findGlobal({ slug: 'settings' })
   }
   ```
   No caching per user decision (every call reads from DB).

3. Create `tests/unit/test-settings.test.ts`:
   - Mock `payload` module: `jest.mock('payload', () => ({ getPayload: jest.fn() }))`
   - Mock `@payload-config`: `jest.mock('@payload-config', () => ({}), { virtual: true })`
   - In beforeEach, set up mock: `const mockFindGlobal = jest.fn().mockResolvedValue({ mwst_satz: 19, firmenname: 'Test GmbH' })` and configure `(getPayload as jest.Mock).mockResolvedValue({ findGlobal: mockFindGlobal })`
   - Test 1: `getSettings()` calls `payload.findGlobal` with `{ slug: 'settings' }` -- verify mockFindGlobal was called with that argument
   - Test 2: `getSettings()` returns the mock data -- assert result has `mwst_satz: 19`
   - Test 3: Calling `getSettings()` twice results in `mockFindGlobal` being called twice (not cached) -- verify `mockFindGlobal.mock.calls.length === 2`

4. Update `src/payload.config.ts`:
   - Add import: `import { Settings } from './payload-globals/settings'`
   - Add to globals array: `globals: [WebhookErrors, Navigation, Footer, Settings]`
   - Register custom view for settings page:
     ```typescript
     views: {
       dashboard: { Component: "@/components/admin/dashboard-overview#default" },
       einstellungen: {
         Component: "@/components/admin/settings-page#default",
         path: "/einstellungen",
       },
       puckEditorWithViewports: { ... },  // keep existing
     },
     ```

5. Update `src/components/admin/custom-nav.tsx`:
   - Add to SYSTEM_SECTION.items array (after existing items):
     `{ label: "Einstellungen", href: "/admin/einstellungen" }`
   - The icon is handled via the nav rendering logic (lucide Settings icon) -- if the nav uses icons, add `import { Settings as SettingsIcon } from 'lucide-react'` and reference it. If the nav items don't have icon fields (check the NavItem type), just add the text label per existing pattern.
  </action>
  <verify>
    <automated>npx jest --testPathPattern="test-settings" --no-coverage -x 2>&1 | tail -20 && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/payload-globals/settings.ts exports `const Settings: GlobalConfig`
    - src/payload-globals/settings.ts contains `slug: 'settings'`
    - src/payload-globals/settings.ts contains `hidden: true`
    - src/payload-globals/settings.ts contains `mwst_satz` field with `defaultValue: 19`
    - src/payload-globals/settings.ts contains `preisanzeige` field with `defaultValue: 'brutto'`
    - src/payload-globals/settings.ts contains `stripe_zahlungslink_ablauf_stunden` with `defaultValue: 24`
    - src/payload-globals/settings.ts contains `stripe_waehrung` with `defaultValue: 'eur'`
    - src/payload-globals/settings.ts contains `angebots_gueltigkeit_tage` with `defaultValue: 30`
    - src/payload-globals/settings.ts contains `agb_pdf` field with `relationTo: 'media'`
    - src/payload-globals/settings.ts contains `pdf_logo` field with `relationTo: 'media'`
    - src/payload-globals/settings.ts contains `email_absender_name`, `email_reply_to`, `email_signatur` fields
    - src/payload-globals/settings.ts contains `zuletzt_aktualisiert_am` and `zuletzt_aktualisiert_von` fields
    - src/payload-globals/settings.ts contains beforeChange hook setting `zuletzt_aktualisiert_am`
    - src/payload-globals/settings.ts access.update checks `req.user?.rolle === 'admin'`
    - src/lib/settings.ts exports `async function getSettings()`
    - src/lib/settings.ts calls `payload.findGlobal({ slug: 'settings' })`
    - src/lib/settings.ts does NOT contain any caching logic (no Map, no let cached, no TTL)
    - tests/unit/test-settings.test.ts exists with at least 3 test cases
    - tests/unit/test-settings.test.ts verifies findGlobal is called with slug 'settings'
    - tests/unit/test-settings.test.ts verifies no caching (two calls = two findGlobal invocations)
    - `npx jest --testPathPattern="test-settings" --no-coverage -x` exits 0
    - src/payload.config.ts contains `import { Settings } from './payload-globals/settings'`
    - src/payload.config.ts globals array includes `Settings`
    - src/payload.config.ts contains `einstellungen:` view with path `"/einstellungen"`
    - src/components/admin/custom-nav.tsx SYSTEM_SECTION.items contains `{ label: "Einstellungen", href: "/admin/einstellungen" }`
  </acceptance_criteria>
  <done>Settings Global registered in Payload with all field groups (Firmendaten, Steuer, Stripe, Dokumente, E-Mail, Meta). getSettings() helper available with unit test proving no-cache behavior. Settings page view registered. Nav link added to System dropdown.</done>
</task>

<task type="auto">
  <name>Task 2: Settings Custom Admin Page with 4 tabs</name>
  <files>src/components/admin/settings-page.tsx</files>
  <read_first>
    - src/payload-globals/settings.ts (field names from Task 1 -- created in same plan)
    - src/components/admin/dashboard-overview.tsx (Custom Admin Page pattern with inline styles)
    - src/components/admin/anfrage-detail-view.tsx (tab pattern, form pattern, toast usage -- first 100 lines for structure)
    - src/components/admin/tab-panel.tsx (tab panel component if it exists -- check sessionStorage pattern)
    - src/app/(payload)/custom.scss (all CSS classes: .admin-content, .tab-panel__*, .form-*, .btn-save, .text-*)
    - .planning/phases/24-foundation/24-UI-SPEC.md (complete UI contract: layout, tabs, fields, states, copy)
    - .planning/phases/24-foundation/24-CONTEXT.md (locked decisions: 4 tabs, admin-only edit, immediate effect)
  </read_first>
  <action>
Create `src/components/admin/settings-page.tsx` as a React client component ('use client') that renders a Custom Admin Page at `/admin/einstellungen`:

**Page structure** (from UI-SPEC.md):
- Outer container with `.admin-content` class
- Page title: `<h1 className="text-display">Einstellungen</h1>`
- Last updated line: `<p className="text-caption">Zuletzt aktualisiert am {DD.MM.YYYY, HH:MM} von {username}</p>` -- format the `zuletzt_aktualisiert_am` date with `new Intl.DateTimeFormat('de-DE', { day: '2-digit', month: '2-digit', year: 'numeric', hour: '2-digit', minute: '2-digit' }).format(new Date(settings.zuletzt_aktualisiert_am))`. Get username by resolving `zuletzt_aktualisiert_von` (it's a relationship to users -- may come populated or as ID).

**Tab navigation:**
- 4 tabs: Firmendaten | Steuer | Stripe | Dokumente
- Use CSS classes `.tab-panel__bar` for container, `.tab-panel__trigger` for each tab button, add `.tab-panel__trigger--active` class on active tab
- Store active tab in `sessionStorage` with key `'settings-tab'` (same pattern as `anfrage-detail-tab` in tab-panel.tsx)
- On mount: read from sessionStorage, fallback to `'firmendaten'`

**Tab content** (`.tab-panel__content`):

**Tab 1 - Firmendaten:**
- Section "Firmenanschrift" (`.text-section-heading`)
  - firmenname (full width `.form-input`)
  - strasse + hausnummer (`.form-row-grid` -- 2 columns)
  - plz + ort (`.form-row-grid` -- 2 columns)
  - telefon (full width)
  - email (full width)
- Section "Steuerdaten"
  - steuernummer (full width)
  - ust_id (full width)
- Section "Bankverbindung"
  - Helper text: `<p className="text-body-muted">Optional -- Stripe ist primaeres Zahlungsmittel</p>`
  - bank_iban + bank_bic (`.form-row-grid`)
  - bank_name (full width)

**Tab 2 - Steuer:**
- Section "MwSt-Konfiguration"
  - mwst_satz (number input with "%" suffix text next to it)
  - Helper text below: `<p className="text-body-muted">Wird fuer alle Preisberechnungen, PDFs und Stripe-Zahlungen verwendet</p>`
  - preisanzeige (select with 2 options)

**Tab 3 - Stripe:**
- Section "Zahlungseinstellungen"
  - stripe_zahlungslink_ablauf_stunden (number input with "Stunden" suffix)
  - stripe_waehrung (select: EUR/USD/PYG)
  - Info box: div with inline styles `padding: '12px 16px', background: 'var(--theme-elevation-50)', border: '1px solid var(--theme-elevation-200)', borderRadius: '6px'` containing text: "API-Keys werden ueber Umgebungsvariablen (.env) konfiguriert und sind hier aus Sicherheitsgruenden nicht sichtbar."

**Tab 4 - Dokumente:**
- Section "Angebots-Einstellungen"
  - angebots_gueltigkeit_tage (number with "Tage" suffix)
- Section "Rechtliches"
  - widerrufsbelehrung (textarea, 6 rows)
  - agb_link (text input)
  - agb_pdf info text: "(Upload ueber Medien-Verwaltung)" -- for now, display the filename if set, link to media collection
  - pdf_logo info text: same pattern
- Section "E-Mail (vorbereitet)"
  - Helper text: `<p className="text-body-muted">Wird in Phase 25 (E-Mail-System) aktiviert</p>`
  - email_absender_name (text)
  - email_reply_to (email)
  - email_signatur (textarea, 4 rows)

**Save button:** At bottom of each tab content, `<button className="btn-save">Einstellungen speichern</button>`

**Data flow:**
- On mount: `fetch('/api/globals/settings')` to load current settings. Show loading state "Einstellungen werden geladen..." while loading.
- Local state for form values using `useState` with the fetched settings as initial values.
- On save: `fetch('/api/globals/settings', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(formData), credentials: 'include' })` -- Payload's Global REST API supports POST to update.
- On success: `toast.success('Einstellungen gespeichert')` using `import { toast } from '@payloadcms/ui'`. Refresh the "Zuletzt aktualisiert" display.
- On error: Show `.error-message` div above save button.
- Button loading state: text "Wird gespeichert..." + `style={{ opacity: 0.6 }}` + `disabled={true}`.

**Read-only mode for non-admins:**
- Fetch current user via `import { useAuth } from '@payloadcms/ui'` -- `const { user } = useAuth()`. Check `user?.rolle === 'admin'`.
- If not admin: all inputs get `disabled` attribute, save button is hidden, show info text: "Nur Administratoren koennen Einstellungen aendern."

**Upload fields (agb_pdf, pdf_logo):**
- These are Payload upload/relationship fields. In the custom page, display the current filename (if set) with a link to the media item. For changing the upload, link to the media collection: "Bitte ueber die Medien-Verwaltung hochladen und hier die ID eintragen" or use a simple text input showing the media ID (since the full Payload upload widget is not available in custom pages). Alternatively, just show the current value as read-only with a link to `/admin/collections/media`.

**Styling:** Use existing CSS classes from custom.scss. For any layout needs not covered by existing classes, use inline styles (per admin component pattern -- no Tailwind in Payload Admin).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/admin/settings-page.tsx contains `'use client'`
    - src/components/admin/settings-page.tsx contains `export default function` (or `export default`)
    - src/components/admin/settings-page.tsx contains `api/globals/settings` (fetch call)
    - src/components/admin/settings-page.tsx contains `sessionStorage` with key `settings-tab`
    - src/components/admin/settings-page.tsx contains 4 tab triggers: 'Firmendaten', 'Steuer', 'Stripe', 'Dokumente'
    - src/components/admin/settings-page.tsx contains `toast.success` call with 'Einstellungen gespeichert'
    - src/components/admin/settings-page.tsx contains `Wird gespeichert...` loading state text
    - src/components/admin/settings-page.tsx contains `Zuletzt aktualisiert am` display
    - src/components/admin/settings-page.tsx contains `useAuth` import from '@payloadcms/ui'
    - src/components/admin/settings-page.tsx contains read-only check: `rolle === 'admin'` or similar
    - src/components/admin/settings-page.tsx contains text "Nur Administratoren koennen Einstellungen aendern"
    - src/components/admin/settings-page.tsx contains text "Optional -- Stripe ist primaeres Zahlungsmittel"
    - src/components/admin/settings-page.tsx contains text "Wird in Phase 25 (E-Mail-System) aktiviert"
    - src/components/admin/settings-page.tsx contains CSS class references: `admin-content`, `tab-panel__bar`, `form-input`, `btn-save`
    - src/components/admin/settings-page.tsx does NOT import from 'tailwindcss' or use `className="bg-*"` tailwind classes
  </acceptance_criteria>
  <done>Custom Admin Page renders at /admin/einstellungen with 4 tabs, loads settings from Payload Global API, saves with toast feedback, shows last-updated timestamp, and enforces admin-only editing.</done>
</task>

</tasks>

<verification>
1. `npx jest --testPathPattern="test-settings" --no-coverage -x` passes
2. `npx tsc --noEmit` passes without errors
3. Settings Global is registered: `grep -n "Settings" src/payload.config.ts` shows import and globals entry
4. Settings page registered as custom view: `grep -n "einstellungen" src/payload.config.ts`
5. Navigation link added: `grep -n "Einstellungen" src/components/admin/custom-nav.tsx`
6. getSettings() helper exists and reads from DB without caching: `grep -n "findGlobal" src/lib/settings.ts`
</verification>

<success_criteria>
- Settings Global has all fields (Firmendaten, Steuer, Stripe, Dokumente, E-Mail, Meta) with correct defaults
- Custom Admin Page renders 4 tabs with correct form fields per UI-SPEC
- Save flow works via Payload Global REST API
- Non-admin users see read-only state
- Navigation link visible in System dropdown
- getSettings() helper available for downstream consumers (tax.ts, PDF, E-Mail) with unit test proving no-cache behavior
</success_criteria>

<output>
After completion, create `.planning/phases/24-foundation/24-01-SUMMARY.md`
</output>
