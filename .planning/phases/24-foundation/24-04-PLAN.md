---
phase: 24-foundation
plan: 04
type: execute
wave: 3
depends_on: [02, 03]
files_modified:
  - src/collections/business/anfragen.ts
  - src/components/admin/anfrage-detail-view.tsx
  - src/components/admin/status-workflow.tsx
  - src/lib/anfrage/price-server.ts
  - src/lib/stripe.ts
  - src/lib/cart/store.ts
  - src/app/api/anfrage/submit/route.ts
  - src/app/api/anfrage/calculate-price/route.ts
  - src/seed/data/preisregeln.ts
  - src/seed/data/farben.ts
  - src/seed/data/verglasungen.ts
  - src/seed/data/extras.ts
  - src/seed/data/sprossen.ts
  - src/seed/data/glasdekore.ts
  - src/seed/data/sicherheitsglas.ts
  - src/seed/data/schallschutz.ts
  - src/components/admin/anfragen-list-view.tsx
  - src/components/admin/product-card.tsx
  - src/components/admin/attention-bar.tsx
  - src/components/admin/dashboard-overview.tsx
  - src/components/admin/anfrage-edit-button.tsx
  - tests/unit/test-optimistic-lock.test.ts
autonomous: true
requirements: [BASE-04]
must_haves:
  truths:
    - "Concurrent edits on Anfrage are detected and the user sees a toast with reload button"
    - "API returns HTTP 409 Conflict when version mismatch occurs"
    - "Anfrage version field starts at 1 and increments on every update"
    - "All price fields across the system store integer cents (not float EUR)"
    - "Stripe unit_amount uses gesamtpreis directly (already in cents, no *100 conversion)"
    - "Admin display components show prices via formatCents (not formatCurrency with float)"
  artifacts:
    - path: "src/collections/business/anfragen.ts"
      provides: "version field + optimistic locking beforeChange hook"
      contains: "version"
    - path: "src/lib/anfrage/price-server.ts"
      provides: "Server price calculation in cents"
      contains: "Math.round"
  key_links:
    - from: "src/collections/business/anfragen.ts"
      to: "HTTP 409 response"
      via: "beforeChange hook throws APIError with 409"
      pattern: "APIError.*409"
    - from: "src/components/admin/anfrage-detail-view.tsx"
      to: "toast notification + reload button"
      via: "catch 409 response, show toast, render Seite neu laden button"
      pattern: "409.*toast.*reload"
    - from: "src/lib/stripe.ts"
      to: "src/collections/business/anfragen.ts"
      via: "gesamtpreis already in cents"
      pattern: "unit_amount.*gesamtpreis"
    - from: "src/components/admin/anfragen-list-view.tsx"
      to: "src/lib/format-currency.ts"
      via: "formatCents for price display"
      pattern: "formatCents"
    - from: "src/components/admin/product-card.tsx"
      to: "src/lib/format-currency.ts"
      via: "formatCents for einzelpreis display"
      pattern: "formatCents"
---

<objective>
Add optimistic locking (version field + 409 conflict detection + reload button) to Anfragen collection, and migrate all price fields from float EUR to integer cents across the stack (price-server, stripe, cart store, seed data, submit route, calculate-price route, admin display components).

Purpose: Prevent silent data loss from concurrent admin edits, and establish cent-integer arithmetic as the single standard for all money operations -- prerequisite for PDF, Stripe, and Angebots features.
Output: Version-locked Anfragen, cent-based prices throughout, updated seed data, updated Stripe integration, admin display using formatCents.
</objective>

<execution_context>
@/Users/janisjankewitz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/janisjankewitz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-foundation/24-CONTEXT.md
@.planning/phases/24-foundation/24-RESEARCH.md
@.planning/phases/24-foundation/24-02-SUMMARY.md

<interfaces>
From src/lib/tax.ts (created in Plan 02):
```typescript
export function calcGrossFromNet(netCents: number, ratePercent: number): number
export function calcNetFromGross(grossCents: number, ratePercent: number): number
export function calcTax(netCents: number, ratePercent: number): number
export function splitLine(unitCents: number, qty: number, ratePercent: number): { netCents, taxCents, grossCents }
```

From src/lib/format-currency.ts (extended in Plan 02):
```typescript
export function formatCurrency(amount: number): string  // existing, backward compat
export function formatPrice(price: number | null | undefined): string  // existing
export function formatCents(cents: number, currency?: string): string  // NEW from Plan 02
```

From src/collections/business/anfragen.ts (current state):
- gesamtpreis: type: 'number', label: 'Gesamtpreis (EUR)'
- produkte[].einzelpreis: type: 'number', label: 'Einzelpreis (EUR)'
- hooks.beforeChange: status transition validation, interne_notizen logging
- hooks.afterChange: webhook sending, Stripe checkout session creation

From src/lib/stripe.ts:
```typescript
export async function createCheckoutSession(
  anfrageId: string, anfrageNummer: string,
  gesamtpreis: number, produktAnzahl: number
)
// Currently: unit_amount: Math.round(gesamtpreis * 100)
```

From src/lib/anfrage/price-server.ts:
```typescript
export async function calculateServerPrice(
  selections: KonfiguratorSelections,
  payload: BasePayload,
  opts?: PriceCalcOptions
): Promise<number>
// Currently returns float EUR (e.g. 249.99)
// Last line: return Math.round(preis * 100) / 100
```

From src/app/api/anfrage/submit/route.ts:
```typescript
// gesamtpreis += serverPrice * item.quantity
// gesamtpreis = Math.round(gesamtpreis * 100) / 100
// rabatt: Math.round(gesamtpreis * (1 - latest.wert / 100) * 100) / 100
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Optimistic Locking on Anfragen (version field + beforeChange hook + UI toast with reload button)</name>
  <files>src/collections/business/anfragen.ts, src/components/admin/anfrage-detail-view.tsx, src/components/admin/status-workflow.tsx, tests/unit/test-optimistic-lock.test.ts</files>
  <read_first>
    - src/collections/business/anfragen.ts (full file -- understand existing beforeChange hooks, field structure)
    - src/components/admin/anfrage-detail-view.tsx (first 150 lines -- understand how it saves data, how to catch 409)
    - src/components/admin/status-workflow.tsx (full file -- this sends status changes, needs to include version)
    - .planning/phases/24-foundation/24-RESEARCH.md (Optimistic Locking beforeChange hook code, Pitfall 6: version update after save)
    - .planning/phases/24-foundation/24-CONTEXT.md (locked: version number field, beforeChange hook, 409 Conflict, Toast + Reload-Button)
    - .planning/phases/24-foundation/24-UI-SPEC.md (Optimistic Locking Toast section: message text, persistent duration, reload action)
  </read_first>
  <behavior>
    tests/unit/test-optimistic-lock.test.ts:
    - Hook passes when data.version matches originalDoc.version (returns data with incremented version)
    - Hook throws APIError with status 409 when data.version !== originalDoc.version
    - Hook error message contains "zwischenzeitlich" (German text per UI-SPEC)
    - Hook increments version: originalDoc.version=1, result.version=2
    - Hook handles first create (operation 'create'): does not throw, sets version to 1
    - Hook handles missing version gracefully (originalDoc.version undefined): treats as version 1
  </behavior>
  <action>
1. **Add version field to Anfragen collection** (`src/collections/business/anfragen.ts`):
   - Add a new field AFTER the `interne_notizen` field (before the closing `]` of fields array):
     ```typescript
     {
       name: 'version',
       type: 'number',
       label: 'Version',
       defaultValue: 1,
       admin: {
         readOnly: true,
         position: 'sidebar',
         description: 'Wird automatisch bei jeder Aenderung erhoeht.',
       },
     },
     ```

2. **Add optimistic locking to beforeChange hook** (`src/collections/business/anfragen.ts`):
   - INSIDE the existing beforeChange hook function (the one that handles status transitions), add optimistic locking check BEFORE any other logic:
     ```typescript
     // Optimistic Locking: compare versions
     if (operation === 'update' && originalDoc && data) {
       if (
         data.version !== undefined &&
         originalDoc.version !== undefined &&
         data.version !== originalDoc.version
       ) {
         throw new APIError(
           'Diese Anfrage wurde zwischenzeitlich von einem anderen Benutzer geaendert. Bitte laden Sie die Seite neu.',
           409,
         )
       }
       // Always increment version on update
       data.version = (originalDoc.version || 1) + 1
     }
     ```
   - Place this check at the BEGINNING of the existing beforeChange handler, before the status transition checks.

3. **Update status-workflow component** (`src/components/admin/status-workflow.tsx`):
   - Find where the component sends PATCH/PUT to update the Anfrage status
   - Include the current `version` field in the request body so the server can compare it
   - After successful save, update the local version state from the response data
   - This component likely gets the Anfrage data as a prop -- ensure it reads `doc.version` and sends it with status changes

4. **Add 409 error handling with reload button in anfrage-detail-view** (`src/components/admin/anfrage-detail-view.tsx`):
   - Find where the component makes save/update requests (fetch calls, Payload API calls)
   - Add catch for HTTP 409 responses:
     ```typescript
     if (response.status === 409) {
       toast.error('Diese Anfrage wurde zwischenzeitlich von einem anderen Benutzer geaendert. Bitte laden Sie die Seite neu.')
       setIsConflict(true)
       return
     }
     ```
   - Add an `isConflict` state variable: `const [isConflict, setIsConflict] = useState(false)`
   - When `isConflict` is true, render a visible reload button in the UI (e.g., above the form content or in the error area):
     ```tsx
     {isConflict && (
       <div className="error-message" style={{ display: 'flex', alignItems: 'center', gap: '12px' }}>
         <span>Diese Anfrage wurde zwischenzeitlich geaendert.</span>
         <button
           onClick={() => window.location.reload()}
           className="btn-save"
           style={{ margin: 0 }}
         >
           Seite neu laden
         </button>
       </div>
     )}
     ```
   - The toast from `@payloadcms/ui` is already imported in this component.

5. **Write unit test** (`tests/unit/test-optimistic-lock.test.ts`):
   - Extract the optimistic locking logic into a testable function or test the hook behavior directly
   - Test the core comparison logic: matching versions pass, mismatching versions throw 409
   - Test version increment: after successful save, version is incremented
  </action>
  <verify>
    <automated>npx jest --testPathPattern="test-optimistic-lock" --no-coverage -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/collections/business/anfragen.ts contains field `{ name: 'version', type: 'number', defaultValue: 1 }`
    - src/collections/business/anfragen.ts contains `admin: { readOnly: true }` on version field
    - src/collections/business/anfragen.ts beforeChange hook contains `data.version !== originalDoc.version`
    - src/collections/business/anfragen.ts beforeChange hook contains `throw new APIError(` with `409`
    - src/collections/business/anfragen.ts beforeChange hook contains `data.version = (originalDoc.version || 1) + 1`
    - src/collections/business/anfragen.ts contains error message "zwischenzeitlich"
    - src/components/admin/anfrage-detail-view.tsx contains `409` response handling
    - src/components/admin/anfrage-detail-view.tsx contains `toast.error` or toast call with version conflict message
    - src/components/admin/anfrage-detail-view.tsx contains `isConflict` state variable
    - src/components/admin/anfrage-detail-view.tsx contains `window.location.reload()` or `router.refresh()` in a reload button
    - src/components/admin/anfrage-detail-view.tsx contains text 'Seite neu laden' as button label
    - src/components/admin/status-workflow.tsx sends `version` field in update request body
    - tests/unit/test-optimistic-lock.test.ts exists with at least 4 test cases
    - `npx jest --testPathPattern="test-optimistic-lock" --no-coverage -x` exits 0
  </acceptance_criteria>
  <done>Anfragen collection has version field starting at 1, incrementing on every update. Concurrent edits detected via beforeChange hook returning 409. Admin UI shows toast warning on version conflict AND renders a "Seite neu laden" reload button when conflict is detected.</done>
</task>

<task type="auto">
  <name>Task 2: Cent Migration -- convert all price fields from float EUR to integer cents</name>
  <files>src/lib/anfrage/price-server.ts, src/lib/stripe.ts, src/app/api/anfrage/submit/route.ts, src/app/api/anfrage/calculate-price/route.ts, src/lib/cart/store.ts, src/seed/data/preisregeln.ts, src/seed/data/farben.ts, src/seed/data/verglasungen.ts, src/seed/data/extras.ts, src/seed/data/sprossen.ts, src/seed/data/glasdekore.ts, src/seed/data/sicherheitsglas.ts, src/seed/data/schallschutz.ts, src/collections/business/anfragen.ts, src/components/admin/anfragen-list-view.tsx, src/components/admin/product-card.tsx, src/components/admin/attention-bar.tsx, src/components/admin/dashboard-overview.tsx, src/components/admin/anfrage-edit-button.tsx</files>
  <read_first>
    - src/lib/anfrage/price-server.ts (full file -- current float-based price calculation)
    - src/lib/stripe.ts (full file -- current Math.round(gesamtpreis * 100) conversion)
    - src/app/api/anfrage/submit/route.ts (full file -- gesamtpreis accumulation, discount application)
    - src/app/api/anfrage/calculate-price/route.ts (full file -- returns price to client)
    - src/lib/cart/store.ts (full file -- cart subtotal calculation)
    - src/lib/cart/types.ts (CartItem type with price field)
    - src/seed/data/preisregeln.ts (first 50 lines -- grundpreis_pro_m2 values)
    - src/seed/data/farben.ts (aufpreis values)
    - src/seed/data/verglasungen.ts (aufpreis values)
    - src/seed/data/extras.ts (aufpreis values)
    - src/seed/data/sprossen.ts (aufpreis values)
    - src/seed/data/glasdekore.ts (aufpreis values)
    - src/seed/data/sicherheitsglas.ts (aufpreis values)
    - src/seed/data/schallschutz.ts (aufpreis values)
    - src/collections/business/preisregeln.ts (field definitions for grundpreis_pro_m2)
    - src/collections/business/anfragen.ts (gesamtpreis and einzelpreis fields -- label update)
    - src/components/admin/anfragen-list-view.tsx (lines 25-35 for imports, lines 485-490 for gesamtpreis display)
    - src/components/admin/product-card.tsx (full file -- einzelpreis display with formatCurrency)
    - src/components/admin/attention-bar.tsx (lines 10-15 for imports, lines 90-95 for gesamtpreis display)
    - src/components/admin/dashboard-overview.tsx (lines 15-25 for imports, lines 85-90 for umsatz calc, lines 425-430 for price display)
    - src/components/admin/anfrage-edit-button.tsx (lines 250-260 for gesamtpreis input with parseFloat)
    - .planning/phases/24-foundation/24-RESEARCH.md (Pitfall 1: float-to-cent migration, State of the Art table)
    - .planning/phases/24-foundation/24-CONTEXT.md (locked: complete migration of all existing prices to cents, formatCents function)
  </read_first>
  <action>
**IMPORTANT: This is a systematic migration. Every file that touches money amounts must switch from float EUR to integer cents. The migration must be consistent -- no mixed float/cent states.**

1. **price-server.ts -- return cents instead of float** (`src/lib/anfrage/price-server.ts`):
   - The function currently returns `Math.round(preis * 100) / 100` (float EUR)
   - Change the ENTIRE calculation to work in cents:
     - `grundpreis_pro_m2` from preisregeln is currently stored as float EUR (e.g., 120 = 120 EUR). After seed migration it will be cents (12000 = 120.00 EUR). Update the area formula:
       - OLD: `let preis = flaeche * regel.grundpreis_pro_m2` (float * float = float)
       - NEW: `let preisCents = Math.round(flaeche * regel.grundpreis_pro_m2)` (float_area * cents = cents, round to integer)
     - `aufpreis` fields on ausstattung collections (farben, verglasungen, etc.) are also float EUR currently. After migration they'll be cents. The `findAufpreis` function returns the raw value which will now be cents.
       - OLD: `preis += findAufpreis(docs, selections.verglasung)` (float + float)
       - NEW: `preisCents += findAufpreis(docs, selections.verglasung)` (cents + cents)
     - Return: `return preisCents` (integer cents, no division by 100)
   - Update the function's JSDoc to document it returns integer cents
   - Update the return type hint if present

2. **stripe.ts -- remove the *100 conversion** (`src/lib/stripe.ts`):
   - OLD: `unit_amount: Math.round(gesamtpreis * 100)`
   - NEW: `unit_amount: gesamtpreis` (already in cents)
   - Update the JSDoc: `@param gesamtpreis - Total price in cents (e.g. 24999 = 249.99 EUR)`
   - Also update `currency` to use Settings value eventually, but for now keep 'eur' hardcoded (Settings integration deferred to Phase 27)

3. **submit route -- accumulate in cents** (`src/app/api/anfrage/submit/route.ts`):
   - `calculateServerPrice()` now returns cents
   - OLD: `gesamtpreis += serverPrice * item.quantity` then `gesamtpreis = Math.round(gesamtpreis * 100) / 100`
   - NEW: `gesamtpreis += serverPrice * item.quantity` (cents * int = cents, no rounding needed since both are integers)
   - Remove the `Math.round(gesamtpreis * 100) / 100` line (was float rounding, not needed for integers)
   - Discount application:
     - Prozent: OLD: `Math.round(gesamtpreis * (1 - latest.wert / 100) * 100) / 100`
     - Prozent NEW: `gesamtpreis = Math.round(gesamtpreis * (1 - latest.wert / 100))` (cents * fraction = cents, round once)
     - Absolut: OLD: `Math.round((gesamtpreis - latest.wert) * 100) / 100`
     - Absolut NEW: `gesamtpreis = Math.max(0, gesamtpreis - Math.round(latest.wert * 100))` (if wert is still in EUR) OR if wert is also in cents: `gesamtpreis = Math.max(0, gesamtpreis - latest.wert)`
     - NOTE: Check rabattcodes collection `wert` field -- if it's "10% off" it's a percentage (no cent conversion). If it's "5 EUR off" the `wert` field stores 5 (float EUR). For now, convert on the fly: `Math.round(latest.wert * 100)` for absolute discounts. Mark as TODO for rabattcodes collection cent migration in a future cleanup.
   - `einzelpreis: serverPrice` in produkteForCMS is now in cents -- correct

4. **calculate-price route** (`src/app/api/anfrage/calculate-price/route.ts`):
   - This route returns the price to the frontend for display
   - `calculateServerPrice()` now returns cents
   - The response should return cents: `{ price: serverPriceCents }`
   - Frontend consumer will need to use `formatCents()` for display -- but that's a frontend change. For the API, just return the integer.
   - Check what the current response format is and update accordingly

5. **Seed data -- convert ALL price-related seed files to cents**:

   `src/seed/data/preisregeln.ts`:
   - Change all `grundpreis_pro_m2` values from float EUR to integer cents:
     - 120 (EUR) -> 12000 (cents), 150 (EUR) -> 15000 (cents), 220 (EUR) -> 22000 (cents), etc.

   `src/seed/data/farben.ts`:
   - Convert all `aufpreis` values from float EUR to integer cents (multiply by 100)

   `src/seed/data/verglasungen.ts`:
   - Convert all `aufpreis` values from float EUR to integer cents

   `src/seed/data/extras.ts`:
   - Convert all `aufpreis` values from float EUR to integer cents

   `src/seed/data/sprossen.ts`:
   - Convert all `aufpreis` values from float EUR to integer cents

   `src/seed/data/glasdekore.ts`:
   - Convert all `aufpreis` values from float EUR to integer cents

   `src/seed/data/sicherheitsglas.ts`:
   - Convert all `aufpreis` values from float EUR to integer cents

   `src/seed/data/schallschutz.ts`:
   - Convert all `aufpreis` values from float EUR to integer cents

   For each file: read the file, find all numeric aufpreis/price values, multiply by 100 to get cents. If a value is 25.50 EUR -> 2550 cents. If a value is 0 -> 0 (no change).

6. **Anfragen collection field labels** (`src/collections/business/anfragen.ts`):
   - Change `gesamtpreis` label from `'Gesamtpreis (EUR)'` to `'Gesamtpreis (Cent)'`
   - Change `einzelpreis` label from `'Einzelpreis (EUR)'` to `'Einzelpreis (Cent)'`
   - These label changes document the unit change for anyone looking at the Payload admin

7. **Cart store** (`src/lib/cart/store.ts`):
   - The `getSubtotal()` function likely sums `item.price * item.quantity`
   - After migration, prices are in cents, so the subtotal will also be in cents -- no formula change needed
   - But verify: if `getSubtotal` does any float rounding, remove it
   - Check `CartItem` type in `src/lib/cart/types.ts` -- update any `price` field comment to say "in cents"

8. **Admin display components -- migrate from formatCurrency to formatCents:**

   `src/components/admin/anfragen-list-view.tsx`:
   - Change import: replace `formatCurrency` with `formatCents` from `@/lib/format-currency`
   - Update display: `formatCurrency(doc.gesamtpreis)` -> `formatCents(doc.gesamtpreis)`

   `src/components/admin/product-card.tsx`:
   - Change import: replace `formatCurrency` with `formatCents` from `@/lib/format-currency`
   - Update all `formatCurrency(p.einzelpreis)` calls to `formatCents(p.einzelpreis)`
   - Update `formatCurrency(p.einzelpreis * stueckzahl)` to `formatCents(p.einzelpreis * stueckzahl)`

   `src/components/admin/attention-bar.tsx`:
   - Change import: replace `formatCurrency` with `formatCents` from `@/lib/format-currency`
   - Update: `formatCurrency(gesamtpreis)` -> `formatCents(gesamtpreis)`

   `src/components/admin/dashboard-overview.tsx`:
   - Change import: replace `formatCurrency` with `formatCents` from `@/lib/format-currency`
   - Update umsatz sum: the sum of `doc.gesamtpreis` values will now be in cents, so `formatCurrency(umsatz)` -> `formatCents(umsatz)`
   - Update: `formatCurrency(doc.gesamtpreis)` -> `formatCents(doc.gesamtpreis)`

   `src/components/admin/anfrage-edit-button.tsx`:
   - The gesamtpreis input currently uses `parseFloat(v)` -- update to `parseInt(v, 10)` or `Math.round(parseFloat(v))` since values are now in cents (integers)
   - Update the input label/placeholder to indicate cents if appropriate

   NOTE: `src/components/admin/tab-panel.tsx` uses `formatCurrency(doc.rueckerstattung_betrag)` for refund amounts -- this is a separate field that may need migration later. Leave as-is for now with a TODO comment if encountered.

   NOTE: Kunden-facing components (`anfrage-detail.tsx`, `anfragen-liste.tsx`, `gast-tracking-form.tsx`, `stripe-pay-button.tsx`) use `formatPrice()` from format-currency.ts. These display prices to customers. After cent migration, `formatPrice()` will need to be updated or these components switched to `formatCents()`. However, these components are in kunden scope (not admin) and share the same API data. Add TODO comments in the components if they are not naturally updated by the type changes. The cart components (`cart-summary.tsx`, `cart-item-card.tsx`) use `formatEUR` from `@/lib/cart/format` which is a separate formatting module -- leave as-is with TODO for cart format migration.

**Migration note:** This does NOT include a database migration for existing Anfragen data. Existing Anfragen in the DB still have float prices. A Payload migration script would be needed for that. For now, we focus on the code migration -- new Anfragen will use cents. A TODO comment should be added: `// TODO: Migrate existing Anfragen prices to cents (Phase 24 cleanup)`
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/anfrage/price-server.ts does NOT contain `Math.round(preis * 100) / 100` (old float rounding removed)
    - src/lib/anfrage/price-server.ts return value is integer cents (no division by 100 at end)
    - src/lib/stripe.ts does NOT contain `Math.round(gesamtpreis * 100)` (old conversion removed)
    - src/lib/stripe.ts contains `unit_amount: gesamtpreis` (direct cents usage)
    - src/app/api/anfrage/submit/route.ts does NOT contain `Math.round(gesamtpreis * 100) / 100`
    - src/app/api/anfrage/submit/route.ts discount calculation uses `Math.round` for cent precision
    - src/seed/data/preisregeln.ts `grundpreis_pro_m2` values are >= 1000 (cents, not single/double digit EUR)
    - src/seed/data/farben.ts aufpreis values are in cents (integers, values >= 100 for non-zero entries)
    - src/seed/data/verglasungen.ts aufpreis values are in cents
    - src/seed/data/extras.ts aufpreis values are in cents
    - src/seed/data/sprossen.ts aufpreis values are in cents
    - src/seed/data/glasdekore.ts aufpreis values are in cents
    - src/seed/data/sicherheitsglas.ts aufpreis values are in cents
    - src/seed/data/schallschutz.ts aufpreis values are in cents
    - src/collections/business/anfragen.ts contains `'Gesamtpreis (Cent)'` or similar cent label
    - src/collections/business/anfragen.ts contains `'Einzelpreis (Cent)'` or similar cent label
    - src/components/admin/anfragen-list-view.tsx imports `formatCents` (not `formatCurrency` for price display)
    - src/components/admin/anfragen-list-view.tsx contains `formatCents(doc.gesamtpreis)` or equivalent
    - src/components/admin/product-card.tsx imports `formatCents` and uses it for einzelpreis display
    - src/components/admin/attention-bar.tsx imports `formatCents` and uses it for gesamtpreis display
    - src/components/admin/dashboard-overview.tsx imports `formatCents` and uses it for umsatz/gesamtpreis display
    - src/components/admin/anfrage-edit-button.tsx uses integer parsing for gesamtpreis (not parseFloat)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All price fields, calculations, seed data, and admin display components use integer cents. Stripe integration receives cents directly. No float-to-cent conversion at display or payment boundaries. Foundation ready for PDF/Angebots/Rechnungs features.</done>
</task>

</tasks>

<verification>
1. `npx jest --testPathPattern="test-optimistic-lock" --no-coverage` -- passes
2. `npx jest --no-coverage` -- full suite passes (no regressions from cent migration)
3. `npx tsc --noEmit` -- no type errors
4. `grep -rn "Math.round.*\* 100\)" src/lib/stripe.ts` returns empty (old conversion removed)
5. `grep -rn "formatCents" src/components/admin/` shows admin display uses formatCents
6. `grep -rn "formatCurrency.*gesamtpreis\|formatCurrency.*einzelpreis" src/components/admin/` returns empty (all migrated)
7. `grep -n "version" src/collections/business/anfragen.ts` shows version field and hook
8. `grep -n "Seite neu laden" src/components/admin/anfrage-detail-view.tsx` shows reload button
9. `grep -n "window.location.reload\|router.refresh" src/components/admin/anfrage-detail-view.tsx` shows reload action
</verification>

<success_criteria>
- Optimistic locking detects concurrent edits with 409 + toast + reload button
- Version field increments on every Anfrage update
- All price calculations use integer cents (no floats for money)
- All 8 seed data files store prices as cents
- Stripe receives gesamtpreis directly as cents (no *100 at boundary)
- Admin display components use formatCents() for price rendering
- Reload button renders when isConflict state is true
- Full test suite passes (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/24-foundation/24-04-SUMMARY.md`
</output>
