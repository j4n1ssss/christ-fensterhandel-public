---
phase: 27-stripe-end-to-end
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/unit/test-stripe-checkout.test.ts
  - tests/unit/test-stripe-webhook.test.ts
  - tests/unit/test-stripe-helpers.test.ts
  - tests/unit/test-stripe-refund.test.ts
  - tests/unit/test-stripe-redirect.test.ts
autonomous: true
requirements: [STRP-01, STRP-02, STRP-03, STRP-04, STRP-05, STRP-06, STRP-07, STRP-08, STRP-09, STRP-10, STRP-11]

must_haves:
  truths:
    - "Test stub files exist for all Stripe behavioral requirements"
    - "Each test file contains describe blocks with test.todo stubs for expected behaviors"
    - "Shared Stripe mock fixtures are available for all test files"
    - "npm test -- --testPathPattern=stripe runs without errors (all tests are .todo or skipped)"
  artifacts:
    - path: "tests/unit/test-stripe-checkout.test.ts"
      provides: "Test stubs for STRP-01, STRP-02, STRP-06, STRP-11 (checkout session creation, fields, double-payment, customer)"
      contains: "createCheckoutSession"
    - path: "tests/unit/test-stripe-webhook.test.ts"
      provides: "Test stubs for STRP-07, STRP-09, STRP-10 (webhook handlers, idempotency, dispute, logging)"
      contains: "handleCheckoutCompleted"
    - path: "tests/unit/test-stripe-helpers.test.ts"
      provides: "Test stubs for STRP-03 (helper functions, status colors, dashboard URL)"
      contains: "getStripeDashboardUrl"
    - path: "tests/unit/test-stripe-refund.test.ts"
      provides: "Test stubs for STRP-08 (refund route, validation, partial/full)"
      contains: "stripe.refunds.create"
    - path: "tests/unit/test-stripe-redirect.test.ts"
      provides: "Test stubs for STRP-04, STRP-05 (redirect route, auto-regeneration, rate limit)"
      contains: "redirect"
  key_links: []
---

<objective>
Create test stub files for all Stripe behavioral requirements before implementation begins.

Purpose: Nyquist compliance -- every plan's verify block references tests that must exist. Wave 0 creates the test scaffolding so that execution plans can run behavioral verification alongside type-checking. Existing test files (test-stripe-checkout.test.ts, test-stripe-webhook.test.ts) will be EXTENDED, not replaced.
Output: 5 test files with .todo stubs covering all STRP-01 through STRP-11 requirements.
</objective>

<execution_context>
@/Users/janisjankewitz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/janisjankewitz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-stripe-end-to-end/27-CONTEXT.md
@.planning/phases/27-stripe-end-to-end/27-VALIDATION.md

<interfaces>
<!-- Existing test infrastructure -->

From jest.config.ts:
```typescript
// preset: ts-jest, testEnvironment: jsdom
// roots: ["<rootDir>/tests"]
// moduleNameMapper: { "^@/(.*)$": "<rootDir>/src/$1" }
// testMatch: ["**/tests/**/*.test.ts", "**/tests/**/*.test.tsx"]
```

From tests/unit/test-stripe-checkout.test.ts (existing -- will be extended):
```typescript
// Already has some basic tests for createCheckoutSession
// Extend with STRP-01, STRP-02, STRP-06, STRP-11 stubs
```

From tests/unit/test-stripe-webhook.test.ts (existing -- will be extended):
```typescript
// Already has some basic tests for webhook handling
// Extend with 4 event type stubs, idempotency, dispute
```

Test naming convention: tests/unit/test-{feature}.test.ts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend existing Stripe test files and create new test stub files</name>
  <read_first>
    - tests/unit/test-stripe-checkout.test.ts (existing -- understand current stubs)
    - tests/unit/test-stripe-webhook.test.ts (existing -- understand current stubs)
    - tests/jest-setup.ts (for setup patterns)
    - .planning/phases/27-stripe-end-to-end/27-VALIDATION.md (per-task verification map)
  </read_first>
  <files>
    tests/unit/test-stripe-checkout.test.ts,
    tests/unit/test-stripe-webhook.test.ts,
    tests/unit/test-stripe-helpers.test.ts,
    tests/unit/test-stripe-refund.test.ts,
    tests/unit/test-stripe-redirect.test.ts
  </files>
  <action>
**1. Extend tests/unit/test-stripe-checkout.test.ts:**

Add the following .todo test stubs (do NOT remove existing tests -- append new describe blocks):

```typescript
// --- Phase 27 Stripe E2E Test Stubs ---

describe('createCheckoutSession (STRP-01)', () => {
  test.todo('creates session with correct currency from Settings');
  test.todo('creates session with expires_at from Settings ablauf_stunden');
  test.todo('clamps expires_at to Stripe 30min-24h window');
  test.todo('passes customer ID from findOrCreateStripeCustomer');
  test.todo('includes anfrage_id in session metadata');
  test.todo('uses redirect URLs with /zahlung/erfolgreich and /zahlung/abgebrochen');
});

describe('findOrCreateStripeCustomer (STRP-11)', () => {
  test.todo('returns existing customer ID from Users collection if userId provided');
  test.todo('looks up Stripe customer by email before creating new one');
  test.todo('creates new customer with DSGVO-minimal data (email + name only)');
  test.todo('stores customer ID on Users collection after creation');
  test.todo('handles guest checkout without userId');
});

describe('expireExistingSession (STRP-06)', () => {
  test.todo('expires open session successfully');
  test.todo('handles already-expired session gracefully');
  test.todo('handles non-existent session gracefully');
});

describe('Stripe fields on Anfragen (STRP-02)', () => {
  test.todo('stripe_payment_status field has correct select options');
  test.todo('stripe_refunded_amount_cents defaults to 0');
  test.todo('all stripe fields are readOnly in admin');
});
```

**2. Extend tests/unit/test-stripe-webhook.test.ts:**

Add the following .todo test stubs:

```typescript
// --- Phase 27 Expanded Webhook Stubs ---

describe('handleCheckoutCompleted (STRP-07)', () => {
  test.todo('sets status to bezahlt and stores payment_intent_id');
  test.todo('skips duplicate events when stripe_payment_status is already bezahlt');
  test.todo('ignores sessions without anfrage_id in metadata');
  test.todo('ignores sessions where payment_status is not paid');
});

describe('handleCheckoutExpired (STRP-05)', () => {
  test.todo('sets stripe_payment_status to abgelaufen without changing anfrage status');
  test.todo('skips duplicate expired events');
  test.todo('does NOT send email on expiry');
});

describe('handleChargeRefunded (STRP-09)', () => {
  test.todo('updates stripe_refunded_amount_cents from charge.amount_refunded');
  test.todo('sets stripe_payment_status to rueckerstattet for full refund');
  test.todo('sets stripe_payment_status to teilweise_erstattet for partial refund');
  test.todo('changes anfrage status to rueckerstattung_abgeschlossen only for full refund from rueckerstattung_ausstehend');
  test.todo('generates Gutschrift PDF on each refund event');
  test.todo('queues rueckerstattung email to customer');
});

describe('handleDisputeCreated (STRP-09)', () => {
  test.todo('sets stripe_payment_status to dispute');
  test.todo('sends immediate staff email with zahlung_dispute event type');
  test.todo('does NOT change anfrage status');
});

describe('Webhook idempotency (STRP-07)', () => {
  test.todo('duplicate checkout.session.completed does not create duplicate updates');
  test.todo('duplicate charge.refunded does not double-count refunded amount');
});

describe('Structured logging (STRP-10)', () => {
  test.todo('webhook handler uses console.info with [Stripe Webhook] prefix');
  test.todo('no console.log calls in webhook handler');
});
```

**3. Create tests/unit/test-stripe-helpers.test.ts (NEW FILE):**

```typescript
/**
 * Test stubs for stripe-helpers.ts (STRP-03)
 * Pure function tests -- no Stripe SDK mocking needed.
 */

describe('PAYMENT_STATUS_COLORS', () => {
  test.todo('has entries for all 6 payment statuses');
  test.todo('offen is amber, bezahlt is green, abgelaufen is red');
});

describe('PAYMENT_STATUS_LABELS', () => {
  test.todo('has German labels for all 6 payment statuses');
});

describe('getStripeDashboardUrl', () => {
  test.todo('returns test dashboard URL when STRIPE_SECRET_KEY starts with sk_test_');
  test.todo('returns live dashboard URL when STRIPE_SECRET_KEY starts with sk_live_');
  test.todo('builds correct URL for payments object type');
  test.todo('builds correct URL for customers object type');
});

describe('ZAHLUNGS_PANEL_VISIBLE_STATUSES', () => {
  test.todo('includes zahlungslink_versendet and all post-payment statuses');
  test.todo('does NOT include neu, in_bearbeitung, angebot_versendet, bestaetigt');
});

describe('REFUND_ALLOWED_STATUSES', () => {
  test.todo('includes bezahlt and all post-bezahlt statuses');
  test.todo('does NOT include rueckerstattung_ausstehend or rueckerstattung_abgeschlossen');
  test.todo('does NOT include early statuses (neu, in_bearbeitung, etc.)');
});
```

**4. Create tests/unit/test-stripe-refund.test.ts (NEW FILE):**

```typescript
/**
 * Test stubs for POST /api/stripe/refund (STRP-08)
 */

describe('Refund API route', () => {
  test.todo('rejects non-admin users with 403');
  test.todo('validates request body with Zod schema');
  test.todo('rejects refund when anfrage status is not in REFUND_ALLOWED_STATUSES');
  test.todo('rejects refund when no payment_intent_id on anfrage');
  test.todo('rejects refund amount exceeding remaining balance');
  test.todo('creates full Stripe refund when no amount_cents provided');
  test.todo('creates partial Stripe refund with specified amount_cents');
  test.todo('sets status to rueckerstattung_ausstehend for full refund');
  test.todo('does NOT change anfrage status for partial refund');
  test.todo('updates stripe_refunded_amount_cents cumulatively');
  test.todo('creates StatusHistorie entry with refund amount and reason');
  test.todo('returns success response with refund_id and amount');
  test.todo('checks optimistic lock version before processing');
  test.todo('handles StripeInvalidRequestError gracefully');
});
```

**5. Create tests/unit/test-stripe-redirect.test.ts (NEW FILE):**

```typescript
/**
 * Test stubs for GET /api/stripe/redirect/[anfrageId] (STRP-04, STRP-05)
 */

describe('Redirect route', () => {
  test.todo('redirects to existing valid Stripe checkout URL');
  test.todo('redirects to /zahlung/fehler when anfrage status is not zahlungslink_versendet');
  test.todo('redirects to /zahlung/fehler when anfrage not found');
  test.todo('auto-regenerates expired session and redirects to new URL');
  test.todo('expires old session before creating new one (STRP-06)');
  test.todo('queues zahlungslink_versendet email on regeneration');
  test.todo('stores new session data on anfrage after regeneration');
});

describe('Payment status polling route', () => {
  test.todo('returns stripe_payment_status from DB for valid session_id');
  test.todo('rejects invalid session_id format (must start with cs_)');
  test.todo('returns 404 for unknown session_id');
  test.todo('returns anfrage_id in response for client-side navigation');
});
```

IMPORTANT: Do NOT remove any existing test content in the existing files. Only APPEND the new describe blocks.
  </action>
  <verify>
    <automated>cd "/Users/janisjankewitz/Desktop/Arbeitsbereich/02_Programmierung/Christ Fensterhandel/playground/02-christ-fensterhandel-gsd" && npm test -- --testPathPattern="test-stripe" 2>&1 | tail -30</automated>
  </verify>
  <done>All 5 test files exist with .todo stubs covering STRP-01 through STRP-11. npm test --testPathPattern=stripe runs without errors. Existing tests in test-stripe-checkout and test-stripe-webhook are preserved.</done>
</task>

</tasks>

<verification>
- `npm test -- --testPathPattern="test-stripe"` runs without errors
- `ls tests/unit/test-stripe-*.test.ts` shows 5 files
- `grep -c "test.todo" tests/unit/test-stripe-checkout.test.ts` shows 15+ todo stubs
- `grep -c "test.todo" tests/unit/test-stripe-webhook.test.ts` shows 15+ todo stubs
- `grep -c "test.todo" tests/unit/test-stripe-helpers.test.ts` shows 10+ todo stubs
- `grep -c "test.todo" tests/unit/test-stripe-refund.test.ts` shows 14 todo stubs
- `grep -c "test.todo" tests/unit/test-stripe-redirect.test.ts` shows 11 todo stubs
</verification>

<success_criteria>
All Stripe test stub files exist and pass (as .todo). Every STRP requirement has at least one corresponding test.todo stub. Existing tests are preserved. npm test --testPathPattern=stripe exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/27-stripe-end-to-end/27-00-SUMMARY.md`
</output>
