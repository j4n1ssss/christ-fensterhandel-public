---
phase: 02-konfigurator-pipeline
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - src/components/konfigurator/steps/step-form.tsx
  - src/components/konfigurator/steps/step-masse.tsx
  - src/components/konfigurator/steps/step-farben.tsx
  - src/components/konfigurator/steps/step-verglasung-extras.tsx
  - src/components/konfigurator/step-content.tsx
  - src/components/konfigurator/preview/window-svg.tsx
  - src/components/konfigurator/preview/svg-parts/dimensions-label.tsx
  - src/components/konfigurator/preview/svg-parts/sprossen-overlay.tsx
autonomous: true
requirements:
  - KONF-08
  - KONF-09
  - KONF-10
  - KONF-11

must_haves:
  truths:
    - "Step 6 zeigt Fensterformen gefiltert nach Fluegelanzahl und Oeffnungsarten"
    - "Step 7 erlaubt Breite/Hoehe-Eingabe in mm mit Min/Max-Validierung vom Profil"
    - "Step 8 zeigt Aussen-/Innen-/Dichtungsfarben mit Gleich-wie-Aussen Option"
    - "Step 9 zeigt Verglasung, Schallschutz, Sicherheitsglas, Glasdekor, Sprossen, Extras"
    - "SVG zeigt Rahmenfarbe, Massangaben und Sprossen"
  artifacts:
    - path: "src/components/konfigurator/steps/step-form.tsx"
      provides: "Step 6: Fensterform selection"
    - path: "src/components/konfigurator/steps/step-masse.tsx"
      provides: "Step 7: Dimension input with validation"
    - path: "src/components/konfigurator/steps/step-farben.tsx"
      provides: "Step 8: Color selection (outside/inside/seal)"
    - path: "src/components/konfigurator/steps/step-verglasung-extras.tsx"
      provides: "Step 9: Glazing and extras multi-section"
  key_links:
    - from: "step-masse.tsx"
      to: "src/lib/konfigurator/schemas.ts"
      via: "getStepSchema(7, { profil }) for Zod validation"
      pattern: "getStepSchema"
    - from: "step-farben.tsx"
      to: "src/lib/konfigurator/filters.ts"
      via: "getFilteredOptions(8) for material-filtered colors"
      pattern: "getFilteredOptions.*8"
    - from: "window-svg.tsx"
      to: "src/lib/konfigurator/store.ts"
      via: "reads frameColor from farbeAussen selection"
      pattern: "farbeAussen|farb_code"
---

<objective>
Steps 6-9 des Konfigurators: Fensterform, Masseingabe mit Validierung, Farbauswahl (Aussen/Innen/Dichtung), und Verglasung/Extras. Die SVG-Vorschau wird erweitert um Rahmenfarbe, Massbeschriftung und Sprossen.

Purpose: Die zweite Haelfte der Konfiguration — Form, Groesse, Farben und Ausstattung. Nach diesem Plan fehlt nur noch die Zusammenfassung (Step 10). Diese Steps enthalten die komplexesten UI-Elemente: React Hook Form fuer Masseingabe, Drei-Farben-Auswahl, Multi-Section Verglasung.
Output: 4 funktionale Step-Components + vollstaendige SVG-Vorschau.
</objective>

<execution_context>
@/Users/janisjankewitz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/janisjankewitz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-konfigurator-pipeline/02-CONTEXT.md
@.planning/phases/02-konfigurator-pipeline/02-RESEARCH.md
@.planning/phases/02-konfigurator-pipeline/02-01-SUMMARY.md
@.planning/phases/02-konfigurator-pipeline/02-03-SUMMARY.md

<interfaces>
<!-- From Plan 02-01: -->
From src/lib/konfigurator/store.ts:
- useKonfiguratorStore with all selections, setSelection, resetDependentSteps

From src/lib/konfigurator/filters.ts:
- getFilteredOptions(step, cmsData, selections)

From src/lib/konfigurator/schemas.ts:
- getStepSchema(step, context?) — Step 7 returns Zod schema with dynamic min/max from profil

From src/components/konfigurator/ui/option-card.tsx:
- OptionCard: { title, description?, imageUrl?, badges?, selected, onClick }

<!-- From Plan 02-03: -->
From src/components/konfigurator/preview/window-svg.tsx:
- WindowSVG with composable SVG parts (frame, wing, handle, opening-indicator)

<!-- CMS Types: -->
From src/payload-types.ts:
- Fensterforman: { id, name, slug, beschreibung, bild, erlaubte_fluegelanzahl, erlaubte_oeffnungsarten }
- Profile.masse: { min_breite_mm, max_breite_mm, min_hoehe_mm, max_hoehe_mm }
- Farben: { id, name, slug, kategorie, ral_code, farb_code, bild, fuer_aussen, fuer_innen, erlaubte_materialien, aufpreis }
- Dichtungsfarben: { id, name, slug, farb_code }
- Verglasungen: { id, name, slug, beschreibung, ug_wert, aufpreis }
- Schallschutz: { id, name, slug, beschreibung, schallschutzklasse, aufpreis }
- Sicherheitsgla: { id, name, slug, beschreibung, typ, aufpreis }
- Glasdekore: { id, name, slug, beschreibung, bild, aufpreis }
- Sprossen: { id, name, slug, beschreibung, typ, aufpreis }
- Extra: { id, name, slug, beschreibung, bild, kategorie, aufpreis }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Steps 6-7 (Fensterform + Masseingabe mit RHF/Zod Validierung)</name>
  <files>
    src/components/konfigurator/steps/step-form.tsx,
    src/components/konfigurator/steps/step-masse.tsx,
    src/components/konfigurator/step-content.tsx,
    src/components/konfigurator/preview/window-svg.tsx,
    src/components/konfigurator/preview/svg-parts/dimensions-label.tsx
  </files>
  <action>
    **Step 6 — Fensterform (step-form.tsx):**
    'use client'. Get filtered forms: `getFilteredOptions(6, cmsData, selections)`. Filters by erlaubte_fluegelanzahl (selected fluegelanzahl must be in the list) AND erlaubte_oeffnungsarten (at least one selected opening type must be allowed). Render as OptionCard grid. Each card: title=name, description=beschreibung, imageUrl from bild. Show badge with form type. Selected: store.fensterform === item.id. Auto-advance on selection. Note: Most configs will show Rechteck. Special forms (Rundbogen, Dreieck, etc.) appear only when CMS data allows them.

    **Step 7 — Masse (step-masse.tsx):**
    'use client'. Uses React Hook Form with zodResolver. Get the selected profil from cmsData to extract masse constraints (min_breite_mm, max_breite_mm, min_hoehe_mm, max_hoehe_mm). Use getStepSchema(7, { profil: selectedProfil }) to get dynamic Zod schema.

    Layout: Two number inputs side by side:
    - "Breite (mm)" — input type="number", step=1, placeholder with min-max range hint
    - "Hoehe (mm)" — input type="number", step=1, placeholder with min-max range hint
    Below inputs: calculated Flaeche display: `{(breite * hoehe / 1_000_000).toFixed(2)} m2`
    Below that: constraint info text: "Erlaubter Bereich: {min_breite}–{max_breite} x {min_hoehe}–{max_hoehe} mm"

    Validation errors shown inline below each input (red text from RHF errors). Use Shadcn Input component if installed, otherwise styled Tailwind input with focus:ring-primary.

    On valid submit: setSelection('masse', { breite, hoehe }) + completeStep(7) + setStep(8).
    No auto-advance here — user clicks Weiter after entering valid dimensions.

    **SVG Dimensions Label (svg-parts/dimensions-label.tsx):**
    Shows width label below the frame and height label to the right. Format: "1200 mm" with small arrow lines. Only visible when masse is set.

    **Update window-svg.tsx:**
    Add dimensions-label rendering when masse is selected. Adjust viewBox proportions when masse changes (wider windows = wider viewBox ratio).

    **Update step-content.tsx:**
    Add mappings: 6 -> StepForm, 7 -> StepMasse.
  </action>
  <verify>
    <automated>cd /Users/janisjankewitz/Desktop/Arbeitsbereich/02_Programmierung/Christ\ Fensterhandel/playground/02-christ-fensterhandel-gsd && npx jest tests/unit/test-schemas.test.ts --passWithNoTests 2>&1 | tail -10</automated>
  </verify>
  <done>Step 6 shows filtered Fensterformen. Step 7 shows Breite/Hoehe inputs with dynamic min/max validation from profile, calculates Flaeche in m2. Invalid dimensions show inline error messages. SVG preview shows dimension labels when masse is entered.</done>
</task>

<task type="auto">
  <name>Task 2: Steps 8-9 (Farben + Verglasung/Extras) + SVG Farbe/Sprossen</name>
  <files>
    src/components/konfigurator/steps/step-farben.tsx,
    src/components/konfigurator/steps/step-verglasung-extras.tsx,
    src/components/konfigurator/step-content.tsx,
    src/components/konfigurator/preview/window-svg.tsx,
    src/components/konfigurator/preview/svg-parts/sprossen-overlay.tsx
  </files>
  <action>
    **Step 8 — Farben (step-farben.tsx):**
    'use client'. Three sections:

    Section 1: "Aussenfarbe" — Get filtered farben: `getFilteredOptions(8, cmsData, selections)` then filter by fuer_aussen === true. Render as color swatches (small square cards with farb_code as background color + name below). Alternatively use OptionCard with image from farb_code. Show kategorie as section groups (Standard, Dekor, Uni, RAL). Show aufpreis if > 0 ("+{aufpreis} EUR"). Selected: store.farbeAussen === item.id.

    Section 2: "Innenfarbe" — Checkbox "Gleich wie Aussenfarbe" (maps to store.gleichWieAussen boolean). If checked: farbeInnen = farbeAussen (visually synced, greyed out). If unchecked: show same color picker filtered by fuer_innen === true. Selected: store.farbeInnen === item.id.

    Section 3: "Dichtungsfarbe" — Show all dichtungsfarben (no filtering needed). Small color swatches with farb_code. Selected: store.dichtungsfarbe === item.id.

    Step complete when all three (farbeAussen, farbeInnen/gleichWieAussen, dichtungsfarbe) are selected.

    **Step 9 — Verglasung & Extras (step-verglasung-extras.tsx):**
    'use client'. Multi-section step with independent selections. Each section is collapsible or visually separated:

    Section 1: "Verglasung" (required) — Show all active verglasungen as OptionCards. Show ug_wert and aufpreis. Selected: store.verglasung.

    Section 2: "Schallschutz" (optional) — Show schallschutz options. Show schallschutzklasse badge + aufpreis. Selected: store.schallschutz. Include "Kein Schallschutz" default option.

    Section 3: "Sicherheitsglas" (optional) — Show sicherheitsglas options. Show typ badge (VSG Aussen/Innen/Beidseitig) + aufpreis. Selected: store.sicherheitsglas. Include "Keins" default.

    Section 4: "Glasdekor" (optional) — Show glasdekore with bild. aufpreis display. Selected: store.glasdekor. Include "Keins" default.

    Section 5: "Sprossen" (optional) — Show sprossen with typ badge + aufpreis. Selected: store.sprossen. Include "Keine" default.

    Section 6: "Extras" (optional, multi-select) — Show extras grouped by kategorie (Griffe, Beschlaege, Sonstiges). Each as toggle card. Selected: store.extras array.

    Step complete when verglasung is selected (rest optional).

    **SVG Sprossen Overlay (svg-parts/sprossen-overlay.tsx):**
    When sprossen is selected, overlay lines on each wing:
    - Wiener Sprossen: horizontal + vertical cross lines
    - Helima Sprossen: similar but thinner lines
    - Aufgesetzte: slightly offset lines
    Rendered as SVG lines within each wing rect.

    **Update window-svg.tsx:**
    - Read farbeAussen from store, resolve to farb_code from cmsData, apply as frame fill color
    - Render sprossen overlay when sprossen is selected
    - SVG now shows: frame (with color), wings, handles, opening indicators, dimensions, sprossen

    **Update step-content.tsx:**
    Add mappings: 8 -> StepFarben, 9 -> StepVerglasungExtras.
  </action>
  <verify>
    <automated>cd /Users/janisjankewitz/Desktop/Arbeitsbereich/02_Programmierung/Christ\ Fensterhandel/playground/02-christ-fensterhandel-gsd && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>Step 8 shows three color sections (Aussen/Innen/Dichtung) with "Gleich wie Aussen" toggle. Step 9 shows 6 sub-sections (Verglasung required, rest optional) with aufpreis display. SVG preview shows selected frame color and sprossen overlay. All 9 steps (1-9) are navigable in sequence.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — TypeScript compiles
2. `npx jest` — all tests pass
3. Steps 6-9 navigable at /konfigurator/fenster after completing steps 1-5
4. Step 6 filters fensterformen by selected fluegelanzahl + oeffnungsarten
5. Step 7 validates masse against profil min/max, shows errors for out-of-range values
6. Step 8 "Gleich wie Aussen" checkbox syncs innen with aussen color
7. Step 9 allows verglasung selection + optional extras
8. SVG shows frame color from selected Aussenfarbe, dimension labels, sprossen overlay
</verification>

<success_criteria>
- Step 6 correctly filters fensterformen by selected fluegelanzahl AND oeffnungsarten
- Step 7 uses React Hook Form + Zod with dynamic min/max from profile, calculates Flaeche
- Step 8 has 3-section color picker with "Gleich wie Aussen" toggle
- Step 9 has 6 sub-sections, verglasung required, rest optional, extras multi-select
- SVG preview reflects all selections: color, dimensions, sprossen
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-konfigurator-pipeline/02-04-SUMMARY.md`
</output>
