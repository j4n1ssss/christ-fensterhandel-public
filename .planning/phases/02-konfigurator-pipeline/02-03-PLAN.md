---
phase: 02-konfigurator-pipeline
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/components/konfigurator/steps/step-produkttyp.tsx
  - src/components/konfigurator/steps/step-material.tsx
  - src/components/konfigurator/steps/step-profil.tsx
  - src/components/konfigurator/steps/step-fluegel.tsx
  - src/components/konfigurator/steps/step-oeffnungsart.tsx
  - src/components/konfigurator/step-content.tsx
  - src/components/konfigurator/preview/window-svg.tsx
  - src/components/konfigurator/preview/svg-parts/frame.tsx
  - src/components/konfigurator/preview/svg-parts/wing.tsx
  - src/components/konfigurator/preview/svg-parts/handle.tsx
  - src/components/konfigurator/preview/svg-parts/opening-indicator.tsx
autonomous: true
requirements:
  - KONF-03
  - KONF-04
  - KONF-05
  - KONF-06
  - KONF-07

must_haves:
  truths:
    - "Step 1 zeigt Produkttypen als Bild-Karten, Auswahl navigiert zu Step 2"
    - "Step 2 zeigt Materialien gefiltert nach Produkttyp mit Lieferzeit/Garantie Badges"
    - "Step 3 zeigt Profile gefiltert nach Material mit technischen Daten (Uw-Wert, Kammern, Bautiefe)"
    - "Step 4 zeigt Fluegelanzahl gefiltert nach Produkttyp + optionale Zusatzlichter"
    - "Step 5 zeigt Oeffnungsarten pro Fluegel mit Griff-Seite bei Dreh/Dreh-Kipp"
    - "SVG Vorschau aktualisiert sich bei Fluegel-Auswahl und Oeffnungsart"
  artifacts:
    - path: "src/components/konfigurator/steps/step-produkttyp.tsx"
      provides: "Step 1: Produkttyp selection with image cards"
    - path: "src/components/konfigurator/steps/step-material.tsx"
      provides: "Step 2: Material selection filtered by produkttyp"
    - path: "src/components/konfigurator/steps/step-profil.tsx"
      provides: "Step 3: Profil selection filtered by material"
    - path: "src/components/konfigurator/steps/step-fluegel.tsx"
      provides: "Step 4: Fluegelanzahl + Zusatzlichter"
    - path: "src/components/konfigurator/steps/step-oeffnungsart.tsx"
      provides: "Step 5: Per-wing opening type + handle side"
  key_links:
    - from: "step-material.tsx"
      to: "src/lib/konfigurator/filters.ts"
      via: "getFilteredOptions(2, cmsData, selections)"
      pattern: "getFilteredOptions"
    - from: "step-profil.tsx"
      to: "src/lib/konfigurator/filters.ts"
      via: "getFilteredOptions(3, cmsData, selections)"
      pattern: "getFilteredOptions"
    - from: "step-oeffnungsart.tsx"
      to: "src/lib/konfigurator/store.ts"
      via: "setSelection('oeffnungsarten', wingOpenings)"
      pattern: "setSelection.*oeffnungsarten"
---

<objective>
Steps 1-5 des Konfigurators implementieren: Produkttyp, Material, Profil, Fluegel und Oeffnungsart. Jeder Step nutzt die konditionale Filterung aus Plan 01 und zeigt Optionen als Bild-Karten mit Badges. Die SVG-Vorschau wird erweitert um Fluegel und Oeffnungsart-Indikatoren.

Purpose: Die erste Haelfte des Konfigurator-Flows — von der Produkttyp-Auswahl bis zur Oeffnungsart pro Fluegel. Nach diesem Plan kann ein User die strukturgebenden Entscheidungen (Was, Woraus, Welches Profil, Wieviele Fluegel, Wie oeffnet es) treffen.
Output: 5 funktionale Step-Components + erweiterte SVG-Vorschau mit Fluegeln.
</objective>

<execution_context>
@/Users/janisjankewitz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/janisjankewitz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-konfigurator-pipeline/02-CONTEXT.md
@.planning/phases/02-konfigurator-pipeline/02-RESEARCH.md
@.planning/phases/02-konfigurator-pipeline/02-01-SUMMARY.md
@.planning/phases/02-konfigurator-pipeline/02-02-SUMMARY.md

<interfaces>
<!-- From Plan 02-01 (lib modules): -->

From src/lib/konfigurator/types.ts:
- CMSData: all collection arrays
- KonfiguratorSelections: all selection fields
- WingOpening: { wingIndex, oeffnungsart, griffSeite }
- StepConfig: { id, name, slug, isComplete }

From src/lib/konfigurator/store.ts:
- useKonfiguratorStore: Zustand hook with cmsData, isLoading, currentStep, completedSteps, selections, setSelection, completeStep, setStep, goToStep, resetDependentSteps

From src/lib/konfigurator/filters.ts:
- getFilteredOptions(step, cmsData, selections): returns filtered collection items
- STEP_DEPENDENCIES: dependency graph

<!-- From Plan 02-02 (UI shell): -->

From src/components/konfigurator/ui/option-card.tsx:
- OptionCard: { title, description?, imageUrl?, badges?, selected, onClick }

From src/components/konfigurator/ui/badge-group.tsx:
- BadgeGroup: { badges: { text, variant }[] }

<!-- CMS Types: -->
From src/payload-types.ts:
- Produkttypen: { id, name, slug, beschreibung, bild, erlaubte_materialien }
- Materialien: { id, name, slug, beschreibung, bild, lieferzeit_wochen, garantie_jahre, erlaubte_profile }
- Profile: { id, name_technisch, name_einfach, slug, beschreibung, bild, qualitaetsstufe, technische_daten: { uw_wert, kammern, bautiefe_mm, dichtungen }, masse: { min_breite_mm, max_breite_mm, min_hoehe_mm, max_hoehe_mm } }
- Fluegelanzahl: { id, name, slug, anzahl, fuer_produkttypen }
- Zusatzlichter: { id, name, slug, beschreibung, kombinierbar_mit }
- Oeffnungsarten: { id, name, slug, beschreibung, bild, fuer_fenster, fuer_balkontuer }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Steps 1-3 (Produkttyp, Material, Profil)</name>
  <files>
    src/components/konfigurator/steps/step-produkttyp.tsx,
    src/components/konfigurator/steps/step-material.tsx,
    src/components/konfigurator/steps/step-profil.tsx,
    src/components/konfigurator/step-content.tsx
  </files>
  <action>
    **Step 1 — Produkttyp (step-produkttyp.tsx):**
    'use client'. Read cmsData.produkttypen from store. Render as OptionCard grid (responsive: 1 col mobile, 2 cols tablet, 3 cols lg). Each card: title=name, description=beschreibung, imageUrl from bild (resolve Media object to URL), no badges. Selected card: store.produkttyp === item.id. onClick: setSelection('produkttyp', item.id) + resetDependentSteps(1) (this triggers cascade reset for Material, Profil, etc). Auto-advance: after selection, completeStep(1) + setStep(2).

    **Step 2 — Material (step-material.tsx):**
    'use client'. Get filtered materials: `getFilteredOptions(2, cmsData, selections)`. Render as OptionCard grid. Each card: title=name, description=beschreibung, imageUrl from bild. Badges: [{text: `${lieferzeit_wochen} Wochen Lieferzeit`, variant: 'info'}, {text: `${garantie_jahre} Jahre Garantie`, variant: 'success'}]. Add a "Beliebt" badge if material has flag or if it's the first (Claude's discretion). Selected: store.material === item.id. onClick: setSelection('material', item.id) + resetDependentSteps(2). Auto-advance on selection.

    **Step 3 — Profil (step-profil.tsx):**
    'use client'. Get filtered profiles: `getFilteredOptions(3, cmsData, selections)`. Render as OptionCard grid. Each card: title=name_einfach (user-friendly), subtitle=name_technisch (technical). Below card content, show technische_daten as specs: "Uw-Wert: {uw_wert} W/(m2K)", "Kammern: {kammern}", "Bautiefe: {bautiefe_mm} mm". Show qualitaetsstufe as colored badge (einstieg=gray, standard=blue, premium=gold, top=green). Selected: store.profil === item.id. onClick: setSelection + resetDependentSteps(3). Auto-advance.

    **Update step-content.tsx:**
    Import and map step components: 1 -> StepProdukttyp, 2 -> StepMaterial, 3 -> StepProfil. Steps 4-10 remain as placeholders for now.
  </action>
  <verify>
    <automated>cd /Users/janisjankewitz/Desktop/Arbeitsbereich/02_Programmierung/Christ\ Fensterhandel/playground/02-christ-fensterhandel-gsd && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>Step 1 renders produkttypen as cards, click selects and advances to Step 2. Step 2 shows filtered materials with Lieferzeit/Garantie badges. Step 3 shows filtered profiles with technical specs. All three steps cascade-reset downstream steps on selection change.</done>
</task>

<task type="auto">
  <name>Task 2: Steps 4-5 (Fluegel, Oeffnungsart) + SVG Fluegel-Darstellung</name>
  <files>
    src/components/konfigurator/steps/step-fluegel.tsx,
    src/components/konfigurator/steps/step-oeffnungsart.tsx,
    src/components/konfigurator/step-content.tsx,
    src/components/konfigurator/preview/window-svg.tsx,
    src/components/konfigurator/preview/svg-parts/frame.tsx,
    src/components/konfigurator/preview/svg-parts/wing.tsx,
    src/components/konfigurator/preview/svg-parts/handle.tsx,
    src/components/konfigurator/preview/svg-parts/opening-indicator.tsx
  </files>
  <action>
    **Step 4 — Fluegel (step-fluegel.tsx):**
    'use client'. Two sections:
    Section 1: "Fluegelanzahl" — get filtered options: `getFilteredOptions(4, cmsData, selections)`. Render as OptionCards. Each card: title=name (e.g. "1-fluegelig", "2-fluegelig"), description if available. Selected: store.fluegelanzahl === item.id.
    Section 2: "Zusatzlichter (optional)" — show checkboxes/toggle cards for Oberlicht/Unterlicht. Filter zusatzlichter by kombinierbar_mit (must contain selected fluegelanzahl). Multi-select: store.zusatzlichter array. Use smaller cards or toggle buttons instead of full OptionCards.
    onClick Fluegel: setSelection + resetDependentSteps(4). Auto-advance on fluegelanzahl selection (zusatzlichter is optional).

    **Step 5 — Oeffnungsart (step-oeffnungsart.tsx):**
    'use client'. This step is per-wing. Read anzahl from selected fluegelanzahl (resolve from cmsData). For each wing (wingIndex 0 to anzahl-1):
    - Section header: "Fluegel {n+1}" (or "Linker Fluegel / Rechter Fluegel" for 2-fluegelig)
    - Show filtered oeffnungsarten as OptionCards: `getFilteredOptions(5, cmsData, selections)`.
    - When Dreh or Dreh-Kipp selected for a wing: show Griff-Seite toggle (links/rechts).
    - When Fest or Kipp: griffSeite = null (no handle needed for Fest, Kipp has no side preference).
    - Store as WingOpening[]: `setSelection('oeffnungsarten', updatedArray)`.
    Step is complete when all wings have oeffnungsart selected (and griffSeite where required).

    **SVG Parts — Composable SVG Components:**
    Refactor window-svg.tsx into composable parts using SVG viewBox (e.g. viewBox="0 0 400 500"):

    `svg-parts/frame.tsx`: Outer frame rectangle. Accept frameColor (default #888). For 'rechteck' form: simple rect with stroke. Rounded corners optional. Fill with frameColor.

    `svg-parts/wing.tsx`: Single wing inside the frame. Positioned based on wingIndex and total wingCount (side by side). Shows a lighter inner rect. When Oberlicht/Unterlicht selected, wings are shorter to make room.

    `svg-parts/handle.tsx`: Small vertical line/dot on the wing edge. Positioned left or right based on griffSeite.

    `svg-parts/opening-indicator.tsx`: Shows opening type symbol inside/on the wing:
    - Fest: X mark (diagonal lines)
    - Kipp: small triangle at bottom edge (pointing up)
    - Dreh: curved arrow from hinge side
    - Dreh-Kipp: both triangle + curved arrow

    **Update window-svg.tsx:**
    Compose Frame + Wings + Handles + Opening indicators. Read from store: fluegelanzahl (anzahl), oeffnungsarten (WingOpening[]), zusatzlichter (oberlicht/unterlicht), frameColor (default until Step 8).

    **Update step-content.tsx:**
    Add mappings: 4 -> StepFluegel, 5 -> StepOeffnungsart.
  </action>
  <verify>
    <automated>cd /Users/janisjankewitz/Desktop/Arbeitsbereich/02_Programmierung/Christ\ Fensterhandel/playground/02-christ-fensterhandel-gsd && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>Step 4 shows filtered Fluegelanzahl cards + optional Zusatzlichter. Step 5 shows per-wing Oeffnungsart selection with Griff-Seite toggle for Dreh/Dreh-Kipp. SVG preview updates to show correct wing count, opening indicators, and handle positions. All 5 steps are navigable in sequence.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — TypeScript compiles
2. `npx jest` — existing tests still pass
3. Steps 1-5 are navigable in sequence at /konfigurator/fenster
4. Step 2 shows only materials linked to selected produkttyp
5. Step 3 shows only profiles linked to selected material
6. Step 4 shows only fluegelanzahl linked to selected produkttyp
7. Step 5 shows per-wing oeffnungsart selection
8. SVG updates when fluegel/oeffnungsart changes
</verification>

<success_criteria>
- All 5 step components render filtered options from CMS data
- Conditional filtering uses CMS relationship fields (not hardcoded)
- Badges show on Material cards (Lieferzeit, Garantie)
- Profile cards show technical specs (Uw-Wert, Kammern, Bautiefe)
- Per-wing opening selection with handle side toggle
- SVG preview shows wings with opening indicators
- Back-navigation and cascade reset work correctly
</success_criteria>

<output>
After completion, create `.planning/phases/02-konfigurator-pipeline/02-03-SUMMARY.md`
</output>
