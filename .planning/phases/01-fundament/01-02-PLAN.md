---
phase: 01-fundament
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/collections/produkte/produkttypen.ts
  - src/collections/produkte/materialien.ts
  - src/collections/produkte/profile.ts
  - src/collections/produkte/fluegelanzahl.ts
  - src/collections/produkte/zusatzlichter.ts
  - src/collections/produkte/oeffnungsarten.ts
  - src/collections/produkte/fensterformen.ts
  - src/payload.config.ts
autonomous: true
requirements:
  - CMS-01
  - CMS-02
  - CMS-03
  - CMS-04
  - CMS-05
  - CMS-06
  - CMS-07

must_haves:
  truths:
    - "All 7 Produkt-collections are visible in Payload Admin under 'Produkte' group"
    - "Each collection has name, slug, aktiv, and sortOrder fields"
    - "Relationship fields connect collections correctly (materialien -> erlaubte_profile, profile -> material, etc.)"
    - "Profile collection has technische_daten group (uw_wert, kammern, bautiefe) and masse group (min/max breite/hoehe)"
    - "Fensterformen has erlaubte_fluegelanzahl and erlaubte_oeffnungsarten multi-ref relationships"
  artifacts:
    - path: "src/collections/produkte/produkttypen.ts"
      provides: "Produkttypen collection (Fenster, Balkontuer)"
      exports: ["Produkttypen"]
    - path: "src/collections/produkte/materialien.ts"
      provides: "Materialien collection (Kunststoff, Holz, Alu, K-Alu)"
      exports: ["Materialien"]
    - path: "src/collections/produkte/profile.ts"
      provides: "Profile collection with technical data and dimension constraints"
      exports: ["Profile"]
    - path: "src/collections/produkte/fluegelanzahl.ts"
      provides: "Fluegelanzahl collection (1/2/3 Fluegel)"
      exports: ["Fluegelanzahl"]
    - path: "src/collections/produkte/zusatzlichter.ts"
      provides: "Zusatzlichter collection (Oberlicht, Unterlicht)"
      exports: ["Zusatzlichter"]
    - path: "src/collections/produkte/oeffnungsarten.ts"
      provides: "Oeffnungsarten collection (Fest, Kipp, Dreh, Dreh-Kipp)"
      exports: ["Oeffnungsarten"]
    - path: "src/collections/produkte/fensterformen.ts"
      provides: "Fensterformen collection (Rechteck, Rundbogen, etc.)"
      exports: ["Fensterformen"]
  key_links:
    - from: "src/collections/produkte/profile.ts"
      to: "src/collections/produkte/materialien.ts"
      via: "relationship field material -> materialien"
      pattern: "relationTo.*materialien"
    - from: "src/collections/produkte/fensterformen.ts"
      to: "src/collections/produkte/fluegelanzahl.ts"
      via: "relationship field erlaubte_fluegelanzahl -> fluegelanzahl"
      pattern: "relationTo.*fluegelanzahl"
    - from: "src/payload.config.ts"
      to: "src/collections/produkte/*.ts"
      via: "collections array imports"
      pattern: "import.*from.*collections/produkte"
---

<objective>
Create all 7 Produkt-group CMS collections: Produkttypen, Materialien, Profile, Fluegelanzahl, Zusatzlichter, Oeffnungsarten, Fensterformen.

Purpose: These collections define the core product data model for the window configurator. Every configurator step in Phase 2 queries these collections.
Output: 7 collection config files registered in payload.config.ts, visible in admin under "Produkte" group.
</objective>

<execution_context>
@/Users/janisjankewitz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/janisjankewitz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fundament/01-RESEARCH.md
@.planning/phases/01-fundament/01-CONTEXT.md
@.planning/phases/01-fundament/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Produkttypen, Materialien, and Profile collections</name>
  <files>src/collections/produkte/produkttypen.ts, src/collections/produkte/materialien.ts, src/collections/produkte/profile.ts</files>
  <action>
Create 3 collection config files in `src/collections/produkte/`:

**produkttypen.ts** (CMS-01):
- slug: `produkttypen`
- labels: singular 'Produkttyp', plural 'Produkttypen'
- admin.group: 'Produkte', useAsTitle: 'name'
- Fields: name (text, required), slug (text, required, unique, sidebar), beschreibung (textarea), bild (upload to media), erlaubte_materialien (relationship to materialien, hasMany: true)
- Standard fields: aktiv (checkbox, default true, sidebar), sortOrder (number, default 0, sidebar)
- All labels in German

**materialien.ts** (CMS-02):
- slug: `materialien`
- labels: singular 'Material', plural 'Materialien'
- admin.group: 'Produkte', useAsTitle: 'name'
- Fields: name (text, required), slug (text, required, unique, sidebar), beschreibung (textarea), bild (upload to media)
- lieferzeit_wochen (number, label 'Lieferzeit (Wochen)'), garantie_jahre (number, label 'Garantie (Jahre)')
- erlaubte_profile (relationship to profile, hasMany: true) -- NOTE: This creates a forward reference. Profile also references materialien. Both directions are useful: materialien knows its profiles, profile knows its material.
- aktiv (checkbox, default true, sidebar), sortOrder (number, default 0, sidebar)

**profile.ts** (CMS-03):
- slug: `profile`
- labels: singular 'Profil', plural 'Profile'
- admin.group: 'Produkte', useAsTitle: 'name_technisch'
- Fields: name_technisch (text, required, label 'Technischer Name'), name_einfach (text, required, label 'Einfacher Name'), slug (text, required, unique, sidebar), beschreibung (textarea), bild (upload to media)
- qualitaetsstufe (select: einstieg/standard/premium/top, label 'Qualitaetsstufe')
- technische_daten (group, label 'Technische Daten'): uw_wert (number, label 'Uw-Wert (W/m2K)'), kammern (number), bautiefe_mm (number, label 'Bautiefe (mm)'), dichtungen (number)
- masse (group, label 'Erlaubte Masse'): min_breite_mm, max_breite_mm, min_hoehe_mm, max_hoehe_mm (all number fields with German labels in mm)
- material (relationship to materialien, required, label 'Material')
- aktiv (checkbox, default true, sidebar), sortOrder (number, default 0, sidebar)

All field labels MUST be in German. Slugs and field names use snake_case.
  </action>
  <verify>
    <automated>cd /Users/janisjankewitz/Desktop/Arbeitsbereich/02_Programmierung/Christ\ Fensterhandel/playground/02-christ-fensterhandel-gsd && npx tsc --noEmit</automated>
  </verify>
  <done>Three collection files exist with correct fields, relationships, German labels, and admin group 'Produkte'</done>
</task>

<task type="auto">
  <name>Task 2: Create Fluegelanzahl, Zusatzlichter, Oeffnungsarten, Fensterformen and register all in payload.config.ts</name>
  <files>src/collections/produkte/fluegelanzahl.ts, src/collections/produkte/zusatzlichter.ts, src/collections/produkte/oeffnungsarten.ts, src/collections/produkte/fensterformen.ts, src/payload.config.ts</files>
  <action>
Create 4 more collection config files in `src/collections/produkte/`:

**fluegelanzahl.ts** (CMS-04):
- slug: `fluegelanzahl`
- labels: singular 'Fluegelanzahl', plural 'Fluegelanzahl'
- admin.group: 'Produkte', useAsTitle: 'name'
- Fields: name (text, required, e.g. "1-fluegelig"), slug (text, required, unique, sidebar), anzahl (number, required, label 'Anzahl Fluegel')
- fuer_produkttypen (relationship to produkttypen, hasMany: true, label 'Fuer Produkttypen') -- determines which product types allow this wing count
- aktiv (checkbox, default true, sidebar), sortOrder (number, default 0, sidebar)

**zusatzlichter.ts** (CMS-05):
- slug: `zusatzlichter`
- labels: singular 'Zusatzlicht', plural: 'Zusatzlichter'
- admin.group: 'Produkte', useAsTitle: 'name'
- Fields: name (text, required), slug (text, required, unique, sidebar), beschreibung (textarea)
- kombinierbar_mit (relationship to fluegelanzahl, hasMany: true, label 'Kombinierbar mit')
- aktiv (checkbox, default true, sidebar), sortOrder (number, default 0, sidebar)

**oeffnungsarten.ts** (CMS-06):
- slug: `oeffnungsarten`
- labels: singular 'Oeffnungsart', plural 'Oeffnungsarten'
- admin.group: 'Produkte', useAsTitle: 'name'
- Fields: name (text, required), slug (text, required, unique, sidebar), beschreibung (textarea), bild (upload to media)
- fuer_fenster (checkbox, default true, label 'Fuer Fenster')
- fuer_balkontuer (checkbox, default false, label 'Fuer Balkontuer')
- aktiv (checkbox, default true, sidebar), sortOrder (number, default 0, sidebar)

**fensterformen.ts** (CMS-07):
- slug: `fensterformen`
- labels: singular 'Fensterform', plural 'Fensterformen'
- admin.group: 'Produkte', useAsTitle: 'name'
- Fields: name (text, required), slug (text, required, unique, sidebar), beschreibung (textarea), bild (upload to media)
- erlaubte_fluegelanzahl (relationship to fluegelanzahl, hasMany: true, label 'Erlaubte Fluegelanzahl')
- erlaubte_oeffnungsarten (relationship to oeffnungsarten, hasMany: true, label 'Erlaubte Oeffnungsarten')
- aktiv (checkbox, default true, sidebar), sortOrder (number, default 0, sidebar)

**Update payload.config.ts:**
- Import all 7 Produkt collections
- Add them to the collections array in the Produkte section (after existing Users, Media)
- Keep the organized comment structure: // Produkte, // System

Run `npx tsc --noEmit` to verify no type errors.
  </action>
  <verify>
    <automated>cd /Users/janisjankewitz/Desktop/Arbeitsbereich/02_Programmierung/Christ\ Fensterhandel/playground/02-christ-fensterhandel-gsd && npx tsc --noEmit</automated>
  </verify>
  <done>All 7 Produkt-collections are created, registered in payload.config.ts, TypeScript compiles, collections will appear under 'Produkte' admin group</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- All 7 collection files exist in `src/collections/produkte/`
- `payload.config.ts` imports and registers all 7 collections
- Each collection has: German labels, admin.group 'Produkte', aktiv field, sortOrder field
- Profile has technische_daten and masse groups with correct number fields
- Fensterformen has multi-ref relationships to fluegelanzahl and oeffnungsarten
</verification>

<success_criteria>
- 7 Produkt-collection config files exist with correct fields and relationships
- All registered in payload.config.ts
- TypeScript compiles without errors
- Running `npm run dev` shows all collections in admin sidebar under "Produkte" group
</success_criteria>

<output>
After completion, create `.planning/phases/01-fundament/01-02-SUMMARY.md`
</output>
