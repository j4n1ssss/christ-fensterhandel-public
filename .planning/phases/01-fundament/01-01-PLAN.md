---
phase: 01-fundament
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/payload.config.ts
  - src/app/layout.tsx
  - src/collections/system/users.ts
  - src/collections/system/media.ts
  - .env
  - tailwind.config.ts
  - tsconfig.json
  - components.json
autonomous: true
requirements:
  - SETUP-01
  - SETUP-02
  - SETUP-03

must_haves:
  truths:
    - "Next.js dev server starts without errors on localhost:3000"
    - "Payload Admin Panel is accessible at /admin"
    - "PostgreSQL connection is established (no ECONNREFUSED)"
    - "TypeScript compiles with strict mode and no errors"
    - "Tailwind CSS is configured and Shadcn UI is initialized"
  artifacts:
    - path: "src/payload.config.ts"
      provides: "Payload CMS configuration with PostgreSQL adapter"
      contains: "postgresAdapter"
    - path: "src/collections/system/users.ts"
      provides: "Users collection with auth enabled"
      exports: ["Users"]
    - path: "src/collections/system/media.ts"
      provides: "Media collection for uploads"
      exports: ["Media"]
    - path: ".env"
      provides: "Environment variables for DATABASE_URL and PAYLOAD_SECRET"
      contains: "DATABASE_URL"
    - path: "tailwind.config.ts"
      provides: "Tailwind configuration"
    - path: "components.json"
      provides: "Shadcn UI configuration"
  key_links:
    - from: "src/payload.config.ts"
      to: "PostgreSQL"
      via: "postgresAdapter with DATABASE_URL"
      pattern: "postgresAdapter"
    - from: "src/payload.config.ts"
      to: "src/collections/system/users.ts"
      via: "collections array import"
      pattern: "import.*Users.*from"
---

<objective>
Bootstrap the Next.js + Payload CMS 3.0 project with PostgreSQL database connection, Tailwind CSS, and Shadcn UI.

Purpose: This is the foundation everything else builds on. No collections can be created without a working Payload setup connected to PostgreSQL.
Output: A running Next.js app with Payload Admin Panel at /admin, PostgreSQL connected, Tailwind + Shadcn initialized.
</objective>

<execution_context>
@/Users/janisjankewitz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/janisjankewitz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fundament/01-RESEARCH.md
@.planning/phases/01-fundament/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Payload CMS project with PostgreSQL and base collections</name>
  <files>package.json, src/payload.config.ts, src/app/layout.tsx, src/collections/system/users.ts, src/collections/system/media.ts, .env, tsconfig.json</files>
  <action>
1. Run `npx create-payload-app@latest . --template blank` in the project root (it is currently empty except .planning/ and CLAUDE.md). If create-payload-app asks for database, choose PostgreSQL. If it asks for package manager, choose npm.

2. After scaffolding, verify the installed Payload version is 3.79.x and @payloadcms/db-postgres is 3.78.x. Install sharp: `npm install sharp`.

3. Create `.env` with:
   - `DATABASE_URL=postgresql://postgres:postgres@localhost:5432/christ_fensterhandel`
   - `PAYLOAD_SECRET=christ-fensterhandel-dev-secret-change-in-production`

4. Ensure the PostgreSQL database `christ_fensterhandel` exists. Run: `createdb christ_fensterhandel 2>/dev/null || true`

5. Move/create collections into the organized structure:
   - `src/collections/system/users.ts` - Users collection with `auth: true`, admin group 'System', German labels. Keep the default Payload Users fields but add German labels.
   - `src/collections/system/media.ts` - Media collection with upload config, admin group 'System', German labels.

6. Update `src/payload.config.ts`:
   - Use `postgresAdapter` with `pool: { connectionString: process.env.DATABASE_URL }` and `idType: 'uuid'`
   - Use `lexicalEditor()` for rich text
   - Import and register Users and Media collections
   - Set `admin.user` to Users slug
   - Set `typescript.outputFile` to resolve to `src/payload-types.ts`

7. Ensure `tsconfig.json` has `strict: true`. Add path alias `@/*` pointing to `src/*` if not already present.

8. Verify the app starts: `npm run dev` should start without errors and Payload Admin should be accessible at http://localhost:3000/admin (it will prompt to create first user).

IMPORTANT: Do NOT use SQLite. The DATABASE_URL must point to PostgreSQL. Per user decision, PostgreSQL from day one.
  </action>
  <verify>
    <automated>cd /Users/janisjankewitz/Desktop/Arbeitsbereich/02_Programmierung/Christ\ Fensterhandel/playground/02-christ-fensterhandel-gsd && npx tsc --noEmit</automated>
  </verify>
  <done>Next.js + Payload app compiles without TypeScript errors, payload.config.ts uses PostgreSQL adapter with UUID IDs, Users and Media collections exist with German labels and System admin group</done>
</task>

<task type="auto">
  <name>Task 2: Initialize Tailwind CSS and Shadcn UI with project Design Tokens</name>
  <files>tailwind.config.ts, components.json, src/app/globals.css</files>
  <action>
1. Tailwind CSS should already be included by create-payload-app. Verify `tailwind.config.ts` exists. If Tailwind 4 is installed (check package.json), adapt config accordingly (Tailwind 4 uses CSS-based config). If Tailwind 3, use standard JS config.

2. Initialize Shadcn UI: `npx shadcn@latest init`. Choose:
   - Style: Default
   - Base color: Neutral (will be customized later with Style Guide)
   - CSS variables: Yes
   - Components directory: src/components/ui
   - Utils: src/lib/utils

3. Verify `components.json` exists after init.

4. Check that `.design/STYLE-GUIDE.md` exists in the project root. If it does, read it and update Tailwind config / CSS variables to match the style guide colors, fonts, and spacing. If it does not exist yet, leave defaults -- the style guide will be created before Phase 2 UI work.

5. Ensure `src/app/globals.css` imports Tailwind directives and has CSS variables for the theme.

NOTE: Shadcn components are not heavily used in Phase 1 (admin-only), but the setup must be ready for Phase 2.
  </action>
  <verify>
    <automated>cd /Users/janisjankewitz/Desktop/Arbeitsbereich/02_Programmierung/Christ\ Fensterhandel/playground/02-christ-fensterhandel-gsd && test -f components.json && test -f tailwind.config.ts && echo "Shadcn and Tailwind configured" || echo "MISSING"</automated>
  </verify>
  <done>Tailwind CSS config exists with project paths, Shadcn UI is initialized with components.json, globals.css has Tailwind directives and CSS variables</done>
</task>

</tasks>

<verification>
- `npm run dev` starts the Next.js app without errors
- Payload Admin Panel loads at http://localhost:3000/admin
- TypeScript compiles: `npx tsc --noEmit` exits 0
- `tailwind.config.ts` and `components.json` exist
- `.env` has DATABASE_URL pointing to PostgreSQL (not SQLite)
</verification>

<success_criteria>
- Next.js dev server runs on localhost:3000
- Payload Admin Panel accessible at /admin with user creation prompt
- PostgreSQL database connected (no connection errors in console)
- TypeScript strict mode, zero compile errors
- Tailwind CSS + Shadcn UI initialized and ready for Phase 2
</success_criteria>

<output>
After completion, create `.planning/phases/01-fundament/01-01-SUMMARY.md`
</output>
